---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 07
type: execute
wave: 3
depends_on:
  - 08-05
  - 08-06
files_modified: []
autonomous: false
requirements:
  - REL-01
must_haves:
  truths:
    - "Branch protection on main is configured to require the `lint-and-test` status check (job name from .github/workflows/ci.yml)"
    - "There is no maintainer-bypass route — required checks must pass; no `[skip eval]` token; admin enforcement enabled where practical"
    - "A pre-release RC tag (v2.0.0-rc.1 OR an `npm publish --dry-run` from a temp branch) has been exercised end-to-end through publish.yml to validate the workflow extension lands correctly"
    - "The CONTRIBUTING.md cross-reference to branch protection is accurate (status-check name matches actual config)"
  artifacts: []
  key_links:
    - from: "GitHub Settings → Branches → main"
      to: ".github/workflows/ci.yml jobs.lint-and-test"
      via: "Required status check"
      pattern: "lint-and-test"
---

<objective>
Configure GitHub branch protection on `main` to require the `lint-and-test` CI status check (D-06), then perform an RC-tag dry-run of the publish.yml workflow extension landed in plan 08-06 to validate it produces the expected Release assets BEFORE the live v2.0.0 cut in plan 08-08.

Purpose: REL-01 — eval suite is required-for-merge with no maintainer override. Mechanical validation of plan 08-06's publish.yml extension before the v2.0.0 cut (per RESEARCH Open Q3 recommendation).

Output: Branch protection rule configured (GitHub Settings change, not a repo file); an RC tag exercise log capturing whether publish.yml produces the expected assets.

No repo files are modified in this plan — it's GitHub Settings + an RC-tag/cleanup ritual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/publish.yml
@CONTRIBUTING.md

<interfaces>
Branch protection target (D-06 + RESEARCH §"State of the Art"):
- Repository: owrede/vault-memory
- Branch pattern: main
- Required status check: lint-and-test (job name from .github/workflows/ci.yml:16)
- Settings:
  - Require pull request before merging: ON (1 approver — solo maintainer self-approves via a separate session OR uses GitHub auto-merge after CI green)
  - Require status checks to pass before merging: ON
  - Status checks: lint-and-test
  - Require branches to be up to date before merging: ON (forces rebase before merge)
  - Do NOT enable: Require signed commits (out of v2.0.0 scope)
  - Do NOT enable: Restrict who can push to matching branches (solo maintainer)
  - DO enable: Include administrators (per D-06 "no override")
  - DO enable: Allow force pushes (set to "No one") — prevent force-push to main
- Verification: gh api repos/owrede/vault-memory/branches/main/protection | jq '.required_status_checks.contexts' returns ["lint-and-test"]

RC tag dry-run (RESEARCH Open Q3):
- Create a temp tag: v2.0.0-rc.1
- This MUST be done after plan 08-06 lands (publish.yml extension) but BEFORE plan 08-08 (v2.0.0 cut)
- Push the RC tag to trigger publish.yml
- Observe: did the workflow build the plugin tarball? Did it generate manifest.sha256? Did it create a draft Release with both attached?
- IMPORTANT: npm publish should NOT actually publish an RC to npm. Per CONTEXT §"Specifics — No `v2.0.0-rc1` in the public NPM channel" — either: (a) make the RC tag a draft Release only (no npm publish run); (b) modify publish.yml temporarily to skip the npm publish step for RC tags; or (c) accept that the RC tag triggers npm publish and that v2.0.0-rc.1 ends up on npm (with a `pre-release` flag) — this is acceptable per semver pre-release semantics
- RECOMMENDED: option (c) — let the RC tag run through the full workflow including npm publish, then delete the RC tag and the npm version with `npm unpublish` (only valid within 72 hours of publish per npm policy)
- ALTERNATIVE simpler dry-run: run `npm publish --dry-run` locally from a feature branch to verify the package builds; for the Release-asset verification, push the RC tag to a fork (alternate repo) to exercise the workflow without polluting main
- PLANNER DECISION: use the local `npm publish --dry-run` for the npm-side validation; use the v2.0.0-rc.1 tag for the Release-asset workflow validation, accepting that v2.0.0-rc.1 will briefly exist as a published version on npm + GitHub Releases and will be cleaned up immediately

Cleanup after RC dry-run:
- Delete the GitHub Release for v2.0.0-rc.1 (`gh release delete v2.0.0-rc.1 --yes`)
- Delete the remote and local git tags (`git push origin :v2.0.0-rc.1; git tag -d v2.0.0-rc.1`)
- npm unpublish @owrede/vault-memory@2.0.0-rc.1 (within 72-hour window)
- Revert any version-bump commit if release.mjs was used (per RESEARCH this is what Plan 08-06 Task 2 verifies the smoke-test path for)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Configure branch protection on main via GitHub Settings (D-06)</name>
  <what-built>Branch protection rule on main requiring the lint-and-test CI status check. Configured via GitHub Settings web UI (no repo file changes).</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    Step-by-step configuration:
    1. Navigate to https://github.com/owrede/vault-memory/settings/branches
    2. Click "Add rule" (or edit existing rule for main)
    3. Branch name pattern: main
    4. Check "Require a pull request before merging"
       - Required approvals: 1 (or 0 for solo flow — but 1 is safer)
       - Dismiss stale pull request approvals when new commits are pushed: optional
       - Require review from Code Owners: NO
    5. Check "Require status checks to pass before merging"
       - Check "Require branches to be up to date before merging"
       - In the status checks search box, type "lint-and-test" and select it (it must already exist as a previous workflow run — if not, run any PR through CI first so the check name registers)
    6. Check "Restrict pushes that create matching branches" — leave UNCHECKED (solo maintainer)
    7. Under "Rules applied to everyone including administrators" — check "Include administrators" (per D-06 no-override)
    8. Under "Allow force pushes" — select "No one"
    9. Under "Allow deletions" — leave UNCHECKED
    10. Click "Create" / "Save changes"

    Verification (after save):
    - Run: `gh api repos/owrede/vault-memory/branches/main/protection | jq '.required_status_checks.contexts'`
    - Expected output: `["lint-and-test"]` (or includes that string)
    - Run: `gh api repos/owrede/vault-memory/branches/main/protection | jq '.enforce_admins.enabled'`
    - Expected output: `true`

    If the lint-and-test check is not selectable:
    - Open a draft PR with any trivial change
    - Wait for CI to run once so the check name registers in the org-level catalog
    - Then return to Settings → Branches and add the rule
    - Close/delete the draft PR after the rule is saved
  </how-to-verify>
  <acceptance_criteria>
    - `gh api repos/owrede/vault-memory/branches/main/protection | jq '.required_status_checks.contexts'` returns an array including "lint-and-test"
    - `gh api repos/owrede/vault-memory/branches/main/protection | jq '.enforce_admins.enabled'` returns true
    - Force-push to main is disallowed
    - Maintainer screenshots the configured rule for the sign-off doc (plan 08-08)
  </acceptance_criteria>
  <resume-signal>Type "branch-protected" with the jq output OR describe blockers</resume-signal>
  <done>main branch protected with lint-and-test required and admins included.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: RC dry-run via npm publish --dry-run (npm side validation)</name>
  <what-built>Mechanical validation that the npm package builds correctly without actually publishing.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    1. From the repo root with a clean working tree:
       ```
       npm run build
       npm publish --dry-run --access public
       ```
    2. Expected output: a list of files that would be published. Confirm:
       - `dist/cli.js` is included (the CLI bundle)
       - `README.md` is included
       - `LICENSE` is included
       - The package size is reasonable (under ~5 MB; the ONNX model and the SQLite native binaries are NOT in the package — they're external/runtime-installed)
       - No `.test.ts` files, no `tests/` directory, no `evals/`, no `.planning/` is included (the `files:` whitelist in package.json governs this)
       - The `bin.vault-memory` field points at `dist/cli.js`
       - The package name is `@owrede/vault-memory` and version is currently `1.0.0` (release.mjs will bump to `2.0.0` in plan 08-08)
    3. If anything looks wrong (e.g., source maps included, test files leaking, missing dist/), STOP and investigate before plan 08-08.
  </how-to-verify>
  <acceptance_criteria>
    - npm publish --dry-run exits 0
    - The would-be-published file list is correct (only dist/, README.md, LICENSE)
    - Package size <5 MB
    - No accidental private files leaking
  </acceptance_criteria>
  <resume-signal>Type "dry-run-clean" with the file list OR describe issues</resume-signal>
  <done>npm publish dry-run validates the package shape.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: RC tag dry-run of publish.yml workflow extension (Release-asset side)</name>
  <what-built>End-to-end validation that publish.yml correctly builds the plugin tarball + generates manifest.sha256 + attaches both as Release assets when triggered by a tag push.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    Per RESEARCH Open Q3 recommendation: cut a v2.0.0-rc.1 tag, observe the workflow, then clean up immediately.

    1. Pre-flight:
       - Ensure plans 08-01 through 08-06 have all landed on main
       - Working tree clean; on main
       - Bump package.json version locally to "2.0.0-rc.1" (manually: `npm version 2.0.0-rc.1 --no-git-tag-version`)
       - Commit + push: `git commit -am "release: v2.0.0-rc.1 (dry-run)"; git push origin main`

    2. Trigger the workflow:
       - `git tag -a v2.0.0-rc.1 -m "v2.0.0-rc.1 dry-run"`
       - `git push origin v2.0.0-rc.1`

    3. Monitor the workflow:
       - `gh run watch` (or open https://github.com/owrede/vault-memory/actions)
       - Verify the workflow runs to completion (npm publish + GitHub Release create)

    4. Verify the assets:
       - `gh release view v2.0.0-rc.1 --json assets | jq '.assets[].name'`
       - Expected output includes: "vault-memory-plugin-v2.0.0-rc.1.tar.gz" AND "manifest.sha256"
       - `gh release view v2.0.0-rc.1 --json body | jq -r '.body'` → should print the RC-1 CHANGELOG section (or whatever is between `## [2.0.0-rc.1]` and the next `## [`)

    5. CLEANUP (CRITICAL — do not skip):
       - `gh release delete v2.0.0-rc.1 --yes`
       - `git push origin :v2.0.0-rc.1`
       - `git tag -d v2.0.0-rc.1`
       - `npm unpublish @owrede/vault-memory@2.0.0-rc.1` (must be within 72 hours of publish — npm policy)
       - Revert the version bump commit: `git revert <commit-sha>` OR `git reset --hard <previous-commit>; git push --force-with-lease origin main` (NOTE: force-push to main is blocked by branch protection from Task 1; this means the revert must go through a PR — acceptable since it's a content-only revert)
       - PREFERRED CLEAN PATH: do the version bump + tag on a temporary branch (e.g., `release-dry-run`) instead of main; push the tag from that branch; then delete the branch and the tag after the workflow run. This avoids touching main with a discardable RC version bump.

    6. Document findings:
       - Did the tarball build step run? Did it produce the expected filename?
       - Did manifest.sha256 generate?
       - Were both attached to the Release?
       - Did the CHANGELOG extract awk command produce the expected release body?
       - Note any failure modes for plan 08-08 to handle.
  </how-to-verify>
  <acceptance_criteria>
    - Workflow ran to completion (green)
    - Release assets included vault-memory-plugin-v2.0.0-rc.1.tar.gz and manifest.sha256
    - The Release body contained the CHANGELOG section for the RC version
    - All cleanup steps completed: tag deleted, npm unpublished, version-bump reverted, branch deleted (if temp-branch path used)
    - Findings logged for plan 08-08
  </acceptance_criteria>
  <resume-signal>Type "rc-dry-run-clean" with the asset list and cleanup confirmation OR describe failures</resume-signal>
  <done>publish.yml extension validated end-to-end; ready for the live v2.0.0 cut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Settings UI→branch-protection API | Configured via web UI; auditability via `gh api repos/.../branches/main/protection` |
| RC tag→publish.yml workflow | Triggering publish.yml with an RC tag invokes npm publish; supply-chain attestation present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-E | Elevation of privilege | Maintainer bypass of branch protection | mitigate | `enforce_admins: true` in branch protection config (Task 1); no `[skip eval]` token |
| T-08-07-SC | Supply chain | RC version polluting npm | mitigate | Cleanup step (npm unpublish + tag delete) within 72-hour npm window; temp-branch path keeps main clean |
| T-08-07-T | Tampering | force-push to main hides RC artifacts | mitigate | "Allow force pushes: No one" configured in Task 1 |
</threat_model>

<verification>
- gh api branch-protection returns lint-and-test as required check + enforce_admins true
- npm publish --dry-run produces clean file list
- RC tag dry-run produces expected Release assets
- All cleanup completed; no RC artifacts persist
</verification>

<success_criteria>
Branch protection on main configured per D-06; publish.yml validated end-to-end via RC tag; ready for live v2.0.0 cut in plan 08-08.
</success_criteria>

<output>
Create .planning/phases/08-polish-eval-suite-v2-0-0-release/08-07-SUMMARY.md documenting: branch-protection config (jq output snippets), npm dry-run file list, RC dry-run asset list, cleanup confirmation.
</output>
