---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 05
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/server.ts
  - src/tool-registry.ts
  - src/memory/resources/index.ts
  - src/resource-registry.ts
  - evals/v1-baseline/tools-list.snapshot.json
  - evals/v1-baseline/resources-list.snapshot.json
  - evals/v1-baseline/dump-resources.mjs
  - evals/v1-baseline/baseline.test.ts
  - scripts/smoketest-non-claude.mjs
  - package.json
autonomous: true
requirements:
  - REL-08
must_haves:
  truths:
    - "After this plan: client.listTools() returns exactly 32 tools when [plugin] is OFF (37 minus 5 deprecated)"
    - "client.listResources() returns exactly 10 Resources (5 existing + 5 new — vaults, models, recent, stats, backlinks)"
    - "All 5 deprecated tools remain CALLABLE (their handlers unchanged); only their description has a deprecation suffix"
    - "Each new Resource read handler delegates to the same internal function the existing tool handler calls (no logic duplication; per RESEARCH §Phase-9 GAT-01 seam preservation)"
    - "Both snapshot files are byte-deterministic — re-running dump scripts produces zero diff"
  artifacts:
    - path: "src/resource-registry.ts"
      provides: "Canonical RESOURCES literal array (5 existing + 5 new) for snapshot generation"
      contains: "vault-memory://vaults"
    - path: "evals/v1-baseline/resources-list.snapshot.json"
      provides: "Pinned Resources surface snapshot (10 entries)"
      contains: "vault-memory://vaults"
    - path: "evals/v1-baseline/dump-resources.mjs"
      provides: "Resources snapshot regeneration script (parallel to dump-tools.mjs)"
      min_lines: 15
    - path: "src/server.ts"
      provides: "5 new server.registerResource calls + 5 description-only edits in tool registrations"
      contains: "vault-memory://vaults"
  key_links:
    - from: "src/server.ts (Resource read handler for vault-memory://vaults)"
      to: "src/server.ts handleListVaults (existing function)"
      via: "direct call from inside the Resource read handler"
      pattern: "handleListVaults\\(manager\\)"
    - from: "evals/v1-baseline/baseline.test.ts"
      to: "evals/v1-baseline/resources-list.snapshot.json"
      via: "snapshot equality assertion"
      pattern: "resources-list.snapshot.json"
    - from: "scripts/smoketest-non-claude.mjs"
      to: "10 Resource URIs"
      via: "EXPECTED_RESOURCE_URIS array assertion"
      pattern: "EXPECTED_RESOURCE_URIS"
---

<objective>
Execute REL-08: promote 5 list-style v1 tools to MCP Resources to land default tool surface at exactly 32 (37 − 5). Each promotion is ADDITIVE — the tool stays callable with its description annotated as deprecated; a new Resource is registered with a parallel URI. The Resource read handler MUST delegate to the same internal function the tool handler already calls (no logic duplication, per RESEARCH GAT-01 seam preservation). Land a new Resources snapshot file and extend the baseline test + non-Claude smoketest to assert the new surface.

Purpose: REL-08 — tool surface inventory ≤32 tools at v2.0.0 ship.

Output: 5 new Resource registrations in src/server.ts + 5 deprecation notes in src/tool-registry.ts + new src/resource-registry.ts canonical literal + evals/v1-baseline/{resources-list.snapshot.json, dump-resources.mjs} + extended baseline.test.ts + extended scripts/smoketest-non-claude.mjs.

Closed set (verified via RESEARCH §"Tool Surface Today"):
- list_vaults → vault-memory://vaults (static URI)
- list_models → vault-memory://models/{vault} (ResourceTemplate)
- recent_notes → vault-memory://recent/{vault} (ResourceTemplate)
- vault_stats → vault-memory://stats/{vault} (ResourceTemplate)
- list_backlinks → vault-memory://backlinks/{vault}/{docId} (ResourceTemplate, two variables; per Phase 5 sign-off recommendation)

The CHANGELOG deprecation block for these 5 tools was landed by plan 08-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@src/server.ts
@src/tool-registry.ts
@src/memory/resources/index.ts
@evals/v1-baseline/baseline.test.ts
@evals/v1-baseline/dump-tools.mjs
@evals/v1-baseline/tools-list.snapshot.json
@scripts/smoketest-non-claude.mjs

<interfaces>
Existing Resource registration pattern (verbatim from src/server.ts:1756-1775, memory-sinks):
- Call shape: server.registerResource(name, uriOrTemplate, metadata, handler)
- Static URI form: pass URI string as 2nd arg (used for list_vaults — vaults are global, not per-vault)
- Templated form: new ResourceTemplate(`${URI_CONST}/{vault}`, { list: undefined })
- Two-variable form: new ResourceTemplate(`${URI_CONST}/{vault}/{docId}`, { list: undefined }) (used for list_backlinks)
- Metadata: { title, description, mimeType: "application/json" }
- Handler async (uri, variables?) returns { contents: [{ uri: uri.href, mimeType: "application/json", text: JSON.stringify(payload, null, 2) }] }

Existing internal handlers to call (DO NOT duplicate logic per GAT-01):
- handleListVaults(manager) — already exists at src/server.ts ~line 2146-2168; returns { vaults: [...], count: N }
- handleListModels(vault) — exists; called by the list_models tool handler
- handleVaultStats(vault) — exists; called by vault_stats
- handleRecentNotes(vault, args) — exists; called by recent_notes
- handleListBacklinks(vault, args) — exists; called by list_backlinks
Each handler accepts vault (and optional args). Resource read handler extracts {vault} (and {docId}) from variables, looks up the vault via manager.require(vault), then calls the handler with default args.

URI constants barrel (existing): src/memory/resources/index.ts:18-34
- Already exports: RESOURCE_URI_LIST_SINKS, RESOURCE_URI_MEMORY_STATS, RESOURCE_URI_LIST_BRIEFS, RESOURCE_URI_LIST_CONTRACTS, RESOURCE_URI_LIST_CONTRACT_VERBS
- ADD: RESOURCE_URI_VAULTS, RESOURCE_URI_MODELS, RESOURCE_URI_RECENT, RESOURCE_URI_STATS, RESOURCE_URI_BACKLINKS

Tool deprecation pattern (per PATTERNS Group B):
- Edit src/tool-registry.ts entries at: list_vaults (line ~42), list_backlinks (line ~198), list_models (line ~341), vault_stats (line ~448), recent_notes (line ~459)
- ONLY change the description string (preserve name and inputSchema byte-identical)
- Suffix template: " DEPRECATED since v2.0.0 — prefer MCP Resource `<uri>` for agent discovery. The tool remains callable through v2.x; removal scheduled for v3.0.0."

Resource snapshot infrastructure (NEW):
- Create src/resource-registry.ts exporting a RESOURCES literal array with shape: { name, uriTemplate, description, mimeType }[] for all 10 Resources (5 existing + 5 new). The existing 5 must be back-filled into this literal so the snapshot reflects the full surface.
- Wire src/server.ts registerResource calls to iterate over RESOURCES (or keep individual calls but ensure the metadata matches RESOURCES — planner picks; iteration is cleaner).
- Create evals/v1-baseline/dump-resources.mjs mirroring dump-tools.mjs: import RESOURCES, emit { resources: RESOURCES } to stdout.
- Generate evals/v1-baseline/resources-list.snapshot.json by running the script.
- Extend evals/v1-baseline/baseline.test.ts with a sibling describe block: "v2 resources/list surface (REL-08)" with two it() blocks — snapshot equality and length === 10.
- Update the existing "has exactly 37 tools" assertion to "has exactly 32 tools after REL-08 promotion" — TOOLS literal does NOT shrink (deprecated tools stay), but the count in the assertion description text needs updating only if it asserts a number; per RESEARCH this is the existing assertion at baseline.test.ts ~line 78.

Wait — CLARIFY: TOOLS literal has 37 entries. Promotion is additive: NO tools removed. So:
- TOOLS.length stays 37 (deprecated tools still in the array)
- expect(TOOLS).toHaveLength(37) STAYS as-is — no change needed there
- Per CONTEXT/RESEARCH: REL-08 target "≤32" applies to the COUNTABLE / non-deprecated tools; the 5 deprecated tools are still in tools/list but are marked deprecated in description.

Re-reading REL-08: "Tool surface inventory ≤32 tools (with MCP Resources promotion)". RESEARCH §"Closed-set recommendation" says "After promotion: 37 − 5 = 32 tools (D-01 target hit)". This implies the 5 deprecated tools DO leave the tools/list output — otherwise the "≤32 tools" claim is false.

REVISED INTERPRETATION (anchored to D-03):
D-03 says "additive then deprecating. v2.0.0 ships the Resource AND keeps the Tool (deprecated in description, removed in v3.0.0)."
Therefore: tools/list still returns 37 entries in v2.0.0. The "32 tools" framing is the CANONICAL DEFAULT SURFACE (the resources are the canonical discovery path; the tools are deprecated callable equivalents).

For the snapshot test, this means TOOLS literal STAYS at 37 — the assertion at line 78 is unchanged. The "32 tools after REL-08" framing belongs in CHANGELOG / README / MIGRATION docs as the "canonical surface" narrative, not in the literal tools-list count.

Final snapshot test state:
- tools-list.snapshot.json: 37 entries (5 with deprecation notice in description; bytes change but length doesn't)
- resources-list.snapshot.json: 10 entries (NEW)
- expect(TOOLS).toHaveLength(37) STAYS
- expect(RESOURCES).toHaveLength(10) — NEW

The smoketest at scripts/smoketest-non-claude.mjs:190-256:
- EXPECTED_TOOLS stays at 37 entries (D-03 additive-only)
- Add EXPECTED_RESOURCE_URIS array with 10 URI strings
- Add a new assertion block matching the existing tool-count block, but for resources
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add URI constants + create src/resource-registry.ts canonical RESOURCES literal</name>
  <files>src/memory/resources/index.ts, src/resource-registry.ts</files>
  <read_first>
    - src/memory/resources/index.ts (existing 5 URI constants + their barrel pattern)
    - src/server.ts lines 1756-1920 (the 5 existing Resource registrations — to back-fill their metadata into RESOURCES)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group A.1" and §"Group C.1"
  </read_first>
  <action>Extend src/memory/resources/index.ts by adding 5 new exported const strings: RESOURCE_URI_VAULTS = "vault-memory://vaults", RESOURCE_URI_MODELS = "vault-memory://models", RESOURCE_URI_RECENT = "vault-memory://recent", RESOURCE_URI_STATS = "vault-memory://stats", RESOURCE_URI_BACKLINKS = "vault-memory://backlinks". (Base URIs without /{vault} suffix — the registerResource ResourceTemplate appends the template variable.) Then create src/resource-registry.ts exporting a RESOURCES literal: an array of { name: string, uriTemplate: string, description: string, mimeType: "application/json" } objects covering ALL 10 Resources (the 5 existing — memory-sinks, memory-stats, briefs, contracts, contract-verbs — with their existing metadata, plus the 5 new ones). For each new entry, the description must include the "Promoted from the `<tool_name>` MCP tool in v2.0.0; the tool remains callable through v2.x" suffix. Export `RESOURCES` as a `readonly` array. The uriTemplate field carries the URI WITH the variable expansion form: e.g. "vault-memory://models/{vault}" for templated, "vault-memory://vaults" for static, "vault-memory://backlinks/{vault}/{docId}" for two-var.</action>
  <verify>
    <automated>test -f src/resource-registry.ts &amp;&amp; grep -q "RESOURCE_URI_VAULTS" src/memory/resources/index.ts &amp;&amp; grep -q "vault-memory://backlinks" src/resource-registry.ts &amp;&amp; node -e "import('./src/resource-registry.ts').catch(()=>{}).then(()=>process.exit(0))" 2>/dev/null; npx tsc --noEmit 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 5 new URI constants exported from src/memory/resources/index.ts
    - src/resource-registry.ts exists with RESOURCES array of length 10
    - npx tsc --noEmit passes (no type errors)
    - Each new Resource entry has a "Promoted from ... v2.0.0" deprecation marker in description
  </acceptance_criteria>
  <done>URI constants + canonical RESOURCES literal ready for Task 2 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Register 5 new Resources in src/server.ts; delegate handlers to existing internal functions</name>
  <files>src/server.ts</files>
  <read_first>
    - src/server.ts lines 1756-1920 (existing 5 Resource registrations — verbatim analog patterns)
    - src/server.ts ~line 2146-2168 (handleListVaults function)
    - src/server.ts (search for handler functions: handleListModels, handleVaultStats, handleRecentNotes, handleListBacklinks — note their signatures)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group A.1, A.2, A.3"
  </read_first>
  <action>In src/server.ts, after the existing 5 server.registerResource calls (around line 1920), add 5 new registerResource calls. Use the static-URI form for "vaults" (analog: memory-sinks at :1756). Use the ResourceTemplate form for "models", "recent", "stats" (analog: contracts at :1850). Use the two-variable ResourceTemplate form for "backlinks". Each read handler MUST: (a) extract variables (vault, docId for backlinks); (b) call manager.require(vault) (or manager.list() for vaults) to resolve the vault — return an in-band error JSON if not found, matching the pattern at src/server.ts:1864-1878; (c) call the existing internal handler function (handleListVaults / handleListModels / etc.) with that vault — DO NOT inline new logic; (d) return { contents: [{ uri: uri.href, mimeType: "application/json", text: JSON.stringify(payload, null, 2) }] }. Source the metadata (title, description, mimeType) from src/resource-registry.ts RESOURCES literal (import it). Each handler must be ≤25 LOC of glue.</action>
  <verify>
    <automated>grep -c "server.registerResource" src/server.ts | awk '{if ($1 &gt;= 10) print "OK"; else { print "FAIL count " $1; exit 1 } }' &amp;&amp; grep -q "vault-memory://vaults" src/server.ts &amp;&amp; grep -q "vault-memory://backlinks" src/server.ts &amp;&amp; grep -q "handleListVaults\|listVaults" src/server.ts &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "server.registerResource" src/server.ts returns ≥10 (5 existing + 5 new)
    - All 5 new URIs literally present
    - Each new Resource read handler calls an existing handle*() function (no logic duplication)
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>5 new Resources registered; handlers delegate to existing internal functions.</done>
</task>

<task type="auto">
  <name>Task 3: Add deprecation notes to 5 tool entries in src/tool-registry.ts</name>
  <files>src/tool-registry.ts</files>
  <read_first>
    - src/tool-registry.ts (TOOLS array entries at lines ~42 list_vaults, ~198 list_backlinks, ~341 list_models, ~448 vault_stats, ~459 recent_notes)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group B"
  </read_first>
  <action>Edit src/tool-registry.ts. For each of the 5 entries (list_vaults, list_backlinks, list_models, vault_stats, recent_notes), append the deprecation suffix to the existing description string. The suffix template MUST match for all 5: ` DEPRECATED since v2.0.0 — prefer MCP Resource \`<uri>\` for agent discovery. The tool remains callable through v2.x; removal scheduled for v3.0.0.` where <uri> is the canonical URI for each tool from the closed set. PRESERVE name and inputSchema byte-identical for each entry — only the description string changes. Do NOT remove any entries from TOOLS — count stays 37.</action>
  <verify>
    <automated>grep -c "DEPRECATED since v2.0.0" src/tool-registry.ts | awk '{if ($1 == 5) print "OK"; else { print "FAIL count " $1; exit 1 } }' &amp;&amp; grep -c "name: " src/tool-registry.ts | awk '{if ($1 == 37) print "OK count"; else { print "FAIL tool count " $1; exit 1 } }' &amp;&amp; npx tsc --noEmit 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 5 occurrences of "DEPRECATED since v2.0.0" in src/tool-registry.ts
    - TOOLS array length stays 37 (no entries removed)
    - inputSchema for each of the 5 entries is unchanged (verify via git diff — only description fields changed)
    - npx tsc --noEmit passes
  </acceptance_criteria>
  <done>5 tool descriptions annotated; name + inputSchema preserved byte-identical.</done>
</task>

<task type="auto">
  <name>Task 4: Create dump-resources.mjs and regenerate both snapshot files</name>
  <files>evals/v1-baseline/dump-resources.mjs, evals/v1-baseline/resources-list.snapshot.json, evals/v1-baseline/tools-list.snapshot.json, package.json</files>
  <read_first>
    - evals/v1-baseline/dump-tools.mjs (full file — 22 lines analog)
    - package.json (current scripts block — locate "eval:snapshot" script)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group C.1, C.2"
  </read_first>
  <action>Create evals/v1-baseline/dump-resources.mjs mirroring dump-tools.mjs verbatim except: import RESOURCES from "../../src/resource-registry.ts"; emit JSON.stringify({ resources: RESOURCES }, null, 2) to stdout. Update package.json scripts block: ensure "eval:snapshot" runs both `node evals/v1-baseline/dump-tools.mjs > evals/v1-baseline/tools-list.snapshot.json` AND `node evals/v1-baseline/dump-resources.mjs > evals/v1-baseline/resources-list.snapshot.json` (chain with && OR add a new script "eval:snapshot:resources"; planner picks based on the existing eval:snapshot shape). Run `npm run eval:snapshot` to regenerate both files. The tools-list.snapshot.json will pick up the 5 description changes from Task 3 (intentional drift). The resources-list.snapshot.json will be newly created with 10 entries.</action>
  <verify>
    <automated>test -f evals/v1-baseline/dump-resources.mjs &amp;&amp; test -f evals/v1-baseline/resources-list.snapshot.json &amp;&amp; node -e "const d=JSON.parse(require('fs').readFileSync('evals/v1-baseline/resources-list.snapshot.json','utf8')); if(d.resources.length!==10){console.error('count',d.resources.length);process.exit(1)} console.log('10 resources OK')" &amp;&amp; node -e "const d=JSON.parse(require('fs').readFileSync('evals/v1-baseline/tools-list.snapshot.json','utf8')); const dep=d.tools.filter(t=&gt;t.description.includes('DEPRECATED')); if(dep.length!==5){console.error('deprecated count',dep.length);process.exit(1)} console.log('5 deprecated tools in snapshot OK')"</automated>
  </verify>
  <acceptance_criteria>
    - evals/v1-baseline/dump-resources.mjs exists
    - resources-list.snapshot.json has exactly 10 entries
    - tools-list.snapshot.json shows 5 entries with "DEPRECATED" in description (intentional)
    - tools-list.snapshot.json total entry count still 37
    - npm run eval:snapshot runs without error
  </acceptance_criteria>
  <done>Both snapshot files regenerated and committed.</done>
</task>

<task type="auto">
  <name>Task 5: Extend baseline.test.ts with Resources snapshot assertion</name>
  <files>evals/v1-baseline/baseline.test.ts</files>
  <read_first>
    - evals/v1-baseline/baseline.test.ts lines 69-79 (existing tools-list snapshot describe block analog)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group C.3"
  </read_first>
  <action>Edit evals/v1-baseline/baseline.test.ts. Import RESOURCES from "../../src/resource-registry.ts". Add a new sibling describe block after the existing "v1 tools/list surface (FND-10)" block: describe("v2 resources/list surface (REL-08)") with two it() blocks — (1) "matches the pinned snapshot exactly" — reads evals/v1-baseline/resources-list.snapshot.json and asserts deep equality against { resources: RESOURCES }; (2) "has exactly 10 resources after REL-08 promotion" — asserts RESOURCES.length === 10. PRESERVE the existing tools/list block unchanged (TOOLS literal count stays 37 per Task 3). Run npm test on just baseline.test.ts to confirm green.</action>
  <verify>
    <automated>grep -q "v2 resources/list surface" evals/v1-baseline/baseline.test.ts &amp;&amp; grep -q "RESOURCES" evals/v1-baseline/baseline.test.ts &amp;&amp; npx vitest run evals/v1-baseline/baseline.test.ts 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - baseline.test.ts has a describe block titled "v2 resources/list surface (REL-08)"
    - Two new it() blocks present (snapshot equality + length === 10)
    - npx vitest run evals/v1-baseline/baseline.test.ts exits 0 (all assertions green)
  </acceptance_criteria>
  <done>baseline.test.ts gates the Resources surface via snapshot equality.</done>
</task>

<task type="auto">
  <name>Task 6: Extend scripts/smoketest-non-claude.mjs with Resources assertion block</name>
  <files>scripts/smoketest-non-claude.mjs</files>
  <read_first>
    - scripts/smoketest-non-claude.mjs lines 190-256 (existing tool-count assertion + the Phase 2 Resources assertion at :258+ if present)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group D"
  </read_first>
  <action>Edit scripts/smoketest-non-claude.mjs. Locate the existing tool-list assertion block. PRESERVE EXPECTED_TOOLS at 37 entries unchanged (per D-03 additive-only). After the existing tool assertion, add (or extend if the Phase 2 Resources block already exists) an EXPECTED_RESOURCE_URIS array constant with these 10 strings: "vault-memory://memory/sinks", "vault-memory://memory/stats", "vault-memory://briefs", "vault-memory://contracts", "vault-memory://contract-verbs", "vault-memory://vaults", "vault-memory://models", "vault-memory://recent", "vault-memory://stats", "vault-memory://backlinks". Add a new assertion block that calls client.listResources() and asserts every EXPECTED_RESOURCE_URI is present in the result, no unexpected URIs, and resources.length === 10. Use the existing fail()/pass() helpers. Build via npm run build and run node scripts/smoketest-non-claude.mjs to confirm green.</action>
  <verify>
    <automated>grep -q "EXPECTED_RESOURCE_URIS" scripts/smoketest-non-claude.mjs &amp;&amp; grep -q "vault-memory://vaults" scripts/smoketest-non-claude.mjs &amp;&amp; npm run build 2>&amp;1 | tail -5 &amp;&amp; node scripts/smoketest-non-claude.mjs 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EXPECTED_RESOURCE_URIS array with 10 entries present in smoketest
    - npm run build succeeds (dist/cli.js produced)
    - node scripts/smoketest-non-claude.mjs exits 0 with all assertions green
    - EXPECTED_TOOLS still at 37 entries (unchanged)
  </acceptance_criteria>
  <done>Smoketest gates the new Resources surface end-to-end against dist/cli.js.</done>
</task>

<task type="auto">
  <name>Task 7: Full test sweep to confirm no regressions</name>
  <files>none (read-only verification)</files>
  <read_first>
    - The full set of files modified in Tasks 1-6
  </read_first>
  <action>Run the full local CI sweep in sequence: npm run lint:check, npm test, npm run eval:baseline, npm run build, node scripts/smoketest-non-claude.mjs. All five steps must exit 0. If any step regresses, fix the regression before declaring done. The tools-list snapshot change (5 description-only edits) is the only intentional diff; everything else should be unchanged. Specifically watch for: (a) any source-neutrality conformance test that registers resources — may need EXPECTED count updates; (b) the Phase 6 sign-off tests asserting the existing 5 Resource count — may need updating to 10 if they assert a count directly.</action>
  <verify>
    <automated>npm run lint:check 2>&amp;1 | tail -3 &amp;&amp; npm test 2>&amp;1 | tail -10 &amp;&amp; npm run eval:baseline 2>&amp;1 | tail -5 &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; node scripts/smoketest-non-claude.mjs 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 5 CI steps exit 0
    - vitest reports: previous test count + ≥2 new tests (from the new describe block in baseline.test.ts) — should match Phase 7 state (1657 server-side) plus the 2 new ones
    - No new flakes
  </acceptance_criteria>
  <done>Full CI sweep green; REL-08 promotion landed without regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client→server (tools/list) | Adding deprecation notes is observable to all MCP clients; voice consistency matters |
| MCP client→server (resources/list) | New URIs become discovery anchors; downstream agents may bookmark them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-T | Tampering | Logic duplication between tool handler and Resource read handler | mitigate | Resource read handlers delegate to existing handle*() functions — no inline logic; verified in Task 2 |
| T-08-05-I | Information disclosure | Resource error response leaks vault names | accept | Existing pattern at src/server.ts:1864-1878 already returns vault name on unknown-vault errors; matches expected MCP error semantics |
| T-08-05-R | Repudiation | Snapshot drift hides surface changes | mitigate | Snapshot equality test at baseline.test.ts catches any drift; CI gates every PR |
</threat_model>

<verification>
- grep -c "server.registerResource" src/server.ts ≥10
- grep -c "DEPRECATED since v2.0.0" src/tool-registry.ts == 5
- resources-list.snapshot.json has 10 entries
- baseline.test.ts has "v2 resources/list surface (REL-08)" describe block
- smoketest passes with EXPECTED_RESOURCE_URIS asserting all 10
- npm run lint:check && npm test && npm run eval:baseline && npm run build && node scripts/smoketest-non-claude.mjs all exit 0
</verification>

<success_criteria>
REL-08 promotion landed: 5 new Resources callable via MCP resources/read; 5 v1 tools annotated as deprecated; both snapshot files pinned; baseline test + non-Claude smoketest gate the surface. Default surface narrative: 32 canonical tools (37 callable; 5 deprecated → Resources) + 10 Resources (5 existing + 5 new).
</success_criteria>

<output>
Create .planning/phases/08-polish-eval-suite-v2-0-0-release/08-05-SUMMARY.md documenting: which 5 URIs were registered, which 5 tool descriptions edited, snapshot diff sizes, final test count.
</output>
