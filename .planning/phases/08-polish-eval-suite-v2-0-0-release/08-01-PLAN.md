---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - REL-02
must_haves:
  truths:
    - "CHANGELOG.md [Unreleased] block contains Phase 5 (compiled briefs) Added entries"
    - "CHANGELOG.md [Unreleased] block contains Phase 7 (Obsidian plugin) Added entries"
    - "Every existing Phase 2/3/4/6 entry is preserved byte-identical (no duplication, no rewrite)"
    - "Voice is terse-technical (no marketing superlatives) matching existing Phase 4/6 entries"
    - "Cross-references to docs/v2/PHASE-5-SIGN-OFF.md resolve and Phase 7 evidence under .planning/phases/07-*/ resolves"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "[Unreleased] block with complete Phase 2-7 coverage and Phase 8 REL-08 deprecation entries"
      contains: "Phase 5 briefs section AND Phase 7 plugin section"
  key_links:
    - from: "CHANGELOG.md"
      to: "docs/v2/PHASE-5-SIGN-OFF.md"
      via: "markdown link from Phase 5 backfill bullet"
      pattern: "docs/v2/PHASE-5-SIGN-OFF\\.md"
    - from: "CHANGELOG.md"
      to: ".planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md OR docs/v2/plugin/README.md"
      via: "markdown link from Phase 7 backfill bullet"
      pattern: "Phase 7|plugin"
---

<objective>
Backfill missing CHANGELOG entries for Phase 5 (compiled briefs) and Phase 7 (Obsidian plugin) into the existing `## [Unreleased]` block. Also stage the REL-08 deprecation entries (5 list-style tools) — these are forward-referenced from the Resources promotion plan (08-05) but landing them here keeps CHANGELOG curation in one place and Wave 1 independent of Wave 2.

Purpose: REL-02 — every user-visible v2 change must appear in `[Unreleased]` before the release script (08-06) renames the heading to `## [2.0.0] — YYYY-MM-DD`.

Output: Edited `CHANGELOG.md` — two new sub-sections under `[Unreleased]` (Phase 5 + Phase 7) plus a `### Deprecated (Phase 8 / REL-08)` block. Existing Phase 2/3/4/6 and Phase 0/1 entries untouched.

Source-audit-correction (RESEARCH §"CHANGELOG Audit"): CONTEXT D-08 claimed Phases 2/3/5/7 missing — RESEARCH verified only Phases **5 + 7** are missing. Phases 2/3/4/6 entries already present. Do NOT double-write.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@CHANGELOG.md
@docs/v2/PHASE-5-SIGN-OFF.md
@.planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md

<interfaces>
Phase 5 backfill source (read for content):
- docs/v2/PHASE-5-SIGN-OFF.md §"What shipped" — the table mapping tools to slices is the spine for ### Added
- Tools shipped Phase 5: compile_brief, get_brief
- Resources shipped Phase 5: vault-memory://briefs (BRF-09)
- New: staleness daemon, migration 013 (brief_sources + daemon_state), ChunkId fragment (D-04 content-stable 7-char hash), OllamaClient.chat() ladder, default-brief-v1 MemoryContract

Phase 7 backfill source (read for content):
- .planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md (Phase 7 has no docs/v2/PHASE-7-SIGN-OFF.md — VERIFICATION.md is equivalent)
- .planning/phases/07-visual-contract-editor-canvas/07-12-SUMMARY.md
- Tools shipped Phase 7 (gated default-OFF by [plugin] enabled): set_runtime_config, resolve_secret, set_mcp_client, get_runtime_stats, trigger_reindex, suppress_contract_write
- New: plugin/ Obsidian community-plugin package, Variant C three-pane editor, .contract JSON envelope (vmFormatVersion: 1), .contract↔.yaml round-trip codec, three reference contracts (meeting-prep, project-status, code-review-brief), safeStorage-backed secrets, hash-aware SuppressionSet, vault-memory://contracts/reloaded notifications, vm-install + vm-update skills

REL-08 deprecation entries (forward from plan 08-05):
- list_vaults → vault-memory://vaults
- list_models → vault-memory://models/{vault}
- recent_notes → vault-memory://recent/{vault}
- vault_stats → vault-memory://stats/{vault}
- list_backlinks → vault-memory://backlinks/{vault}/{docId}

Voice anchor (verbatim from existing CHANGELOG.md:17-26 Phase 6 entry):
- One bullet per major-capability group; bullet starts with **bold short name**
- Tool count delta stated explicitly ("Tool count: X → Y; additive only")
- Cross-references to docs/v2/PHASE-N-SIGN-OFF.md + ADRs
- No marketing superlatives. No emojis. No "blazingly fast". No "magnificent"
- Group entries by Keep-a-Changelog category: ### Added / ### Changed / ### Deprecated / ### Dependencies / ### Migration / ### Documentation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit existing CHANGELOG state and source documents</name>
  <files>CHANGELOG.md (read), docs/v2/PHASE-5-SIGN-OFF.md (read), .planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md (read), .planning/phases/07-visual-contract-editor-canvas/07-12-SUMMARY.md (read)</files>
  <read_first>
    - CHANGELOG.md (full file — verify which Phase headings are present)
    - docs/v2/PHASE-5-SIGN-OFF.md
    - .planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group G — CHANGELOG.md backfill"
  </read_first>
  <action>Open CHANGELOG.md and confirm via grep that headings for Phase 2 (memory), Phase 3 (assembly), Phase 4 (graph), Phase 6 (contracts) are all already present in the `## [Unreleased]` block. Confirm Phase 5 (briefs) and Phase 7 (plugin) headings are absent. Read PHASE-5-SIGN-OFF.md §"What shipped" table and extract user-visible items: tools (compile_brief, get_brief), Resource (vault-memory://briefs / BRF-09), migration 013, staleness daemon (with lock file at ~/.vault-memory/locks/<vault>.lock), ChunkId 7-char content-stable hash (D-04), OllamaClient.chat() LLM ladder (ADR-005), default-brief-v1 MemoryContract, eval scenarios (briefs-curated.yaml + briefs-staleness-stub.yaml), test count delta. For Phase 7, read VERIFICATION.md and 07-12-SUMMARY.md and extract: plugin/ community-plugin package, Variant C editor (palette + Svelte Flow canvas + Zod-derived inspector), .contract JSON envelope (vmFormatVersion: 1), three reference .contract files (meeting-prep, project-status, code-review-brief), six gated plugin tools (set_runtime_config, resolve_secret, set_mcp_client, get_runtime_stats, trigger_reindex, suppress_contract_write — DEFAULT OFF), safeStorage-backed secrets (PLG-02), reindex + stats + connectors chrome (PLG-03/04/05), hash-aware SuppressionSet.consume(path, hash) extension (CAN-08), vm-install + vm-update skills (CAN-09 distribution half), six plugin docs under docs/v2/plugin/. Produce a working list of bullet items per phase grouped by Keep-a-Changelog category (Added / Changed / Dependencies / Migration / Documentation). DO NOT write to CHANGELOG.md yet.</action>
  <verify>
    <automated>grep -c "^### " CHANGELOG.md | awk '{ if ($1 &gt;= 5) print "OK"; else { print "FAIL: too few subheadings"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - Working list captures ≥6 Phase 5 items grouped under Keep-a-Changelog categories (Added / Resources / Migration / Dependencies / Documentation)
    - Working list captures ≥8 Phase 7 items, with the 6 plugin tools explicitly flagged as "gated by `[plugin] enabled = true`, default OFF"
    - No working-list item appears in current CHANGELOG.md `[Unreleased]` (i.e., no double-write planned)
    - Source-audit output noted: which Phase N entries are present in CHANGELOG today
  </acceptance_criteria>
  <done>Working list of Phase 5 + Phase 7 backfill bullets produced; no file written yet.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 5 + Phase 7 backfill entries and REL-08 deprecation entries into [Unreleased]</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (re-read; especially lines 13–128 for the existing [Unreleased] structure and lines 17–26 for Phase 6 voice anchor)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group G" (verbatim Phase 6 entry analog and REL-08 deprecation entry template)
  </read_first>
  <action>Edit CHANGELOG.md `## [Unreleased]` block by inserting two NEW sub-sections matching the existing Phase 4 / Phase 6 voice: one for Phase 5 (compile_brief + get_brief tools, vault-memory://briefs Resource, staleness daemon, migration 013, ChunkId fragment, OllamaClient.chat() ladder per ADR-005, default-brief-v1 MemoryContract; explicit "Tool count: 32 → 34" line; cross-link `docs/v2/PHASE-5-SIGN-OFF.md` + `docs/v2/adr/005-brief-compile-strategy.md`) and one for Phase 7 (plugin/ Obsidian community-plugin package, Variant C three-pane editor, .contract JSON envelope vmFormatVersion: 1, three reference .contract files, six plugin tools gated by `[plugin] enabled = true` default OFF, safeStorage-backed secrets, reindex/stats/connectors chrome, hash-aware SuppressionSet.consume() extension, vm-install + vm-update skills with SHA-256 verification, six plugin docs; cross-link `docs/v2/plugin/README.md` + `docs/v2/adr/007-contract-editor.md`; explicit "default-OFF tool count: unchanged from Phase 6 (37)" line). Then APPEND a `### Deprecated (Phase 8 / REL-08)` block with 5 bullets — one per promoted tool naming the canonical Resource URI (`vault-memory://vaults`, `vault-memory://models/{vault}`, `vault-memory://recent/{vault}`, `vault-memory://stats/{vault}`, `vault-memory://backlinks/{vault}/{docId}`) and stating "The tool remains callable through v2.x; removal scheduled for v3.0.0." DO NOT touch existing Phase 0/1/2/3/4/6 entries. DO NOT rename `## [Unreleased]` to `## [2.0.0]` — the release script (08-06) handles that.</action>
  <verify>
    <automated>grep -q "Phase 5" CHANGELOG.md &amp;&amp; grep -q "compile_brief" CHANGELOG.md &amp;&amp; grep -q "Phase 7" CHANGELOG.md &amp;&amp; grep -q "vault-memory://vaults" CHANGELOG.md &amp;&amp; grep -q "vault-memory://backlinks" CHANGELOG.md &amp;&amp; grep -q "^## \[Unreleased\]" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "compile_brief" CHANGELOG.md` succeeds (Phase 5 backfilled)
    - `grep -q "Variant C" CHANGELOG.md` succeeds (Phase 7 backfilled)
    - `grep -q "default OFF\|default-OFF" CHANGELOG.md` succeeds for the plugin tool entry (verifies the gating note is present)
    - `grep -c "vault-memory://" CHANGELOG.md` returns ≥5 (5 new Resource URIs in the deprecation block, plus the existing Phase 2/5/6 Resource references)
    - The `## [Unreleased]` heading is intact (no rename yet)
    - `grep -i "blazingly\|magnificent\|amazing\|revolutionary\|world.class" CHANGELOG.md` returns 0 matches (voice discipline)
    - Existing Phase 2/3/4/6 entries unchanged: `git diff CHANGELOG.md` shows only additions, no modifications to existing lines below the insertion point
  </acceptance_criteria>
  <done>CHANGELOG.md contains Phase 5 + Phase 7 + REL-08 deprecation entries under `[Unreleased]`; existing entries preserved; voice consistent with Phase 4/6 analogs.</done>
</task>

<task type="auto">
  <name>Task 3: Validate cross-references and run lint</name>
  <files>CHANGELOG.md (read-only verification)</files>
  <read_first>
    - CHANGELOG.md (the just-edited file)
  </read_first>
  <action>For every `docs/v2/...` link added in Task 2, verify the file exists on disk (use `test -f` per link). Specifically check `docs/v2/PHASE-5-SIGN-OFF.md`, `docs/v2/adr/005-brief-compile-strategy.md`, `docs/v2/plugin/README.md`, `docs/v2/adr/007-contract-editor.md`. If any link is broken, fix it to point at the actual file (Phase 7 has no PHASE-7-SIGN-OFF.md; the equivalent is `.planning/phases/07-visual-contract-editor-canvas/VERIFICATION.md` but that is an internal planning artifact — link instead to `docs/v2/plugin/README.md` for users). Run `npm run lint:check` and confirm prettier formatting on CHANGELOG.md does not introduce unintended diff (if it does, accept the prettier output as the canonical form).</action>
  <verify>
    <automated>for link in $(grep -oE "docs/v2/[A-Za-z0-9._/-]+\.md" CHANGELOG.md | sort -u); do test -f "$link" || { echo "MISSING: $link"; exit 1; }; done &amp;&amp; npm run lint:check 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Every `docs/v2/*.md` link in CHANGELOG.md resolves to an existing file
    - `npm run lint:check` exits 0 OR exits non-zero only due to prettier formatting that has been accepted as-is
    - `git diff --stat CHANGELOG.md` shows only `CHANGELOG.md` modified (no collateral edits)
  </acceptance_criteria>
  <done>All CHANGELOG cross-references resolve; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→users | CHANGELOG is read by downstream library consumers and npm users; misleading entries are a supply-chain communication risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-I | Information disclosure | CHANGELOG.md cross-references | mitigate | Every `docs/v2/*.md` link validated by `test -f` in Task 3 (no broken links shipped) |
| T-08-01-R | Repudiation | CHANGELOG voice drift | mitigate | Lint grep for marketing superlatives ("blazingly", "magnificent", "amazing") returns 0 matches; voice anchored to existing Phase 4/6 entries |
</threat_model>

<verification>
- `grep -q "compile_brief" CHANGELOG.md` — Phase 5 backfilled
- `grep -q "Variant C" CHANGELOG.md` — Phase 7 backfilled
- `grep -c "vault-memory://" CHANGELOG.md` returns ≥5
- All `docs/v2/*.md` links in CHANGELOG resolve (`test -f` per link)
- `npm run lint:check` exits 0
- `git diff` shows only CHANGELOG.md modified
</verification>

<success_criteria>
CHANGELOG.md `[Unreleased]` block now contains Phase 5 + Phase 7 + REL-08 deprecation entries. Existing Phase 0/1/2/3/4/6 entries unchanged. Voice matches Phase 4/6 anchors. Ready for the release script (plan 08-06) to rename `[Unreleased]` → `[2.0.0]`.
</success_criteria>

<output>
Create `.planning/phases/08-polish-eval-suite-v2-0-0-release/08-01-SUMMARY.md` when done documenting: which bullets were added, which links resolved, the exact line range of the inserted content.
</output>
