---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: false
requirements:
  - REL-03
  - REL-04
  - REL-09
must_haves:
  truths:
    - "A reader new to vault-memory can install + run + call an MCP tool within 30 seconds of reading section 1"
    - "Section 2 communicates the v2 pitch in ≤200 words: agentic knowledge layer over Obsidian; more sources coming"
    - "Section 3 ASCII architecture diagram renders correctly in GitHub web view AND a 100-column terminal"
    - "Section 5 names Phase 9 (hard gate) and v3.0.0 (Notion connector) explicitly so users know multi-source is coming"
    - "Maintainer signs off on the rewritten README cold-read (REL-09 gate at checkpoint)"
  artifacts:
    - path: "README.md"
      provides: "Six-section v2 README (30-sec example → what this is → architecture → what's new → roadmap → install & docs)"
      contains: "Phase 9"
  key_links:
    - from: "README.md"
      to: "docs/v2/MIGRATION-V1-TO-V2.md"
      via: "section 6 install-and-docs link"
      pattern: "MIGRATION-V1-TO-V2"
    - from: "README.md"
      to: "docs/v2/plugin/INSTALL.md"
      via: "section 1 install link"
      pattern: "docs/v2/plugin/INSTALL"
    - from: "README.md"
      to: "docs/v2/ARCHITECTURE.md"
      via: "section 3 architecture-diagram source"
      pattern: "docs/v2/ARCHITECTURE"
---

<objective>
Rewrite `README.md` around the v2 pitch using the D-11 six-section structure: (1) 30-second example, (2) What this is, (3) Architecture (ASCII diagram), (4) What's new in v2, (5) Roadmap, (6) Install & docs. Preserve the existing v2 framing voice from `README.md:1-21` ("any MCP-aware agent", "Obsidian is the v2 source connector"). Lock down the v2.0.0 reader experience.

Purpose: REL-03 (rewrite around new pitch), REL-04 (Roadmap names Phase 9/v3 explicitly), REL-09 (maintainer signs off).

Output: Replaced `README.md` with six sections, ASCII architecture diagram, working links to docs/v2/ artifacts, and a maintainer cold-read checkpoint at the end.

Constraints: Tight, technical, zero marketing superlatives. ASCII only (no SVG per D-11 §3). No emojis except where already in CHANGELOG.md (none expected). Preserve the SemVer-stability link in §6 per RESEARCH Pitfall 7.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@README.md
@docs/v2/ARCHITECTURE.md
@docs/v2/AGENT_AGNOSTIC.md

<interfaces>
Six-section target (from D-11 + RESEARCH §"README Rewrite Scope"):

§1 30-second example:
  - `npm install -g @owrede/vault-memory`
  - `vault-memory add-vault ~/Notes --name notes`
  - `vault-memory serve`
  - Claude Desktop config snippet pointing the MCP server at `vault-memory serve`
  - One-paragraph "what you can do" — e.g. "Ask the agent to summarize last week's meetings — it instantiates the `meeting-prep` task contract"

§2 What this is (≤200 words, preserve voice from README.md:1-21):
  - "Local-first, source-agnostic-ready agentic knowledge layer over your Obsidian notes"
  - Mention: indexed with local embeddings (Ollama), live-watcher, exposed to any MCP-aware agent (Claude Desktop, Claude Code, ChatGPT Custom Connectors, MCP Inspector)
  - "Obsidian is the v2 source connector; same MCP surface backs future adapters via SourceConnector / DeliveryAdapter / ChangeFeed seams introduced in Phase 1"
  - "Nothing leaves your machine. No cloud sync, no API keys, no telemetry"

§3 Architecture (ASCII diagram from docs/v2/ARCHITECTURE.md L0–L5):
  - Render the layer model as a single ASCII block
  - L0 retrieval (search/hybrid/embeddings) → L1 graph (typed edges, expand, cluster) → L2 memory (sinks, provenance) → L3 assembly (bundles, dossiers) → L4 briefs (compiled documents) → L5 contracts (declarative DSL)
  - Adapter seams (SourceConnector / DeliveryAdapter / ChangeFeed) flank the layer stack
  - Width ≤96 columns to render in 100-col terminal

§4 What's new in v2 (bullets from CHANGELOG.md [Unreleased] after plan 08-01 lands):
  - Phase 2: memory namespace + provenance + 3 tools
  - Phase 3: assembly tools (4) + citation packets + authority/staleness rescore
  - Phase 4: graph-as-retrieval (expand, cluster) + typed edges
  - Phase 5: compiled brief layer + staleness daemon
  - Phase 6: task contract DSL + 3 reference contracts
  - Phase 7: Obsidian plugin (Variant C editor, chrome, 6 gated tools — default OFF)
  - Tool surface: 23 v1 → 32 default v2.0.0 (with 5 list-style tools deprecated → MCP Resources); plugin OFF baseline
  - Resources: 5 → 10 (vaults, models, recent, stats, backlinks added)

§5 Roadmap (explicit Phase 9 + v3 per REL-04, source `.planning/ROADMAP.md` §"Phase 9" + §"Phase 10"):
  - "Phase 9 — Pre-Phase-10 premise check (hard gate)" — verifies seams, ADRs, capability descriptors before v3 work
  - "v3.0.0 — Notion connector & multi-source proof" — adds Notion as a second source via the adapter seams; deferred from v2 scope
  - Brief mention: v3.x postgres-fs storage adapter (idea, not committed); v4.0.0 multi-user (ideas, not committed)

§6 Install & docs:
  - Plugin install: link to `docs/v2/plugin/INSTALL.md`
  - Plugin README: `docs/v2/plugin/README.md`
  - MIGRATION v1→v2: `docs/v2/MIGRATION-V1-TO-V2.md`
  - Architecture deep-dive: `docs/v2/ARCHITECTURE.md`
  - Memory contract: `docs/v2/MEMORY_CONTRACT.md`
  - Agent-agnostic statement: `docs/v2/AGENT_AGNOSTIC.md`
  - ADR index: `docs/v2/adr/README.md`
  - CHANGELOG: `./CHANGELOG.md`
  - SemVer stability statement (Pitfall 7 mitigation): "SemVer-locked tool API per the v1.0.0 declaration. See `CHANGELOG.md` for full history."

Voice anchor (verbatim from README.md:1-21 — preserve this voice in §2):
> Local-first, source-agnostic-ready knowledge layer for Obsidian vaults, exposed to any MCP-aware agent.
> vault-memory turns one or more Obsidian vaults into a queryable, agent-native knowledge base — running entirely on your machine.

Banned voice (per CLAUDE.md / RULES.md `Professional Honesty`):
- "blazingly fast", "magnificent", "amazing", "revolutionary", "world-class", "production-ready" (without evidence), "battle-tested" (without metric)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the new six-section README (replace existing content)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — preserve §1-21 voice; replace §22+)
    - docs/v2/ARCHITECTURE.md (source for §3 ASCII diagram — extract the L0–L5 layer model)
    - docs/v2/AGENT_AGNOSTIC.md (source for §2 pitch — "MCP is canonical; Skills are one delivery mechanism")
    - CHANGELOG.md (just edited by plan 08-01 — source for §4 bullets)
    - .planning/ROADMAP.md §"Phase 9" + §"Phase 10 (v3.0.0)" + §"v3.0.0 — Deferred" (source for §5 Roadmap)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group H — README.md rewrite"
  </read_first>
  <action>Rewrite README.md as a six-section document. Section 1 ("30-second example") shows fenced bash for `npm install -g @owrede/vault-memory`, then `vault-memory add-vault ~/Notes --name notes`, then `vault-memory serve`; a Claude Desktop config JSON snippet with the `mcpServers` entry pointing at the installed `vault-memory` binary; a one-paragraph "what you can do" naming the `meeting-prep` task contract. Section 2 ("What this is") preserves the voice from current README.md:1-21 verbatim and tightens to ≤200 words around the agentic-knowledge-layer pitch + the adapter-seams promise + "nothing leaves your machine". Section 3 ("Architecture") renders an ASCII block diagram of the L0–L5 layer model from docs/v2/ARCHITECTURE.md — 96-column max width, box-drawing characters from a portable subset (`+-|` only; NO `─│└┐` UNICODE since they trip GitHub's monospace rendering in some themes per RESEARCH §"State of the Art" — actually USE Unicode box-drawing since current CHANGELOG and existing PHASE-N-SIGN-OFF.md files use them successfully; verify by previewing on github.com). Section 4 ("What's new in v2") is a bulleted summary of CHANGELOG `[Unreleased]` phase entries with the explicit tool-count delta (23 → 32 default; +6 plugin gated OFF) and Resources delta (5 → 10). Section 5 ("Roadmap") names Phase 9 (hard gate) and v3.0.0 (Notion connector) per the ROADMAP.md framing — one paragraph each. Section 6 ("Install & docs") provides anchored markdown links to the 8 docs/v2 artifacts named above, ending with the SemVer-stability statement linking back to CHANGELOG.md. Preserve any LICENSE / contributing footer that exists. DO NOT use marketing superlatives (lint-grep gate runs in Task 2).</action>
  <verify>
    <automated>grep -q "Phase 9" README.md &amp;&amp; grep -q "v3" README.md &amp;&amp; grep -q "MIGRATION-V1-TO-V2" README.md &amp;&amp; grep -qi "meeting-prep" README.md &amp;&amp; grep -q "MCP-aware agent" README.md &amp;&amp; awk '/^## /{n++} END{exit !(n &gt;= 6)}' README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains exactly ≥6 second-level `## ` headings (the six sections)
    - `grep -q "Phase 9" README.md && grep -q "v3" README.md` succeeds (REL-04)
    - `grep -q "MIGRATION-V1-TO-V2" README.md` succeeds (§6 link present)
    - `grep -q "meeting-prep" README.md` succeeds (§1 30-second example names the contract)
    - `grep -q "MCP-aware agent" README.md` succeeds (§2 voice preserved)
    - `grep -q "docs/v2/ARCHITECTURE\|docs/v2/plugin\|docs/v2/adr" README.md` returns ≥3 matches (§6 links)
    - ASCII architecture diagram is ≤96 columns wide on the widest line: `awk '{ if (length > 96 && /[+|+-]|│|─/) { print "TOO_WIDE:" NR; exit 1 } }' README.md` exits 0
  </acceptance_criteria>
  <done>README.md replaced with six-section v2 structure; all required keywords present; ASCII diagram fits 96 columns.</done>
</task>

<task type="auto">
  <name>Task 2: Validate links and voice discipline; verify diagram renders</name>
  <files>README.md (read-only verification)</files>
  <read_first>
    - README.md (just-rewritten file)
  </read_first>
  <action>For every markdown link in README.md pointing to `docs/v2/...` or a relative path, verify the target file exists via `test -f`. Run a voice-discipline grep against the banned-superlatives list ("blazingly", "magnificent", "amazing", "revolutionary", "world.class", "production.ready", "battle.tested") — every match must be 0 OR (if present in a code block / quoted context) annotated as intentional. Print the ASCII architecture diagram block to stdout via `awk '/^```$/,/^```$/' README.md | head -60` and visually confirm it renders. Run `npm run lint:check`.</action>
  <verify>
    <automated>for link in $(grep -oE "(docs/v2/[A-Za-z0-9._/-]+\.(md|png)|CHANGELOG\.md|\./CHANGELOG\.md)" README.md | sort -u); do test -f "${link#./}" || { echo "MISSING: $link"; exit 1; }; done &amp;&amp; ! grep -iE "blazingly|magnificent|revolutionary|world.class" README.md &amp;&amp; npm run lint:check 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Every relative link in README.md resolves to an existing file
    - `grep -iE "blazingly|magnificent|revolutionary|world.class" README.md` returns 0 matches (voice clean)
    - `npm run lint:check` exits 0 OR prettier-only formatting drift accepted
    - Manual: maintainer reads through the architecture diagram in a 100-column terminal and confirms the box drawing is intact
  </acceptance_criteria>
  <done>Links resolve; voice lint clean; diagram readable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer cold-read sign-off (REL-09)</name>
  <what-built>Six-section v2 README.md replacing the v1.0.0 README. Section 1: 30-second install example with claude-desktop snippet. Section 2: pitch preserving the existing "any MCP-aware agent" voice. Section 3: ASCII L0–L5 architecture diagram. Section 4: what's new in v2 bullets sourced from CHANGELOG. Section 5: Roadmap naming Phase 9 (hard gate) + v3.0.0 (Notion connector). Section 6: install & docs links + SemVer-stability statement.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    1. Read README.md from top to bottom WITHOUT prior context (close any open Phase 8 planning artifacts first).
    2. Verify Section 1 example would let a brand-new user install + serve + call one tool in under 60 seconds.
    3. Verify Section 2 pitch reads as ≤200 words, terse-technical, no marketing language.
    4. Render README.md in GitHub web view (paste on a temp branch or use `gh repo view -w`). Confirm the ASCII architecture diagram renders correctly in the web view (no broken box-drawing, no wrapped lines).
    5. Confirm Section 5 explicitly names "Phase 9" and "v3.0.0" or "v3" with one-paragraph framing for each.
    6. Confirm Section 6 has working links to docs/v2/plugin/INSTALL.md, docs/v2/MIGRATION-V1-TO-V2.md (Wave 1 plan 08-03 produces this — link may be unresolved at this checkpoint; flag if so), docs/v2/ARCHITECTURE.md, docs/v2/adr/README.md.
    7. Flag any section that reads poorly or needs another revision.
  </how-to-verify>
  <acceptance_criteria>
    - Maintainer types "approved" OR provides specific revision asks
    - If MIGRATION-V1-TO-V2.md does not yet exist (plan 08-03 hasn't shipped), the link in §6 is allowed to be a forward reference — maintainer confirms or flags
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe required revisions</resume-signal>
  <done>Maintainer approves README cold-read; REL-09 satisfied for the README portion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→npmjs.com | README.md is published as the package's display text on npmjs.com; voice errors visible to every downstream user |
| repo→GitHub web | README.md renders on the repo's landing page; the ASCII diagram's portability matters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-R | Repudiation | README voice | mitigate | Voice grep gates banned-superlative list (Task 2); maintainer cold-read checkpoint (Task 3) |
| T-08-02-I | Information disclosure | Broken README links | mitigate | Every relative link validated via `test -f` in Task 2 |
</threat_model>

<verification>
- `grep -q "Phase 9" README.md && grep -q "v3" README.md` — REL-04 satisfied
- `grep -q "MCP-aware agent" README.md` — REL-03 pitch preserved
- `grep -iE "blazingly|magnificent|revolutionary" README.md` returns 0
- All relative links in README.md resolve
- Maintainer checkpoint approved (REL-09 partial — full REL-09 requires sign-off doc in plan 08-08)
</verification>

<success_criteria>
README.md rewritten as a six-section v2-first document. Phase 9 + v3 named explicitly. Maintainer signs off on cold-read.
</success_criteria>

<output>
Create `.planning/phases/08-polish-eval-suite-v2-0-0-release/08-02-SUMMARY.md` documenting the section layout, the ASCII diagram dimensions, link validation result, and maintainer sign-off line.
</output>
