---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 08
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
  - 08-04
  - 08-05
  - 08-06
  - 08-07
files_modified:
  - docs/v2/PHASE-8-SIGN-OFF.md
  - CHANGELOG.md
  - package.json
  - package-lock.json
autonomous: false
requirements:
  - REL-02
  - REL-06
  - REL-07
  - REL-09
must_haves:
  truths:
    - "docs/v2/PHASE-8-SIGN-OFF.md exists with maintainer signature line"
    - "v2.0.0 git tag exists in the origin remote"
    - "GitHub Release for v2.0.0 exists with three required assets attached: vault-memory-plugin-v2.0.0.tar.gz, manifest.sha256, vault-memory-plugin-walkthrough.mp4"
    - "npm view @owrede/vault-memory@2.0.0 version returns 2.0.0"
    - "CHANGELOG.md `[Unreleased]` block renamed to `[2.0.0] — YYYY-MM-DD`"
    - "Live dry-run of vm-install skill succeeds against the published GitHub Release (Plan 07-11 deferred human-verify resolved)"
  artifacts:
    - path: "docs/v2/PHASE-8-SIGN-OFF.md"
      provides: "Phase 8 sign-off artifact mirroring PHASE-6-SIGN-OFF.md template (D-18)"
      contains: "REL-01"
    - path: "CHANGELOG.md"
      provides: "Renamed Unreleased block; current state is [2.0.0] — YYYY-MM-DD plus a fresh empty [Unreleased] above"
      contains: "## [2.0.0]"
  key_links:
    - from: "docs/v2/PHASE-8-SIGN-OFF.md"
      to: "all REL-01..REL-09 evidence"
      via: "Requirements coverage table with anchor commit SHAs"
      pattern: "REL-0[1-9]"
    - from: "v2.0.0 GitHub Release"
      to: "vault-memory-plugin-walkthrough.mp4"
      via: "manually-attached Release asset"
      pattern: "vault-memory-plugin-walkthrough.mp4"
---

<objective>
Cut v2.0.0. Write the PHASE-8-SIGN-OFF.md artifact, run scripts/release.mjs to bump version + rename CHANGELOG + tag + push atomically, monitor publish.yml to completion, manually attach the screencast MP4 to the auto-created GitHub Release, run the live vm-install dry-run that resolves Phase 7 Plan 07-11 Task 3's deferred human-verify, and record maintainer sign-off.

Purpose: REL-02 final curation; REL-06 v2.0.0 tag + GitHub Release; REL-07 npm publish; REL-09 maintainer sign-off.

Output: PHASE-8-SIGN-OFF.md committed; v2.0.0 tag pushed; npm package live; GitHub Release with all 3 required assets attached; vm-install/vm-update skills functional with the live URL.

This is the terminal plan for Phase 8 and the v2.0.0 milestone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@docs/v2/PHASE-6-SIGN-OFF.md
@CHANGELOG.md
@scripts/release.mjs
@CONTRIBUTING.md
@.github/workflows/publish.yml

<interfaces>
PHASE-8-SIGN-OFF.md structure (anchored to PATTERNS §"Group J" + D-18; template family from docs/v2/PHASE-6-SIGN-OFF.md):

# Phase 8 Sign-Off — Polish, eval suite, v2.0.0 release

**Phase:** 8 — Polish, eval suite, v2.0.0 release
**Sign-off date:** YYYY-MM-DD
**Branch:** main (no per-phase branch for Phase 8)
**Maintainer:** _to be recorded at PR approval time_
**Final tool count:** 32 canonical (37 callable, 5 deprecated → Resources) + 10 MCP Resources

## Phase Summary
One paragraph framing the phase goal (cut v2.0.0; eval suite as merge gate; CHANGELOG curation; README + MIGRATION; REL-08 promotion) and what shipped.

## Requirements Coverage
| ID | Description | Status | Plan | Anchor commit |
|----|-------------|--------|------|---------------|
| REL-01 | Eval suite required-for-merge with no override | Complete | 08-06 + 08-07 | sha |
| REL-02 | CHANGELOG curated covering every user-visible v2 change | Complete | 08-01 + this plan (rename) | sha |
| REL-03 | README rewritten around new pitch | Complete | 08-02 | sha |
| REL-04 | README Roadmap names Phase 9/v3 | Complete | 08-02 | sha |
| REL-05 | MIGRATION-V1-TO-V2.md complete | Complete | 08-03 | sha |
| REL-06 | v2.0.0 git tag + GitHub Release | Complete | this plan | sha |
| REL-07 | npm publish completed with provenance | Complete | this plan (publish.yml) | sha |
| REL-08 | Tool surface inventory: 32 canonical (Resources promotion) | Complete | 08-05 | sha |
| REL-09 | Maintainer signed off | Complete | this plan | sha |

## ROADMAP Success Criteria Coverage
For each of the 5 criteria in ROADMAP.md §"Phase 8" → state "MET" + evidence paragraph.

## Phase 7 Carryovers Resolved
- Screencast: vault-memory-plugin-walkthrough.mp4 attached to v2.0.0 Release (link)
- Plugin tarball: vault-memory-plugin-v2.0.0.tar.gz attached (link)
- manifest.sha256: attached (link)
- vm-install dry-run: PASSED — live URL: https://github.com/owrede/vault-memory/releases/download/v2.0.0/vault-memory-plugin-v2.0.0.tar.gz
- vm-update template substitution: validated on v2.0.x simulation
- docs/v2/plugin/INSTALL.md + CONTRACT-EDITOR.md deferral notes resolved (08-04)

## Tool Surface Inventory
- Pre-Phase-8 default: 37 tools, 5 Resources
- Post-Phase-8 default: 37 tools (5 deprecated) → 32 canonical, 10 Resources
- Plugin-enabled additive: +6 tools (unchanged from Phase 7)
- Net delta: 37 → 32 canonical via Resources promotion (additive); no breaking changes

## Test Floor
- Pre: 1657 server-side + 136 plugin
- Post: 1657 + 2 new Resources snapshot tests = 1659 server-side; plugin unchanged
- CI green on the release commit + tag

## Known Limitations / Out-of-Scope
[bulleted list from CONTEXT §<deferred>]
- Multi-OS CI matrix (Linux-only in v2.0.0; v2.x add)
- VTT caption file for screencast (baked-in titles only)
- Eval-suite override token (rejected; no `[skip eval]`)
- Standalone eval-fixture tarball asset (deferred to v3)
- Prebuilt CLI binary asset (npm tarball is the canonical channel)
- YouTube hosting for screencast (GitHub Release MP4 only)
- Launch promotion plan (maintainer's choice; out of scope)
- `vault-memory migrate-v1-to-v2` CLI (no data migration needed)
- CONTRIBUTING.md full overhaul (minimum sections only)
- SVG architecture diagram (ASCII for v2.0.0)

## Maintainer Sign-Off
| Field | Value |
|-------|-------|
| Signed by | Oliver Wrede |
| Date | YYYY-MM-DD |
| PR | (n/a — direct push for release commit) |
| Commit | <sha of the release commit> |

Release ordering (RESEARCH §"Open Q3" final recommendation):
1. Plan 08-07 Task 3 already validated publish.yml via RC dry-run; cleanup confirmed
2. Maintainer reviews all Wave 1 + Wave 2 artifacts cold (README, CHANGELOG, MIGRATION, CONTRIBUTING)
3. Write PHASE-8-SIGN-OFF.md (this plan Task 1) — leaving the maintainer-sign field as placeholder, REL-06/07 commit SHAs as TBD
4. Run npm run release 2.0.0 — script handles version bump + CHANGELOG rename + commit + tag + push
5. Monitor publish.yml to green
6. Manually upload vault-memory-plugin-walkthrough.mp4 to the auto-created v2.0.0 Release
7. Click "Publish" on the Release (if it landed as a draft)
8. Live dry-run of `vm-install` skill against the published Release URL
9. Update PHASE-8-SIGN-OFF.md with the resolved commit SHAs + maintainer signature line + amend or commit
10. (Optional) post-release: tweet / blog / etc — out of plan scope

NOTE: The release commit (step 4) automatically renames CHANGELOG `[Unreleased]` → `[2.0.0] — YYYY-MM-DD`. The PHASE-8-SIGN-OFF.md commit (step 9) lands AFTER the tag because it requires the release commit SHA. Per CONTEXT specifics §"Sign-off is the LAST commit before the tag" — this conflicts with reality. RESOLUTION: write the SIGN-OFF doc with placeholder SHAs BEFORE step 4, commit it as part of the release commit (release.mjs already adds CHANGELOG.md to the commit; add PHASE-8-SIGN-OFF.md to the git add list manually OR amend the release commit post-hoc — release.mjs uses no --amend per CLAUDE.md "Never amend"), then update the actual SHA in a follow-up commit if needed. PREFERRED: write the SIGN-OFF doc BEFORE step 4 with placeholder SHAs, manually add it to the staging area via `git add docs/v2/PHASE-8-SIGN-OFF.md` between the release.mjs `git add` and `git commit` calls — or modify release.mjs to also add docs/v2/PHASE-8-SIGN-OFF.md if it exists. SIMPLEST PATH: write SIGN-OFF doc as Task 1; modify release.mjs Phase 5 to `git add ... docs/v2/PHASE-8-SIGN-OFF.md` as well; the placeholder SHAs are updated to the release commit's actual SHA in a follow-up amend-via-revert-and-replay OR left as `<this commit>` placeholder text (acceptable per the sign-off doc voice).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/v2/PHASE-8-SIGN-OFF.md</name>
  <files>docs/v2/PHASE-8-SIGN-OFF.md</files>
  <read_first>
    - docs/v2/PHASE-6-SIGN-OFF.md (template family — most recent sign-off)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group J"
    - .planning/ROADMAP.md §"Phase 8" (5 success criteria — to be cited)
    - CHANGELOG.md (current `[Unreleased]` block — coverage source)
  </read_first>
  <action>Create docs/v2/PHASE-8-SIGN-OFF.md mirroring docs/v2/PHASE-6-SIGN-OFF.md structure with the Phase 8 deltas described in the interfaces block. Open with frontmatter-style header (Phase, Sign-off date placeholder, Branch=main, Maintainer placeholder, Final tool count line). Include sections: Phase Summary (one paragraph framing the release-engineering scope), Requirements Coverage (table of REL-01..REL-09 with plan and "Anchor commit" = TBD placeholders to be filled by Task 5), ROADMAP Success Criteria Coverage (5 criteria from ROADMAP.md §Phase 8 success criteria with "MET" + evidence paragraph each), Phase 7 Carryovers Resolved (screencast / plugin tarball / manifest.sha256 / vm-install dry-run / vm-update template / plugin doc URL resolution — each with placeholder release-URL link), Tool Surface Inventory (37 → 32 canonical + 10 Resources delta), Test Floor (1657 → 1659 server-side), Known Limitations (verbatim from CONTEXT §<deferred>), Maintainer Sign-Off table (Signed-by placeholder, Date, PR n/a, Commit placeholder). The release.mjs script will add this file to the release commit's staging area (per the plan in Task 4 below).</action>
  <verify>
    <automated>test -f docs/v2/PHASE-8-SIGN-OFF.md &amp;&amp; grep -q "REL-01" docs/v2/PHASE-8-SIGN-OFF.md &amp;&amp; grep -q "REL-09" docs/v2/PHASE-8-SIGN-OFF.md &amp;&amp; grep -q "Maintainer" docs/v2/PHASE-8-SIGN-OFF.md &amp;&amp; grep -q "32 canonical\|32 tools" docs/v2/PHASE-8-SIGN-OFF.md &amp;&amp; grep -q "Phase 7" docs/v2/PHASE-8-SIGN-OFF.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at docs/v2/PHASE-8-SIGN-OFF.md
    - All REL-01..REL-09 cells present in requirements table (grep returns 9 matches for REL-0[1-9])
    - 5 success-criteria sub-sections present
    - Phase 7 carryovers section present
    - Maintainer sign-off table present (signature field as placeholder)
  </acceptance_criteria>
  <done>Sign-off doc drafted with placeholders; ready to be included in the release commit.</done>
</task>

<task type="auto">
  <name>Task 2: Extend scripts/release.mjs to also stage PHASE-8-SIGN-OFF.md if present</name>
  <files>scripts/release.mjs</files>
  <read_first>
    - scripts/release.mjs (the script from plan 08-06)
  </read_first>
  <action>In scripts/release.mjs Phase 5 (commit + tag + push), edit the `git add` line to ALSO include `docs/v2/PHASE-${PHASE}-SIGN-OFF.md` when the file exists. The simplest implementation: derive PHASE from version — for v2.0.0, the sign-off doc lives at docs/v2/PHASE-8-SIGN-OFF.md. Hardcoded approach is acceptable (this is a one-off release script): change the `git add` line to also conditionally add `docs/v2/PHASE-8-SIGN-OFF.md` if it exists on disk. Use a small Node check: `import { existsSync } from "node:fs"; if (existsSync("docs/v2/PHASE-8-SIGN-OFF.md")) execSync("git add docs/v2/PHASE-8-SIGN-OFF.md")`. This keeps the release commit atomic with the sign-off doc inclusion. Test the change with node --check.</action>
  <verify>
    <automated>node --check scripts/release.mjs &amp;&amp; grep -q "PHASE-8-SIGN-OFF" scripts/release.mjs</automated>
  </verify>
  <acceptance_criteria>
    - scripts/release.mjs syntax-valid post-edit
    - PHASE-8-SIGN-OFF.md will be staged alongside CHANGELOG.md + package.json + package-lock.json in the release commit
  </acceptance_criteria>
  <done>release.mjs ready to bundle sign-off doc into the release commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer cold-read of all Wave 1 + Wave 2 artifacts</name>
  <what-built>The complete set of v2.0.0 release-facing artifacts: README.md, CHANGELOG.md `[Unreleased]` block, docs/v2/MIGRATION-V1-TO-V2.md, CONTRIBUTING.md, docs/v2/PHASE-8-SIGN-OFF.md, the screencast thumbnail in docs/v2/plugin/.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    Read each in turn with fresh eyes:
    1. README.md — does the cold-read flow tell a new user what vault-memory is and how to install in 30 seconds?
    2. CHANGELOG.md `[Unreleased]` — does it cover every user-visible v2 change? Are Phase 5 + Phase 7 + REL-08 deprecation entries present?
    3. docs/v2/MIGRATION-V1-TO-V2.md — would a downstream TS lib consumer know what to change?
    4. CONTRIBUTING.md — is the release recipe accurate? Is the merge-gate-no-override note clear?
    5. docs/v2/PHASE-8-SIGN-OFF.md — does the success-criteria coverage convince a reader the phase is complete?
    6. docs/v2/plugin/screencast-thumbnail.png — does it render at GitHub default image scaling?
    Flag any revisions needed BEFORE running the release script. Revisions can be quick fix-up commits before Task 4.
  </how-to-verify>
  <acceptance_criteria>
    - Maintainer types "approved" OR provides specific edits and re-checks after applying them
    - All 6 artifacts pass cold-read
  </acceptance_criteria>
  <resume-signal>Type "approved-for-cut" or describe required revisions</resume-signal>
  <done>Maintainer approves the v2.0.0 artifact set.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Run npm run release 2.0.0 and monitor publish.yml</name>
  <what-built>Execution of scripts/release.mjs which bumps version, renames CHANGELOG, commits PHASE-8-SIGN-OFF.md + CHANGELOG.md + package.json + package-lock.json, tags v2.0.0, pushes atomically with --follow-tags. publish.yml then runs npm publish (with provenance) and creates the GitHub Release with the plugin tarball + manifest.sha256 attached.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    1. Pre-flight one final time:
       - Working tree CLEAN (git status returns empty)
       - On main branch (git branch --show-current = main)
       - origin/main is up to date with local
       - CHANGELOG.md `[Unreleased]` is non-empty
       - docs/v2/PHASE-8-SIGN-OFF.md exists (Task 1)

    2. Run: `npm run release 2.0.0`
       - The script prompts/validates → bumps version → renames CHANGELOG → commits → tags → pushes
       - Stderr will print the confirmation message + a note about MP4 upload

    3. Monitor publish.yml:
       - `gh run watch`
       - Or open https://github.com/owrede/vault-memory/actions
       - Expected steps: setup-node → npm ci → lint:check → npm test → npm run build → verify version matches tag → npm publish --access public --provenance → build plugin tarball → generate manifest.sha256 → Create GitHub Release (softprops/action-gh-release@v2) with files:

    4. Verify npm publish:
       - `npm view @owrede/vault-memory@2.0.0 version` returns `2.0.0`
       - Verify provenance attestation link present at npmjs.com/package/@owrede/vault-memory

    5. Verify GitHub Release:
       - `gh release view v2.0.0 --json assets | jq '.assets[].name'`
       - Expected: includes `vault-memory-plugin-v2.0.0.tar.gz` AND `manifest.sha256`
       - The release body contains the CHANGELOG section for v2.0.0

    6. STOP if any step fails. Diagnose. Common failures:
       - publish.yml fails at "verify version matches tag" — means package.json wasn't pushed in the same atomic operation as the tag (Pitfall 2). Fix: re-run release.mjs after correcting state.
       - Tarball build fails — plugin/esbuild.config.mjs may have a CI-specific issue; investigate plugin/package.json.
       - Release create fails — softprops action may need a different `make_latest` setting; check error.
  </how-to-verify>
  <acceptance_criteria>
    - npm run release exits 0
    - publish.yml completes green
    - `npm view @owrede/vault-memory@2.0.0 version` returns `2.0.0`
    - `gh release view v2.0.0 --json assets | jq '.assets[].name'` includes the tarball and manifest.sha256
    - Release body contains the v2.0.0 CHANGELOG section
  </acceptance_criteria>
  <resume-signal>Type "v2.0.0-live" with the npm + gh outputs OR describe the failure</resume-signal>
  <done>v2.0.0 published; tarball + checksum attached to Release.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: Upload the screencast MP4 + run vm-install live dry-run</name>
  <what-built>The screencast MP4 attached as the third required Release asset; the vm-install skill validated against the live published Release URL (resolves Phase 7 Plan 07-11 Task 3 deferred human-verify).</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    1. Upload the MP4:
       - From the GitHub Release page (https://github.com/owrede/vault-memory/releases/tag/v2.0.0) or via `gh release upload v2.0.0 /path/to/vault-memory-plugin-walkthrough.mp4`
       - The local path is the one staged in plan 08-04 Task 1
       - Confirm upload: `gh release view v2.0.0 --json assets | jq '.assets[].name'` now includes the MP4

    2. If the Release was created as a draft (softprops default is draft: false; check), click "Publish Release" on the GitHub UI

    3. Verify the MP4 URL resolves: open https://github.com/owrede/vault-memory/releases/download/v2.0.0/vault-memory-plugin-walkthrough.mp4 in a browser — should download or stream

    4. Verify the doc cross-references work: open docs/v2/plugin/INSTALL.md on GitHub web and click the screencast thumbnail — it should navigate to the MP4

    5. Live vm-install dry-run (Phase 7 Plan 07-11 Task 3 carryover):
       - On a fresh test Obsidian vault (or a sandboxed environment), run the vm-install skill
       - The skill downloads vault-memory-plugin-v2.0.0.tar.gz from the live Release URL
       - The skill verifies the SHA-256 against manifest.sha256
       - The plugin is sideloaded into .obsidian/plugins/vault-memory/
       - Open Obsidian, enable the plugin, confirm it loads without errors
       - Open examples/contracts/meeting-prep.contract — the visual editor opens

    6. Live vm-update dry-run:
       - Simulate an upgrade by overriding the version (the template substitution accepts v__VERSION__ → resolved at runtime)
       - Confirm the script's URL-resolution logic produces the correct download URL for v2.0.0
       - Full vm-update test waits for v2.0.1 (out of scope)
  </how-to-verify>
  <acceptance_criteria>
    - `gh release view v2.0.0 --json assets | jq '.assets[].name'` returns all 3: tarball, manifest.sha256, MP4
    - MP4 URL resolves (HTTP 200 on download)
    - vm-install skill successfully installs the plugin from the live Release
    - vm-update template substitution produces a valid URL
    - Phase 7 Plan 07-11 Task 3 deferred human-verify is hereby resolved
  </acceptance_criteria>
  <resume-signal>Type "skills-live" with the asset list and vm-install outcome OR describe failures</resume-signal>
  <done>All 3 Release assets present; skills functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Update PHASE-8-SIGN-OFF.md with resolved SHAs + sign and commit</name>
  <what-built>Final PHASE-8-SIGN-OFF.md with the release commit SHA filled in, all REL-01..REL-09 anchor-commit SHAs resolved, maintainer signature line filled in. Committed as a follow-up commit on main.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    1. Get the release commit SHA:
       - `git log --oneline -1` should show "release: v2.0.0" — copy that SHA
    2. Get anchor commit SHAs for each REL-XX:
       - REL-01 (eval gate / branch protection): no commit (GitHub Settings change); reference the protection-rule snapshot from plan 08-07 Task 1
       - REL-02 (CHANGELOG): the release commit SHA from Task 4 (the rename) AND the earlier plan 08-01 commit
       - REL-03 (README): plan 08-02 commit SHA
       - REL-04 (Roadmap in README): same as REL-03
       - REL-05 (MIGRATION): plan 08-03 commit SHA
       - REL-06 (tag): the v2.0.0 tag itself (`git rev-list -n 1 v2.0.0`)
       - REL-07 (npm publish): the publish.yml workflow run URL
       - REL-08 (Resources promotion): plan 08-05 commit SHA
       - REL-09 (sign-off): this commit (to be filled in after writing)
    3. Edit docs/v2/PHASE-8-SIGN-OFF.md — replace TBD placeholders with the resolved SHAs/URLs
    4. Fill in the Maintainer Sign-Off table:
       - Signed by: Oliver Wrede
       - Date: YYYY-MM-DD (today)
       - PR: n/a (direct push for release commit)
       - Commit: the SHA of THIS sign-off commit (use a placeholder like `<this commit>` and update after commit OR use git rev-parse HEAD after the commit lands)
    5. Commit + push:
       - `git add docs/v2/PHASE-8-SIGN-OFF.md`
       - `git commit -m "docs(08): finalize sign-off for v2.0.0"`
       - Note: branch protection requires a PR. Open a PR with this single commit; CI is green; auto-merge or fast-merge.
    6. Verify the final state:
       - `git log --oneline -3` shows the sign-off commit, the release commit, and the preceding work
       - The sign-off doc has no TBD placeholders remaining (except possibly the `<this commit>` self-reference)
  </how-to-verify>
  <acceptance_criteria>
    - PHASE-8-SIGN-OFF.md has zero "TBD" placeholders (other than acceptable self-referential placeholders)
    - Maintainer signature filled in (Signed by + Date)
    - Commit landed on main via PR
    - `git log --grep "finalize sign-off" --oneline` returns the commit
  </acceptance_criteria>
  <resume-signal>Type "phase-8-signed" with the commit SHA OR describe blockers</resume-signal>
  <done>Phase 8 sign-off complete; v2.0.0 milestone closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local→origin (release tag) | Atomic --follow-tags push from release.mjs |
| publish.yml→npmjs.com | npm publish --provenance via OIDC |
| publish.yml→GitHub Releases | softprops/action-gh-release@v2 uploads tarball + manifest.sha256 |
| maintainer→GitHub Releases (manual upload) | MP4 upload via web UI or gh CLI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-T | Tampering | Version drift between tag and package.json | mitigate | release.mjs uses atomic --follow-tags push; publish.yml verifies match before publish |
| T-08-08-SC | Supply chain | npm publish without provenance | mitigate | publish.yml already configured with `--provenance` + `id-token: write` |
| T-08-08-I | Information disclosure | Sign-off references private commit metadata | accept | Repo is public; all commit SHAs are intentionally public |
| T-08-08-R | Repudiation | Sign-off commit attribution | mitigate | git commit signed by maintainer's configured identity; PR record on GitHub |
</threat_model>

<verification>
- `git tag -l v2.0.0` returns the tag
- `gh release view v2.0.0 --json assets | jq '.assets[].name'` returns 3 entries: tarball, manifest.sha256, MP4
- `npm view @owrede/vault-memory@2.0.0 version` returns `2.0.0`
- `gh release view v2.0.0 --json body | jq -r '.body'` returns the CHANGELOG section
- docs/v2/PHASE-8-SIGN-OFF.md exists with maintainer signature
- vm-install skill works against the live Release
- All 9 REL requirements have status `Complete` in PHASE-8-SIGN-OFF.md table
</verification>

<success_criteria>
v2.0.0 cut: tag pushed, npm published with provenance, GitHub Release with all 3 required assets, CHANGELOG renamed, PHASE-8-SIGN-OFF.md committed and signed. Phase 8 done; Phase 9 (HARD GATE) is the next phase to plan.
</success_criteria>

<output>
Create .planning/phases/08-polish-eval-suite-v2-0-0-release/08-08-SUMMARY.md documenting: release commit SHA, tag SHA, npm publish workflow run URL, Release URL, asset list, vm-install dry-run outcome, sign-off commit SHA.
</output>
