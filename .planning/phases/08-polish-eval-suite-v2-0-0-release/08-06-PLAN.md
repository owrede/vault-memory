---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 06
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - scripts/release.mjs
  - CONTRIBUTING.md
  - package.json
  - .github/workflows/publish.yml
autonomous: true
requirements:
  - REL-01
  - REL-06
must_haves:
  truths:
    - "`npm run release` runs scripts/release.mjs end-to-end: validates clean tree + on main + non-empty Unreleased, runs npm test, bumps version via npm version, renames CHANGELOG, commits, tags, pushes atomically"
    - "CONTRIBUTING.md documents the Cut-a-release recipe + the merge-gate-no-override policy"
    - "publish.yml extended to build the plugin tarball + manifest.sha256 and attach both as Release assets via softprops/action-gh-release v2 files: parameter"
    - "The release script NEVER force-pushes; never skips tests; refuses to run if working tree is dirty"
  artifacts:
    - path: "scripts/release.mjs"
      provides: "Single-script release ritual (D-17)"
      min_lines: 100
    - path: "CONTRIBUTING.md"
      provides: "Cut-a-release recipe + eval-gate documentation (D-06 + D-17)"
      min_lines: 30
    - path: ".github/workflows/publish.yml"
      provides: "Tag-triggered publish workflow extended with plugin-tarball asset attach"
      contains: "vault-memory-plugin"
  key_links:
    - from: "scripts/release.mjs"
      to: "CHANGELOG.md"
      via: "block-rename: ## [Unreleased] → ## [X.Y.Z] — YYYY-MM-DD"
      pattern: "## \\[Unreleased\\]"
    - from: ".github/workflows/publish.yml"
      to: "vault-memory-plugin-v2.0.0.tar.gz + manifest.sha256"
      via: "softprops/action-gh-release files: parameter"
      pattern: "softprops/action-gh-release"
    - from: "CONTRIBUTING.md"
      to: "scripts/release.mjs"
      via: "Cut-a-release recipe paragraph naming `npm run release`"
      pattern: "npm run release"
---

<objective>
Build the release infrastructure: (1) scripts/release.mjs single-script release ritual (D-17), (2) CONTRIBUTING.md with Cut-a-release recipe + eval-gate-no-override policy (D-06 + D-17), (3) extension to .github/workflows/publish.yml to build the plugin tarball and attach it + manifest.sha256 as Release assets (D-15 / Phase 7 carryover from Plan 07-11). The MP4 screencast asset is uploaded manually post-workflow (per RESEARCH Open Question 2).

Purpose: REL-06 — `npm run release` cuts the tag; CI auto-creates GitHub Release with all required assets. REL-01 — CONTRIBUTING.md documents the merge gate.

Output: One new script + one new markdown file + one workflow extension.

Constraints: ESM Node script matching project conventions (cf. scripts/smoketest-non-claude.mjs). No external dependencies. Atomic git push via `git push --follow-tags`. Refuses dirty tree, refuses non-main branch, refuses empty Unreleased. CHANGELOG rename produces exact format "## [X.Y.Z] — YYYY-MM-DD" matching publish.yml's awk extractor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@scripts/smoketest-non-claude.mjs
@.github/workflows/publish.yml
@.github/workflows/ci.yml
@package.json
@CHANGELOG.md

<interfaces>
scripts/release.mjs structure (anchored to PATTERNS §"Group E"):
- ESM Node 22 (file ends with .mjs; uses import statements)
- Imports: { readFile, writeFile } from "node:fs/promises"; { execSync, spawnSync } from "node:child_process"; { argv, exit } from "node:process"
- Top JSDoc explaining the release ritual
- CLI args: optional first arg = version string (e.g., "2.0.0"); if absent, prompt or auto-derive from package.json (recommended: REQUIRE explicit version arg for v2.0.0 cut)
- Phase 1: pre-flight validations
  - git status --porcelain returns empty (clean tree)
  - git branch --show-current returns "main"
  - CHANGELOG.md has a `## [Unreleased]` line
  - The Unreleased block is non-empty (more than just whitespace + `_Nothing yet._`)
  - process.exit(1) with clear stderr message on any failure
- Phase 2: test gate
  - execSync("npm test", { stdio: "inherit" }) — fail-fast
- Phase 3: version bump
  - execSync(`npm version ${VERSION} --no-git-tag-version`) — bumps package.json + package-lock.json
- Phase 4: CHANGELOG rename
  - Read CHANGELOG.md, find "## [Unreleased]" line index
  - Splice: replace the line with: "## [Unreleased]", "", "_Nothing yet._", "", `## [${VERSION}] — ${TODAY}` (TODAY = ISO date YYYY-MM-DD)
  - Write CHANGELOG.md
  - Heading format MUST be exactly "## [X.Y.Z]" (per Pitfall 2 — matches awk extractor in publish.yml:76)
- Phase 5: commit + tag + atomic push
  - execSync("git add package.json package-lock.json CHANGELOG.md")
  - execSync(`git commit -m "release: v${VERSION}"`)
  - execSync(`git tag -a v${VERSION} -m "v${VERSION}"`)
  - execSync("git push --follow-tags origin main") — single atomic push (Pitfall 2)
- Phase 6: confirmation message
  - Print to stderr: "Tag v${VERSION} pushed. .github/workflows/publish.yml will now run: npm publish (with provenance) → create GitHub Release. Upload the MP4 screencast asset manually after the workflow completes."

Error-handling depth: thorough (releases are rare; a botched release is expensive). Each phase failure prints a clear actionable error message to stderr.

CONTRIBUTING.md structure (anchored to PATTERNS §"Group K"):
- H1: Contributing to vault-memory
- H2: Cut a release (D-17)
  - Fenced bash: `npm run release 2.0.0` (or omit arg = error)
  - Numbered list of what the script does (mirrors Phase 1-6 above)
  - Note that publish.yml takes over after the push
  - Note that MP4 screencast asset must be uploaded manually post-workflow
- H2: Eval suite is a merge gate (D-06)
  - State: PRs targeting main cannot merge unless `lint-and-test` is green
  - List the 4 sub-checks (lint:check, npm test, eval:baseline, build + smoketest)
  - State explicitly: "There is no `[skip eval]` override. A red eval is a real signal."
  - Note: branch protection is configured via GitHub Settings → Branches → main → Require status checks (configured separately by plan 08-07)

publish.yml extension (anchored to PATTERNS §"Group F"):
- Locate the existing "Create GitHub Release" step that uses softprops/action-gh-release@v2 (around line 94-103)
- Insert NEW steps BEFORE it: "Build plugin tarball" + "Generate manifest.sha256"
  - cd plugin && npm ci && node esbuild.config.mjs
  - cd .. && tar -czf "vault-memory-plugin-v${GITHUB_REF_NAME#v}.tar.gz" -C plugin .
  - shasum -a 256 "vault-memory-plugin-v${GITHUB_REF_NAME#v}.tar.gz" > manifest.sha256
  - echo "TARBALL=vault-memory-plugin-v${GITHUB_REF_NAME#v}.tar.gz" >> "$GITHUB_ENV"
- Modify the softprops step to add a `files:` parameter listing the tarball + manifest.sha256
- Do NOT add the MP4 to the files: parameter (RESEARCH Open Q2 → manual upload)

package.json scripts block:
- Add: "release": "node scripts/release.mjs"
- All other scripts unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author scripts/release.mjs</name>
  <files>scripts/release.mjs, package.json</files>
  <read_first>
    - scripts/smoketest-non-claude.mjs (ESM Node script style anchor — header/imports/exit-code conventions)
    - .github/workflows/publish.yml lines 50-90 (awk CHANGELOG extractor — confirm "## [X.Y.Z]" heading format)
    - CHANGELOG.md (current "## [Unreleased]" structure)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group E"
    - package.json (scripts block — where to add "release" entry)
  </read_first>
  <action>Create scripts/release.mjs as an ESM Node 22 script with the 6-phase structure described in the interfaces block. Open with a JSDoc block (matching smoketest-non-claude.mjs:1-46 style) explaining the release ritual, the gates, and a Usage line: `node scripts/release.mjs 2.0.0`. Implement Phase 1 validations: parse argv[2] as VERSION (error if missing or not matching /^\d+\.\d+\.\d+(-\w+\.\d+)?$/); run git status --porcelain and exit 1 with stderr "Working tree is dirty — commit or stash first" if non-empty; run git branch --show-current and exit 1 with stderr "Not on main branch (currently: X)" if not "main"; read CHANGELOG.md and exit 1 with stderr "No `## [Unreleased]` heading found" if absent; parse the Unreleased block content (everything between `## [Unreleased]` and the next `## [` heading) and exit 1 with stderr "Unreleased block is empty — add user-visible changes before release" if it's only whitespace or `_Nothing yet._`. Implement Phase 2: execSync("npm test", { stdio: "inherit" }) — fail-fast. Implement Phase 3: execSync(`npm version ${VERSION} --no-git-tag-version`, { stdio: "inherit" }). Implement Phase 4: re-read CHANGELOG.md, locate `## [Unreleased]` line index, splice in: new Unreleased block ("## [Unreleased]", "", "_Nothing yet._", "") followed by the renamed heading (`## [${VERSION}] — ${TODAY}` where TODAY = new Date().toISOString().slice(0, 10)); write back. CRITICAL: the renamed heading uses EM DASH "—" U+2014 to match existing CHANGELOG formatting (verify by reading existing entries in CHANGELOG.md). Implement Phase 5: execSync("git add package.json package-lock.json CHANGELOG.md"); execSync(`git commit -m "release: v${VERSION}"`); execSync(`git tag -a v${VERSION} -m "v${VERSION}"`); execSync("git push --follow-tags origin main", { stdio: "inherit" }). Implement Phase 6: stderr message confirming what happens next (publish.yml runs; MP4 upload manual). Add "release": "node scripts/release.mjs" to package.json scripts.</action>
  <verify>
    <automated>test -f scripts/release.mjs &amp;&amp; node --check scripts/release.mjs &amp;&amp; grep -q '"release"' package.json &amp;&amp; node scripts/release.mjs 2>&amp;1 | grep -qE "version|argument|usage" &amp;&amp; echo "release script rejects missing arg OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/release.mjs exists and passes node --check (syntax valid)
    - Running without args produces a clear usage error to stderr
    - package.json scripts block has "release" pointing at the script
    - Script contains 6 distinct phases (grep for "Phase 1" through "Phase 6" or equivalent section markers)
    - File length ≥100 lines
  </acceptance_criteria>
  <done>scripts/release.mjs ready; smoke-tested by running with no args.</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test the release script with a dry-run RC version</name>
  <files>(temporary — no permanent file changes)</files>
  <read_first>
    - scripts/release.mjs (the just-written script)
  </read_first>
  <action>Smoke-test release.mjs WITHOUT actually pushing. Create a scratch worktree or use a feature branch and stash any pending changes. Test each pre-flight gate individually: (a) on a dirty tree → script should fail with "Working tree is dirty"; (b) on a feature branch (not main) → "Not on main"; (c) revert to main + clean tree but with `## [Unreleased]` block populated → should pass pre-flight, run npm test (allow this to run), then bump version, rename CHANGELOG. STOP before the git push (comment out the push line OR use a fake remote). Verify the CHANGELOG rename produces exactly `## [2.0.0-rc.1] — YYYY-MM-DD` with the em-dash character. REVERT the test changes (git checkout package.json package-lock.json CHANGELOG.md; remove any test tag). DO NOT actually push or publish. This task documents that the script works mechanically; the real cut is plan 08-08.</action>
  <verify>
    <automated>git status --porcelain | wc -l | awk '{if ($1 == 0) print "clean tree post-smoketest OK"; else { echo "FAIL: dirty after smoketest"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - Each pre-flight gate behaves as designed (manual verification log captured in summary)
    - Working tree is fully reverted after smoke test
    - No test tag remains in `git tag -l "v2.0.0-rc*"`
  </acceptance_criteria>
  <done>release.mjs mechanically validated; ready for production cut in plan 08-08.</done>
</task>

<task type="auto">
  <name>Task 3: Author CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <read_first>
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group K"
    - .github/workflows/ci.yml lines 1-50 (job name = "lint-and-test" — confirm)
    - scripts/release.mjs (the just-written script — content reference)
  </read_first>
  <action>Create CONTRIBUTING.md with two H2 sections plus a one-line H1 header. Section "Cut a release": fenced bash block showing `npm run release 2.0.0`; numbered list of the 6 phases the script performs (validates clean tree, runs npm test, bumps version via npm version, renames CHANGELOG, commits + tags, pushes atomically via --follow-tags); note that .github/workflows/publish.yml takes over after the push and handles npm publish + GitHub Release creation; explicit note: "Upload the MP4 screencast asset manually to the GitHub Release after the workflow completes (per D-13/D-14)." Section "Eval suite is a merge gate": state that PRs targeting main cannot merge unless the GitHub Actions check `lint-and-test` is green; list the four sub-checks (npm run lint:check; npm test; npm run eval:baseline; npm run build + node scripts/smoketest-non-claude.mjs); state explicitly: "If the check is red, fix the failure and push a new commit. There is no `[skip eval]` override. A red eval is a real signal."; note that branch protection is configured via GitHub Settings → Branches → main → "Require status checks to pass before merging" with `lint-and-test` selected (cross-reference plan 08-07). Voice: terse, technical, no marketing. ~30-50 lines total.</action>
  <verify>
    <automated>test -f CONTRIBUTING.md &amp;&amp; grep -q "npm run release" CONTRIBUTING.md &amp;&amp; grep -q "lint-and-test" CONTRIBUTING.md &amp;&amp; grep -q "no .skip eval. override" CONTRIBUTING.md || grep -q "no \`\[skip eval\]\` override" CONTRIBUTING.md &amp;&amp; [ "$(wc -l &lt; CONTRIBUTING.md)" -ge 25 ]</automated>
  </verify>
  <acceptance_criteria>
    - CONTRIBUTING.md exists at repo root
    - Contains "npm run release" recipe
    - Contains "lint-and-test" status-check name
    - Contains the explicit no-override statement
    - Length ≥25 lines
  </acceptance_criteria>
  <done>CONTRIBUTING.md authored with two minimum required sections.</done>
</task>

<task type="auto">
  <name>Task 4: Extend .github/workflows/publish.yml to build plugin tarball + attach as Release asset</name>
  <files>.github/workflows/publish.yml</files>
  <read_first>
    - .github/workflows/publish.yml (full file — locate the softprops/action-gh-release@v2 step around line 94-103)
    - plugin/esbuild.config.mjs (verify the plugin build command)
    - plugin/package.json (verify "npm ci" + esbuild config produces the expected output)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group F"
  </read_first>
  <action>Edit .github/workflows/publish.yml. Before the existing "Create GitHub Release" step (uses: softprops/action-gh-release@v2), insert NEW steps: (1) "Build plugin tarball" — runs `cd plugin && npm ci` then `node esbuild.config.mjs` (or whatever the plugin's build command is — verify via plugin/package.json scripts); after building, cd back to the repo root and run `TARBALL="vault-memory-plugin-v${GITHUB_REF_NAME#v}.tar.gz"; tar -czf "$TARBALL" -C plugin .; echo "TARBALL=$TARBALL" >> "$GITHUB_ENV"`. (2) "Generate manifest.sha256" — runs `shasum -a 256 "$TARBALL" > manifest.sha256`. Then modify the existing softprops/action-gh-release@v2 step to add a `files:` parameter (multi-line YAML literal block): `${{ env.TARBALL }}` and `manifest.sha256`. DO NOT add the MP4 to files: (manual upload per D-14 / RESEARCH Open Q2). Validate YAML syntax via `actionlint` if available or `yq eval .` on the file.</action>
  <verify>
    <automated>grep -q "vault-memory-plugin" .github/workflows/publish.yml &amp;&amp; grep -q "manifest.sha256" .github/workflows/publish.yml &amp;&amp; grep -q "files:" .github/workflows/publish.yml &amp;&amp; (command -v yq &gt;/dev/null &amp;&amp; yq eval '.' .github/workflows/publish.yml &gt;/dev/null) || echo "YAML lint not available; manual review required"</automated>
  </verify>
  <acceptance_criteria>
    - publish.yml has new "Build plugin tarball" step before the GitHub Release step
    - publish.yml has new "Generate manifest.sha256" step
    - softprops/action-gh-release@v2 step has a files: parameter listing both the tarball and manifest.sha256
    - YAML is valid (yq parse OK or actionlint clean)
    - MP4 is NOT in the files: parameter (manual upload)
  </acceptance_criteria>
  <done>publish.yml extended to ship plugin tarball + checksum as Release assets.</done>
</task>

<task type="auto">
  <name>Task 5: Full test sweep + dry-run publish.yml validation</name>
  <files>(read-only verification)</files>
  <read_first>
    - All files modified in Tasks 1-4
  </read_first>
  <action>Run the full CI sweep locally: npm run lint:check, npm test, npm run eval:baseline, npm run build, node scripts/smoketest-non-claude.mjs. All five must exit 0 (Plan 08-05's REL-08 work is already merged at this point per dependency on plan 08-01; the snapshot tests should still pass). Then validate publish.yml syntax via actionlint (if available) or yq. Optionally use `act -j publish --dryrun` to mechanically validate the workflow if act is installed.</action>
  <verify>
    <automated>npm run lint:check 2>&amp;1 | tail -3 &amp;&amp; npm test 2>&amp;1 | tail -5 &amp;&amp; npm run eval:baseline 2>&amp;1 | tail -3 &amp;&amp; npm run build 2>&amp;1 | tail -3 &amp;&amp; node scripts/smoketest-non-claude.mjs 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 5 CI steps exit 0
    - publish.yml YAML parses correctly
  </acceptance_criteria>
  <done>Release infrastructure validated locally; ready for plan 08-07 (branch protection) and plan 08-08 (live cut).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local maintainer→github.com | `git push --follow-tags` is the trust handoff to GitHub Actions for npm publish; releases.mjs runs only on the maintainer's machine |
| github.com→npmjs.com | publish.yml runs `npm publish --provenance` with OIDC; supply-chain attestation comes from GitHub's OIDC provider |
| github.com→Release assets | softprops/action-gh-release@v2 uploads tarball + manifest.sha256 with the maintainer's GITHUB_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-T | Tampering | scripts/release.mjs allowing dirty-tree commits | mitigate | Phase 1 validation refuses dirty tree; refuses non-main branch; refuses empty Unreleased |
| T-08-06-T | Tampering | publish.yml supply-chain (npm publish without provenance) | mitigate | Existing `--provenance` + `id-token: write` already in publish.yml; not regressed by Task 4 |
| T-08-06-R | Repudiation | Force-push to main during release | mitigate | release.mjs uses --follow-tags (no --force); branch protection in plan 08-07 prevents force-push to main |
| T-08-06-SC | Tampering | Plugin tarball substitution | mitigate | manifest.sha256 ships alongside the tarball as Release asset; downstream vm-install verifies it (Phase 7 Plan 07-11) |
</threat_model>

<verification>
- node --check scripts/release.mjs succeeds
- npm run release (no args) prints a usage error to stderr
- CONTRIBUTING.md exists with both required sections
- publish.yml has Build-plugin-tarball + manifest.sha256 + files: parameter
- Full CI sweep exits 0
</verification>

<success_criteria>
Release infrastructure landed: scripts/release.mjs ready, CONTRIBUTING.md documents the ritual + merge gate, publish.yml extended to ship the plugin tarball + checksum. Plans 08-07 and 08-08 consume this infrastructure.
</success_criteria>

<output>
Create .planning/phases/08-polish-eval-suite-v2-0-0-release/08-06-SUMMARY.md documenting: release.mjs line count, CONTRIBUTING.md sections, publish.yml diff size, smoke-test outcome.
</output>
