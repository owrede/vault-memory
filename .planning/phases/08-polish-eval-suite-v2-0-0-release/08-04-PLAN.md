---
phase: 08-polish-eval-suite-v2-0-0-release
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v2/plugin/screencast-thumbnail.png
  - docs/v2/plugin/INSTALL.md
  - docs/v2/plugin/CONTRACT-EDITOR.md
autonomous: false
requirements:
  - REL-02
must_haves:
  truths:
    - "A ≤8-minute MP4 screencast covering install → first contract authored → first instantiate_contract call exists on disk, ready for upload as a GitHub Release asset"
    - "A PNG thumbnail (≤600px wide, click-through hint) exists at docs/v2/plugin/screencast-thumbnail.png and is committed to the repo"
    - "docs/v2/plugin/INSTALL.md no longer contains 'TBD' / 'deferred to Phase 8' / 'RELEASE_URL_PLACEHOLDER' markers — every screencast and Release-URL reference is resolved or marked as 'attached at v2.0.0 release time'"
    - "docs/v2/plugin/CONTRACT-EDITOR.md has its screencast-deferral note replaced with the canonical reference pattern"
  artifacts:
    - path: "docs/v2/plugin/screencast-thumbnail.png"
      provides: "Static thumbnail for click-through links from README + INSTALL + CONTRACT-EDITOR"
      min_lines: 0
    - path: "docs/v2/plugin/INSTALL.md"
      provides: "Updated install doc with resolved screencast reference"
      contains: "screencast"
    - path: "docs/v2/plugin/CONTRACT-EDITOR.md"
      provides: "Updated contract-editor doc with resolved screencast reference"
      contains: "screencast"
  key_links:
    - from: "docs/v2/plugin/INSTALL.md"
      to: "docs/v2/plugin/screencast-thumbnail.png"
      via: "markdown image-link click-through"
      pattern: "screencast-thumbnail"
    - from: "docs/v2/plugin/CONTRACT-EDITOR.md"
      to: "docs/v2/plugin/screencast-thumbnail.png"
      via: "markdown image-link click-through"
      pattern: "screencast-thumbnail"
---

<objective>
Land the two Phase 7 carryover artifacts that ship with the v2.0.0 GitHub Release: (1) the ≤8-minute plugin walkthrough screencast (recorded by the maintainer, exported as MP4 1080p — NOT committed to the repo per D-14; staged locally for upload as a Release asset by plan 08-08) and (2) the docs/v2/plugin/screencast-thumbnail.png that lives in-tree as the click-through anchor. Resolve the deferral notes in docs/v2/plugin/INSTALL.md + docs/v2/plugin/CONTRACT-EDITOR.md so they reference the GitHub Release MP4 by the canonical URL pattern.

Purpose: REL-02 indirect (the CHANGELOG-curated v2.0.0 includes the screencast as a Phase 7 carryover deliverable); main goal is unblocking the live-vault dry-run of vm-install / vm-update at release time (carries from Phase 7 Plan 07-11 Task 3 deferred human-verify).

Output: Three on-disk artifacts plus one off-disk artifact (MP4 staged on maintainer's machine, ready for upload).

Scope (D-13 + D-14): Strict 5-7 min storyboard / 8 min hard cap. MP4 1080p. No music. Captions baked in via title cards. Recorded with macOS QuickTime. Uploaded to GitHub Release only — not committed to git.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-CONTEXT.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-RESEARCH.md
@.planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md
@docs/v2/plugin/INSTALL.md
@docs/v2/plugin/CONTRACT-EDITOR.md
@skills/vm-install/setup.sh
@skills/vm-update/update.sh

<interfaces>
Screencast storyboard (verbatim from D-13):
1. 0:00–1:00 Install via vm-install skill — show the skill run, prompt to enable in Obsidian
2. 1:00–2:00 Open examples/contracts/meeting-prep.contract in Obsidian — visual editor opens automatically; palette + canvas + inspector visible
3. 2:00–3:30 Edit one verb arg in the inspector — show typed-form autocomplete + live .yaml regenerating on save
4. 3:30–5:00 Switch to terminal / Claude Desktop / MCP Inspector — call instantiate_contract({name: "meeting-prep", inputs: {...}}) — show the brief returned
5. 5:00–7:00 Quick tour of plugin chrome — stats panel, secrets entry, reindex trigger, connectors panel

Recording constraints (D-13):
- Hard cap: ≤8 min
- macOS QuickTime native screen recording
- Cursor highlights ON
- Export: MP4 1080p
- No background music
- Captions baked in as static title-card highlights (no separate VTT)
- Minimal intro/outro card with project name + version "v2.0.0"

Asset target filenames:
- vault-memory-plugin-walkthrough.mp4 (Release asset, NOT committed to repo per D-14)
- docs/v2/plugin/screencast-thumbnail.png (committed to repo; ≤600px wide; renders cleanly in README at default GitHub image scaling)

Canonical Release URL pattern (per RESEARCH Pitfall 5):
- MP4: https://github.com/owrede/vault-memory/releases/download/v2.0.0/vault-memory-plugin-walkthrough.mp4

Deferral-note locations to update (read these for current state):
- docs/v2/plugin/INSTALL.md — find "TBD" / "deferred" / "RELEASE_URL_PLACEHOLDER" / "Phase 8" mentions and replace with the resolved pattern
- docs/v2/plugin/CONTRACT-EDITOR.md — same

Skill scripts (per Pitfall 5 — no edit needed):
- skills/vm-install/setup.sh:26 already has literal v2.0.0 URL; works once Release is published
- skills/vm-update/update.sh:23 uses v__VERSION__ template; works at runtime
- This plan does NOT touch these files; final verification in plan 08-08
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Record the screencast (maintainer-only)</name>
  <what-built>D-13 storyboard reference for the 5–7 minute walkthrough. Not yet recorded — this task IS the recording.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    Before recording:
    1. Ensure a fresh test Obsidian vault is available with the v2.0.0 plugin sideloaded (or that the vm-install dry-run path is set up).
    2. Open examples/contracts/meeting-prep.contract in advance to confirm the editor opens.
    3. Stage Claude Desktop or MCP Inspector to call instantiate_contract({name: "meeting-prep", inputs: {meeting_date: "2026-05-19"}}) and observe the brief returned.
    4. Test that QuickTime screen-record with cursor highlights produces 1080p MP4 output.

    Recording (storyboard):
    - Section A (0:00–1:00) — Run the vm-install skill from Claude or shell; Obsidian recognizes the plugin; user enables it
    - Section B (1:00–2:00) — Open meeting-prep.contract; the visual editor (palette + canvas + inspector) auto-launches via the registered view
    - Section C (2:00–3:30) — Edit one verb arg in the right-pane inspector; show the typed-form autocomplete; on save, switch to the file tree and show the regenerated _contracts/meeting-prep.yaml updating live
    - Section D (3:30–5:00) — Switch to Claude Desktop or MCP Inspector; call instantiate_contract; show the brief returned in the agent's output
    - Section E (5:00–7:00) — Quick chrome tour: settings tab (Ollama URL, embed model), secrets panel (add a secret via name; demonstrate the safeStorage prompt), reindex button (manual trigger; show progress), connectors panel (list peer MCP clients)
    - Intro/outro cards: 2-3 second title cards top and tail with "vault-memory v2.0.0 — Plugin walkthrough" and a closing card "github.com/owrede/vault-memory"

    Post-record:
    1. Export from QuickTime as MP4 (Apple H.264 1080p preset is fine).
    2. Save locally at: vault-memory-plugin-walkthrough.mp4 in the maintainer's release-staging directory (NOT in the repo).
    3. Verify file size is reasonable (under ~200 MB; trim if needed).
    4. Spot-check the cursor highlights are visible and the audio (if any) is balanced.
    5. Verify total runtime ≤8:00 — if over, trim a beat (Section E is the most compressible).
  </how-to-verify>
  <acceptance_criteria>
    - vault-memory-plugin-walkthrough.mp4 exists on the maintainer's local filesystem
    - Total runtime is ≤8:00 (D-13 hard cap)
    - All five storyboard sections are present
    - Exported at 1080p MP4
    - Maintainer confirms ready for upload to GitHub Release in plan 08-08
  </acceptance_criteria>
  <resume-signal>Type "recorded" and provide the local filepath; or describe blockers</resume-signal>
  <done>MP4 staged locally; not yet committed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Produce screencast-thumbnail.png and commit to docs/v2/plugin/</name>
  <what-built>Static PNG image for click-through links in README and plugin docs.</what-built>
  <files>(human checkpoint — no autonomous file change)</files>
  <action>This is a human checkpoint task. The maintainer performs the steps documented in &lt;how-to-verify&gt; below; the &lt;acceptance_criteria&gt; section lists the conditions that must hold before resuming.</action>
  <verify><human-check>See &lt;how-to-verify&gt; for the manual verification steps.</human-check></verify>
  <how-to-verify>
    Producing the thumbnail:
    1. Pick the most visually distinctive frame from the screencast — recommendation: Section B at around 1:30, showing the three-pane editor with palette + canvas + inspector all visible.
    2. Capture it as a still PNG using QuickTime's "Copy current frame" or macOS Preview.
    3. Resize to width ≤600px (e.g., 600x375 for 16:10 framing).
    4. Optionally add a minimal play-button overlay (use Preview's annotation tools — no need for a design tool) to communicate "click for video".
    5. Save as docs/v2/plugin/screencast-thumbnail.png.
    6. File size target: under 200 KB after PNG optimization.

    After saving:
    1. Verify the file exists at the expected path.
    2. Render it in a markdown preview (paste a markdown image link `![](docs/v2/plugin/screencast-thumbnail.png)` and check it displays correctly at GitHub default image scaling).
    3. Commit it along with the doc edits in Task 3 (Task 3 stages both for a single commit).
  </how-to-verify>
  <acceptance_criteria>
    - docs/v2/plugin/screencast-thumbnail.png exists
    - Width ≤600px verified by `file docs/v2/plugin/screencast-thumbnail.png` or `identify docs/v2/plugin/screencast-thumbnail.png` (ImageMagick) — manual check by maintainer if neither tool available
    - PNG file size <200 KB
    - Maintainer confirms readable thumbnail content
  </acceptance_criteria>
  <resume-signal>Type "thumbnail-ready" or describe blockers</resume-signal>
  <done>screencast-thumbnail.png staged at the canonical path.</done>
</task>

<task type="auto">
  <name>Task 3: Replace deferral notes in docs/v2/plugin/INSTALL.md and CONTRACT-EDITOR.md</name>
  <files>docs/v2/plugin/INSTALL.md, docs/v2/plugin/CONTRACT-EDITOR.md</files>
  <read_first>
    - docs/v2/plugin/INSTALL.md (full file — locate all TBD/deferred/Phase-8/RELEASE_URL_PLACEHOLDER mentions)
    - docs/v2/plugin/CONTRACT-EDITOR.md (full file — same)
    - .planning/phases/08-polish-eval-suite-v2-0-0-release/08-PATTERNS.md §"Group L — Phase 7 carryover edits"
  </read_first>
  <action>Grep docs/v2/plugin/INSTALL.md for tokens matching "TBD|deferred|Phase 8|RELEASE_URL_PLACEHOLDER|screencast" (case-insensitive). For each match, replace with the canonical screencast reference pattern: a markdown image-link where the image is the thumbnail and the link target is the Release URL. The replacement block uses this exact form: `[![Plugin walkthrough (5-7 min)](./screencast-thumbnail.png)](https://github.com/owrede/vault-memory/releases/download/v2.0.0/vault-memory-plugin-walkthrough.mp4)` — adjust the relative thumbnail path based on the file's location (INSTALL.md and CONTRACT-EDITOR.md both live in docs/v2/plugin/ so `./screencast-thumbnail.png` is correct). Do NOT change unrelated content. Do NOT edit skills/vm-install/setup.sh or skills/vm-update/update.sh (per Pitfall 5 they are already correct; final verification is in plan 08-08). Apply the same pattern to docs/v2/plugin/CONTRACT-EDITOR.md.</action>
  <verify>
    <automated>! grep -i "RELEASE_URL_PLACEHOLDER\|TBD.*screencast\|screencast.*TBD\|deferred to Phase 8" docs/v2/plugin/INSTALL.md docs/v2/plugin/CONTRACT-EDITOR.md &amp;&amp; grep -q "screencast-thumbnail.png" docs/v2/plugin/INSTALL.md &amp;&amp; grep -q "screencast-thumbnail.png" docs/v2/plugin/CONTRACT-EDITOR.md &amp;&amp; grep -q "releases/download/v2.0.0/vault-memory-plugin-walkthrough.mp4" docs/v2/plugin/INSTALL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep for "RELEASE_URL_PLACEHOLDER|TBD.*screencast|deferred to Phase 8" returns 0 matches in both docs
    - Both docs contain the canonical screencast-thumbnail.png reference
    - Both docs contain the v2.0.0 Release MP4 URL
    - git diff shows only the two doc files modified (no collateral edits to plugin/ source code, skills/, or other docs)
  </acceptance_criteria>
  <done>Deferral notes resolved; canonical screencast pattern landed in both docs.</done>
</task>

<task type="auto">
  <name>Task 4: Validate that the screencast pattern also appears in README.md (cross-link to plan 08-02)</name>
  <files>README.md (read-only verification)</files>
  <read_first>
    - README.md (just rewritten by plan 08-02)
  </read_first>
  <action>Grep README.md to confirm it references docs/v2/plugin/screencast-thumbnail.png at least once (this confirms plan 08-02's §6 install-and-docs section correctly links the screencast). If README.md does NOT reference the thumbnail, file a flag for plan 08-02's revision — do NOT modify README.md here (out of scope; README is plan 08-02's territory). Proceed if README contains the thumbnail reference.</action>
  <verify>
    <automated>grep -q "screencast-thumbnail.png\|vault-memory-plugin-walkthrough" README.md &amp;&amp; echo "README links screencast OK" || { echo "WARN: README does not reference screencast — flag plan 08-02"; exit 0; }</automated>
  </verify>
  <acceptance_criteria>
    - Either README.md references the screencast thumbnail / MP4 (success) OR a flag is raised for plan 08-02 revision (non-blocking warning)
  </acceptance_criteria>
  <done>README cross-reference checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo→Release-asset CDN | screencast MP4 is uploaded to GitHub Releases (CDN-hosted); URL pattern is predictable; integrity is publisher-attested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-I | Information disclosure | Screencast leaks private data | mitigate | Maintainer pre-records in a fresh test vault (no private notes); Task 1 manual checkpoint |
| T-08-04-T | Tampering | MP4 substitution risk after upload | accept | GitHub Releases is the trust anchor; URL is predictable; not in scope for v2.0.0 (would require signed-release upgrade in v2.x) |
</threat_model>

<verification>
- vault-memory-plugin-walkthrough.mp4 staged locally on maintainer's machine (manual-verified at Task 1)
- docs/v2/plugin/screencast-thumbnail.png exists and is ≤600px wide (Task 2)
- grep "RELEASE_URL_PLACEHOLDER|deferred to Phase 8" docs/v2/plugin/{INSTALL,CONTRACT-EDITOR}.md returns 0 matches
- Both docs reference screencast-thumbnail.png and the v2.0.0 MP4 URL
- README cross-reference confirmed
</verification>

<success_criteria>
Phase 7 carryover screencast staged for upload; thumbnail committed to repo; plugin docs updated. Plan 08-08 uploads the MP4 at release time and verifies the URLs resolve.
</success_criteria>

<output>
Create .planning/phases/08-polish-eval-suite-v2-0-0-release/08-04-SUMMARY.md documenting: local MP4 path, recording duration, thumbnail dimensions, doc edits applied.
</output>
