---
phase: 02-memory-namespace-provenance-contract
plan: 08
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-03b, 02-04, 02-05, 02-06, 02-07]
files_modified:
  - CHANGELOG.md
  - .planning/STATE.md
autonomous: false
requirements:
  - MEM-01
  - MEM-02
  - MEM-03
  - MEM-04
  - MEM-05
  - MEM-06
  - MEM-07
  - MEM-08
  - MEM-09
  - MEM-10
  - MEM-11
  - MEM-12
must_haves:
  truths:
    - "All 324+ Phase 1 tests + every Phase 2 plan's tests pass under one `npm test` invocation"
    - "v1-baseline eval suite remains byte-for-byte green: `npm run eval:baseline` exits 0"
    - "`scripts/lint-adapters.sh` reports zero seam-preservation violations across the entire Phase 2 surface"
    - "All 5 ROADMAP Phase 2 success criteria are demonstrably true with named verification steps for each"
    - "All 12 MEM requirements (MEM-01..MEM-12) trace to specific plan deliverables — the traceability table in this plan body matches the actual codebase"
    - "MCP Inspector / `scripts/smoketest-non-claude.mjs` from Phase 1 still passes AND exercises at least one new memory tool (`record_observation`) and one new Resource (`memory_stats`) — agent-agnosticism is preserved end-to-end"
    - "CHANGELOG `[Unreleased]` section is updated with all Phase 2 additions, including the audit_log new-filter capability documented in CHANGELOG and `docs/tools/audit_log.md` (NOT in the audit_log MCP description text — that text is byte-identical to Phase 1)"
    - "STATE.md is updated to reflect Phase 2 completion (position pointer moves to Phase 3); decisions/blockers list rolls over"
    - "Final autonomous: false checkpoint with the user — the developer reviews the traceability + signs off before any external merge"
  artifacts:
    - path: CHANGELOG.md
      provides: Unreleased section with the Phase 2 changes (Added: 3 tools + 2 resources + audit column; Changed: extended WriteConflict union, audit_log gains optional input filter; Documentation: ADR-004 amendment + audit_log doc page)
    - path: .planning/STATE.md
      provides: phase position updated to phase 3; pending todos updated
  key_links:
    - from: CHANGELOG.md
      to: .planning/phases/02-memory-namespace-provenance-contract/
      via: Phase 2 summary entries reference plan numbers (02-01..02-07, 02-03b)
      pattern: "02-0[1-7]|02-03b|Phase 2"
---

<objective>
Final phase-gate verification + sign-off. This is NOT an implementation plan — every code deliverable already landed in Plans 02-01..02-07 and 02-03b. This plan's job is to:

1. **Verify in one go** that all Phase 2 work composes correctly — `npm test`, `npm run eval:baseline`, `scripts/lint-adapters.sh`, `npx tsc --noEmit`, `node scripts/smoketest-non-claude.mjs`.
2. **Map each ROADMAP Phase 2 success criterion** to a specific concrete verification step + the plan that satisfied it.
3. **Map each MEM-NN requirement** (12 total) to the plan(s) that delivered it — including the 02-03 / 02-03b split.
4. **Run the MCP Inspector / non-Claude smoketest** to confirm Phase 2's new tools + Resources work from a non-Claude MCP client.
5. **Update CHANGELOG.md** with the Phase 2 additions under `[Unreleased]` — including the audit_log filter (documented HERE in CHANGELOG, NOT in the MCP description text).
6. **Update STATE.md** to reflect phase completion + roll forward to Phase 3 position.
7. **Final checkpoint with the user** — `autonomous: false`.

This plan does NOT modify production source code. The only file changes are CHANGELOG.md and STATE.md plus the verification reports / summary.

Risk note: this plan can find latent integration issues. Any such finding goes back to the corresponding plan as a fix-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-memory-namespace-provenance-contract/02-CONTEXT.md
@.planning/phases/02-memory-namespace-provenance-contract/02-01-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-02-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-03-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-03b-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-04-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-05-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-06-SUMMARY.md
@.planning/phases/02-memory-namespace-provenance-contract/02-07-SUMMARY.md
@CHANGELOG.md
@scripts/smoketest-non-claude.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-stack verification — tests, lint, type-check, baseline eval</name>
  <files>(no files modified — verification only)</files>
  <action>
    Run the full suite of automated checks. Each command must exit 0 cleanly:

    1. `npm test` — must report all suites passing, total count ≥ 324 (Phase 1 baseline) + Phase 2 additions across Plans 02-01..02-07 and 02-03b. Capture the exact test count for the SUMMARY.
    2. `npx tsc --noEmit` — must exit cleanly.
    3. `bash scripts/lint-adapters.sh` — must report zero seam-preservation violations.
    4. `npm run eval:baseline` — v1-baseline eval suite must remain byte-for-byte green. The audit_log description text MUST be byte-identical to Phase 1 (Plan 02-06 invariant); the only audit_log snapshot diff is on its `inputSchema` (one new optional field).
    5. `npm run lint:check` (if Phase 1 added the alias for the seam-preservation lint script) must exit clean.

    If ANY command fails: record the failure in a `BLOCKER` note, do NOT proceed to subsequent tasks, and report back to the orchestrator.
  </action>
  <verify>
    <automated>npm test && npx tsc --noEmit && bash scripts/lint-adapters.sh && npm run eval:baseline && npm run lint:check 2>/dev/null || true</automated>
  </verify>
  <done>All five commands exit cleanly; failure shifts back to originating plan.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP Phase 2 success criteria — one verification step per criterion</name>
  <files>(no files modified — verification only; findings recorded in SUMMARY)</files>
  <action>
    Demonstrate each of the 5 ROADMAP Phase 2 success criteria with a specific automated verification step. Record results in the SUMMARY.

    **Criterion 1** — naive `write_note` to memory-sink path rejected + v1 tools refuse `source: agent` outside sink.
    ```bash
    npx vitest run --no-coverage \
      -t "MEM-11" \
      -t "v1 write_note refuses memory-sink targets" \
      -t "agent_write_outside_sink" \
      src/server.test.ts src/memory/validator.test.ts src/adapters/delivery/conformance.test.ts
    ```

    **Criterion 2** — record/recall/supersede tools write/read through DeliveryAdapter; validator centralized.
    ```bash
    npx vitest run --no-coverage \
      -t "MEM-02 + MEM-04 end-to-end" \
      -t "MEM-03 recall end-to-end" \
      src/server.test.ts
    grep -rn "validateAgentWrite" src/memory/tools/ src/adapters/delivery/ | grep -v ".test.ts"
    # Expect: validateAgentWrite is called from the delivery adapters only,
    # NOT from the tool controllers (under src/memory/tools/).
    ```

    **Criterion 3** — handle parser is only resolver; sentinel prevents bare-folder resolution.
    ```bash
    grep -rn "_memory/" src/ | grep -v ".test.ts" | grep -v "src/memory/" | grep -v "src/adapters/delivery/obsidian-fs/sentinel.ts"
    # Expect: zero matches
    npx vitest run --no-coverage -t "sentinel_missing" src/adapters/delivery/obsidian-fs/write.test.ts
    ```

    **Criterion 4** — memory_stats + list_sinks shipped as Resources; audit_log flags memory-sink writes.
    ```bash
    npx vitest run --no-coverage -t "MEM-09" src/server.test.ts
    grep -c 'name: "memory_stats"\|name: "list_sinks"' src/tool-registry.ts
    # Expect: 0 (neither appears as a tool)
    grep -c "registerResource" src/server.ts
    # Expect: >= 2
    npx vitest run --no-coverage -t "MEM-08" src/server.test.ts src/audit/audit.test.ts
    ```

    **Criterion 5** — ADR-004 amendment committed before implementation; 20-doc fixture.
    ```bash
    git log --oneline docs/v2/adr/004-memory-sink-handles.md | head -5
    FIX_COUNT=$(find evals/fixtures/v2-test-vault/_memory/ -name '*.md' | wc -l | tr -d ' ')
    [ "$FIX_COUNT" = "20" ] || { echo "FAIL: expected 20 fixture docs, got $FIX_COUNT"; exit 1; }
    npx vitest run --no-coverage evals/v2-fixtures.test.ts
    ```

    Record each criterion's verification command output in `02-08-SUMMARY.md`. If any criterion fails verification, BLOCKER → fix in the originating plan.
  </action>
  <verify>
    <automated>echo "Criterion verification commands listed in action; executor runs each and records output"</automated>
  </verify>
  <done>All 5 success criteria demonstrably true with named verification steps; results recorded in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: MEM-01..MEM-12 traceability table (accounts for 02-03/02-03b split)</name>
  <files>(no files modified — table written into SUMMARY)</files>
  <action>
    Build a single canonical traceability table mapping each of the 12 MEM requirements to its delivering plan(s), the concrete files involved, and the test or verification step that confirms it. Write into `02-08-SUMMARY.md`:

    | Req ID | Description | Plan(s) | Key file(s) | Verifying test/step |
    |--------|-------------|---------|-------------|---------------------|
    | MEM-01 | MemorySink handle parser is the only resolver | 02-02 | src/memory/sink.ts, src/memory/registry.ts | sink.test.ts + registry.test.ts + grep for `_memory/` literal outside src/memory/ |
    | MEM-02 | record_observation MCP tool | 02-04 | src/memory/tools/record-observation.ts | record-observation.test.ts + server.test.ts MEM-02 e2e block |
    | MEM-03 | recall MCP tool | 02-05 | src/memory/tools/recall.ts, src/memory/citation-packet.ts | recall.test.ts + server.test.ts MEM-03 e2e block |
    | MEM-04 | supersede MCP tool (forward-only) | 02-04 | src/memory/tools/supersede.ts | supersede.test.ts + server.test.ts MEM-04 e2e block |
    | MEM-05 | Centralized provenance validator at DeliveryAdapter.write() | 02-02 (substrate) + 02-03 (validator at delivery seam) | src/memory/validator.ts, src/adapters/delivery/obsidian-fs/index.ts, conformance.test.ts | validator.test.ts + conformance cases 11-18 + grep `validateAgentWrite` call sites |
    | MEM-06 | .memory-sink sentinel file mechanics | 02-02 (provisioning) + 02-03 (runtime check) | src/adapters/delivery/obsidian-fs/sentinel.ts | sentinel.test.ts + conformance cases 19-21 |
    | MEM-07 | v1 tools refuse memory-sink targets | 02-03b | src/write/write.ts, src/frontmatter/update.ts | write.test.ts + update.test.ts entry-point Guard cases + conformance case 18 + server.test.ts MEM-11 block |
    | MEM-08 | audit_log distinctly flags memory-sink writes | 02-06 | src/db/schema.ts migration v9 + src/audit/audit.ts | audit.test.ts + server.test.ts MEM-08 cases |
    | MEM-09 | memory_stats + list_sinks as MCP Resources | 02-06 | src/memory/resources/, src/server.ts | server.test.ts MEM-09 cases + Resources are NOT in tool-registry |
    | MEM-10 | 20-doc fixture with diverse provenance | 02-07 | evals/fixtures/v2-test-vault/_memory/ + tests/fixtures/malformed-memory/ | evals/v2-fixtures.test.ts |
    | MEM-11 | Naive write_note → clear structured error | 02-03b | src/server.test.ts MEM-11 block | server.test.ts MEM-11 integration test |
    | MEM-12 | ADR-004 amendment ratification (doc-only) | 02-01 | docs/v2/adr/004-memory-sink-handles.md, docs/v2/MEMORY_CONTRACT.md | git log + grep for amendment footer |

    For each row, confirm the test/step name actually exists in the codebase.
  </action>
  <verify>
    <automated>echo "Traceability table written; executor confirms each named test exists via grep"</automated>
  </verify>
  <done>Table complete in SUMMARY; every row has a real, locatable verification; MEM-07 and MEM-11 correctly attribute to 02-03b (not 02-03).</done>
</task>

<task type="auto">
  <name>Task 4: MCP Inspector / non-Claude smoketest — exercise a new tool + a new Resource</name>
  <files>(no files modified — verification step; script invocation is the deliverable)</files>
  <action>
    Run `scripts/smoketest-non-claude.mjs`.

    1. Confirm the script exists and is executable.
    2. Read the script's current scope. If it does NOT already exercise a memory tool + a memory Resource, extend it (small additive change).
    3. Run the smoketest against a freshly-built server:
       ```bash
       npm run build && node scripts/smoketest-non-claude.mjs
       ```
       Expected: script exits 0; both new assertions (record_observation tool + memory_stats Resource) pass.

    NOTE: if `scripts/smoketest-non-claude.mjs` doesn't yet exist, raise as BLOCKER for Phase 1 retro fix rather than authoring it in Phase 2.
  </action>
  <verify>
    <automated>test -x scripts/smoketest-non-claude.mjs && npm run build && node scripts/smoketest-non-claude.mjs</automated>
  </verify>
  <done>Smoketest passes; record_observation tool + memory_stats Resource exercised end-to-end through a non-Claude MCP client.</done>
</task>

<task type="auto">
  <name>Task 5: Update CHANGELOG.md with Phase 2 additions</name>
  <files>CHANGELOG.md</files>
  <action>
    Update `CHANGELOG.md` `[Unreleased]` section with all Phase 2 changes. Plan 02-01 already added a Documentation line for the ADR-004 amendment; this task adds the implementation entries.

    Append (or update if subsections already exist) under `[Unreleased]`:

    ```markdown
    ### Added
    - `record_observation` MCP tool: write a labeled memory observation through a configured MemorySink with mandatory provenance (Phase 2 plan 02-04 / MEM-02).
    - `recall` MCP tool: retrieve memory documents filtered by `min_confidence`, `types`, `max_age_days`, and `sink`; returns Phase 3 citation packets (Phase 2 plan 02-05 / MEM-03).
    - `supersede` MCP tool: forward-only mark of a memory document as superseded by a replacement (Phase 2 plan 02-04 / MEM-04).
    - `vault-memory://memory/sinks` MCP Resource: lists configured memory sinks per vault (Phase 2 plan 02-06 / MEM-09).
    - `vault-memory://memory/stats` MCP Resource: per-sink doc counts, type/status breakdown, last-write timestamp (Phase 2 plan 02-06 / MEM-09).
    - `is_memory_sink_write` column on `write_audit` (migration v9) + optional filter on the `audit_log` tool (Phase 2 plan 02-06 / MEM-08).
    - `decomposeDocId` helper on `src/adapters/registry.ts` for splitting DocIds into `(scheme, authority, resource)` parts (Phase 2 plan 02-02).
    - `pathInSink` + `joinVaultPath` helpers in `src/adapters/delivery/obsidian-fs/path.ts` — the SOLE licensed sink/vault path-join sites for Phase 2+ (Phase 2 plan 02-02).
    - MemorySink runtime: handle parser, sentinel mechanics, provenance contract loader, MemorySinkRegistry (Phase 2 plan 02-02 / MEM-01, MEM-05, MEM-06).
    - 5 net-new memory fixture docs including the A→B→C Spire budget supersede chain (Phase 2 plan 02-07 / MEM-10).
    - `tests/fixtures/malformed-memory/` tree with 5 deliberately-broken fixtures for validator unit tests (Phase 2 plan 02-07 / MEM-10).
    - `docs/tools/audit_log.md` documenting the new `is_memory_sink_write` filter on the audit_log tool (Phase 2 plan 02-06).

    ### Changed
    - `WriteConflict.reason` discriminated union extended (additively, backwards-compatible) with 7 new codes: `missing_provenance`, `invalid_provenance`, `supersede_mismatch`, `agent_write_outside_sink`, `non_agent_write_inside_sink`, `sentinel_missing`, `sink_write_blocked`. Optional envelope fields added: `sinkName`, `key`, `observedValue`, `suggestion` (Phase 2 plan 02-03 / MEM-05, MEM-07, MEM-11).
    - v1 `write_note`, `update_frontmatter`, `delete_note` now refuse memory-sink-resolved targets at the tool entry-point with `sink_write_blocked` (Phase 2 plan 02-03b / MEM-07).
    - `audit_log` tool gains optional `is_memory_sink_write` input filter and adds `is_memory_sink_write: boolean` to each returned row. The MCP `description` text is byte-identical to Phase 1 — the new capability is documented in CHANGELOG and `docs/tools/audit_log.md` only (Phase 2 plan 02-06 / MEM-08).
    - Server bootstrap order: `loadConfig → manager.openAll → registry.registerMemorySinks → catchupVault → server.connect` — sentinels are provisioned before catchup walks the fixture (Phase 2 plan 02-03b).

    ### Documentation
    - ADR-004 amended: underscored PropertyBag keys (`observed_at`, `superseded_by`), confidence enum aligned to `[direct, inferred, uncertain]`, `superseded_reason` field added with cross-field rule (Phase 2 plan 02-01 / MEM-12). [Already in CHANGELOG from plan 02-01 — confirm only.]
    - `docs/v2/MEMORY_CONTRACT.md` aligned with the amended ADR-004.
    ```

    Do NOT add a release tag.
  </action>
  <verify>
    <automated>grep -q "record_observation" CHANGELOG.md && grep -q "memory-sinks" CHANGELOG.md && grep -q "is_memory_sink_write" CHANGELOG.md && grep -q "Phase 2 plan 02-" CHANGELOG.md && grep -q "02-03b" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG `[Unreleased]` section reflects all Phase 2 additions; references concrete plan numbers including 02-03b; release tag untouched.</done>
</task>

<task type="auto">
  <name>Task 6: Update STATE.md — phase position, decisions, blockers</name>
  <files>.planning/STATE.md</files>
  <action>
    Update `.planning/STATE.md` to reflect Phase 2 completion:

    1. **Position pointer**: move from `current_phase: 02-memory-namespace-provenance-contract` to `current_phase: 03-bundles-authority-staleness`.
    2. **Decisions ledger**: append Phase 2's locked decisions D-01..D-04 from `02-CONTEXT.md` as historical record.
    3. **Blockers**: clear any Phase 2 blockers listed.
    4. **Quick Tasks table**: clear any "Phase 2 next steps" rows; add new rows for Phase 3 if appropriate.
    5. **Phase 2 summary entry**: add a one-paragraph summary referencing the 12 MEM requirements + the 2 new Resources + the audit column + the 02-03/02-03b split.
  </action>
  <verify>
    <automated>grep -q "current_phase: 03" .planning/STATE.md || grep -q "Phase 3" .planning/STATE.md</automated>
  </verify>
  <done>STATE.md reflects Phase 2 completion + Phase 3 readiness; historical entries preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 2 — full implementation across plans 02-01..02-07 and 02-03b: ADR-004 amendment + MemorySink runtime + provenance validator + v1 entry-point guards + bootstrap wiring + 3 new MCP tools + 2 new MCP Resources + audit discriminator + 20-doc fixture + 5 malformed test fixtures. Plus this plan's verification.</what-built>
  <how-to-verify>
    Developer review steps:

    1. Open `.planning/phases/02-memory-namespace-provenance-contract/02-08-SUMMARY.md`. Confirm:
       - All 5 ROADMAP Phase 2 success criteria have a real verification command + a "passed" mark.
       - MEM-01..MEM-12 traceability table is complete; MEM-07 and MEM-11 attribute to 02-03b.
       - `npm test`, `npx tsc --noEmit`, `bash scripts/lint-adapters.sh`, `npm run eval:baseline`, smoketest — all marked as exiting 0.

    2. Read `CHANGELOG.md` `[Unreleased]` section. Confirm:
       - All new tools (record_observation, recall, supersede) listed under Added.
       - Both Resources (memory/sinks, memory/stats) listed under Added.
       - WriteConflict union extension listed under Changed.
       - audit_log filter capability listed under Changed (with explicit note that description text is unchanged).
       - ADR-004 amendment listed under Documentation.

    3. Read `.planning/STATE.md`. Confirm Phase 3 position and no Phase 2 blockers.

    4. Spot-check the safety invariant: call `write_note({vault, path: "_memory/observations/forbidden.md", body: "agent should not be able to do this"})`. Expect `{"ok":false,"reason":"sink_write_blocked","sinkName":"default","suggestion":"Use record_observation for sink 'default'."}`.

    5. Spot-check the new tool: call `record_observation({...})`; expect a `{ok:true, doc_id, newHash, created}` response.

    6. Spot-check a Resource: `resources/read({uri: "vault-memory://memory/stats"})`; expect a JSON blob with `total_docs >= 1`.

    7. Type `approved` to seal Phase 2, or list specific issues.
  </how-to-verify>
  <resume-signal>Type "approved" to seal Phase 2, or list specific issues.</resume-signal>
</task>

</tasks>

<verification>
- `npm test` exits 0 with all suites passing.
- `npx tsc --noEmit` exits 0.
- `bash scripts/lint-adapters.sh` reports zero violations.
- `npm run eval:baseline` exits 0.
- `node scripts/smoketest-non-claude.mjs` exits 0 and exercises ≥1 memory tool + ≥1 memory Resource.
- All 5 ROADMAP Phase 2 success criteria have a named verification step that passes.
- All 12 MEM requirements trace to specific plan deliverables — including the 02-03/02-03b split for MEM-05/06/07/11.
- CHANGELOG and STATE updated.
- Developer checkpoint approved.
</verification>

<success_criteria>
- Phase 2 is end-to-end green: every test, every lint, every eval, every smoketest.
- All 5 ROADMAP success criteria provable by named, repeatable verification commands.
- All 12 MEM requirements traced (including the 02-03/02-03b split — MEM-07/MEM-11 land in 02-03b).
- The safety invariant ("agent writes go only to a labeled MemorySink with mandatory provenance, centralized at DeliveryAdapter.write()") is observably true at runtime.
- v1.x backwards compatibility preserved: 324+ v1 tests pass; v1-baseline eval green; the only tools/list snapshot diff is additive (3 new tool entries + audit_log inputSchema gains one optional field); v1 tool description texts are byte-identical.
- Agent-agnosticism preserved.
- Phase 2 is shippable as v2.0.0-alpha or moves cleanly into Phase 3 planning.
</success_criteria>

<output>
Create `.planning/phases/02-memory-namespace-provenance-contract/02-08-SUMMARY.md` when done. The SUMMARY contains:
- ROADMAP success-criteria verification table (5 rows, all PASS).
- MEM-NN traceability table (12 rows, all linked to plan + test; MEM-07/MEM-11 link to 02-03b).
- Test counts.
- Snapshot diff summary (audit_log inputSchema diff only; descriptions byte-identical).
- Smoketest invocation output.
- Developer sign-off marker.
</output>
