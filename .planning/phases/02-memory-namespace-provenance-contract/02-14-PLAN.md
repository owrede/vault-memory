---
phase: 02-memory-namespace-provenance-contract
plan: 14
type: execute
wave: 11
depends_on: [02-08, 02-10]
gap_closure: true
resolves: [WR-05, WR-08]
files_modified:
  - src/adapters/delivery/obsidian-fs/index.ts
  - src/adapters/delivery/obsidian-fs/write.test.ts
  - src/adapters/delivery/obsidian-fs/index.test.ts
autonomous: true
requirements:
  - MEM-05
  - MEM-06
  - MEM-08
must_haves:
  truths:
    - "WR-05: `ObsidianFsDelivery.update(id, patch, opts)` called WITHOUT `opts.expectedHash` returns `{ok:false, reason:'hash_mismatch', message: 'update() requires opts.expectedHash for hashProtected=\"strong\" adapters'}` — mirrors the `delete()` path's existing behavior at line 360"
    - "WR-05: `update(id, patch, {expectedHash: <current-hash>})` continues to work — current behavior preserved"
    - "WR-05: The `hashProtected: 'strong'` capability descriptor at line 97 now reflects reality for both `update()` and `delete()` — no fabricated expectedHash anywhere"
    - "WR-08: `ObsidianFsDelivery.write(id, doc, opts)` stamps the audit row's `is_memory_sink_write` based on `registry.findSinkContaining(id) !== null` — NOT on `opts.sink !== undefined`. Any future bypass that lands in a sink without `opts.sink` still gets correctly flagged"
    - "WR-08: `ObsidianFsDelivery.update(id, patch, opts)` derives the audit flag the same way"
    - "WR-08: `ObsidianFsDelivery.delete(id, opts)` derives the audit flag the same way (this path is normally refused by the sink-write-blocked check, but for any admin/future bypass the flag is truthful)"
    - "WR-08: When `memorySinkRegistry` is undefined (Phase 1 fixture tests), the flag falls back to `false` — preserves Phase 1 behavior for back-compat fixture constructors"
    - "Existing 02-04..02-08 audit-row stamping tests still pass — `record_observation` and `supersede` continue to produce `is_memory_sink_write: true` rows (because the resolved DocId DOES land in a sink, so the new derivation agrees with the old one for those paths)"
    - "Plan 02-10's wave-10 modifications to `src/adapters/delivery/obsidian-fs/index.ts` (sentinel-check wiring) are in place before this plan runs — this plan is wave 11, sequential after 02-10"
  artifacts:
    - path: src/adapters/delivery/obsidian-fs/index.ts
      provides: `update()` refuses with hash_mismatch when expectedHash is missing; all three operations derive `isMemorySinkWrite` from `findSinkContaining(id)`
      contains: "update\\(\\) requires opts\\.expectedHash|findSinkContaining"
    - path: src/adapters/delivery/obsidian-fs/index.test.ts
      provides: at least 5 new tests covering WR-05 (missing expectedHash refused; supplied expectedHash works) and WR-08 (audit flag derived from resolved target across all three ops; no-registry fallback)
      contains: "expectedHash"
  key_links:
    - from: src/adapters/delivery/obsidian-fs/index.ts
      to: registry.findSinkContaining(id)
      via: derivation of isMemorySinkWrite from resolved target rather than caller intent
      pattern: "findSinkContaining"
    - from: src/adapters/delivery/obsidian-fs/index.ts
      to: WR-05 update refusal
      via: parallel check to delete() at line 344
      pattern: "update\\(\\) requires opts\\.expectedHash"
---

<objective>
Close **WR-05** and **WR-08** — two findings on the `ObsidianFsDelivery` facade in `src/adapters/delivery/obsidian-fs/index.ts`:

- **WR-05 (warning):** `update()` silently fabricates `expectedHash` when the caller omits it (lines 295–299: reads current on-disk hash and passes it as `effectiveExpectedHash`). This makes `hashProtected: "strong"` (line 97) a lie for the `update()` path — the OCC contract is silently downgraded to best-effort. `delete()` already enforces this correctly at line 360.
- **WR-08 (warning):** The audit row's `is_memory_sink_write` flag is derived from `opts.sink !== undefined` (line 229, 313, 375). The facade's `preflight` ALREADY resolves the target sink via `registry.findSinkContaining(id)` (used for Guard B and `sink_write_blocked` deletion check). Use that resolved truth for the audit stamp too: a write that lands in a sink without `opts.sink` (legacy `writeNote` bypass, future code paths) still gets correctly flagged.

**Wave-11 coordination note:** This plan was moved from wave 10 to wave 11 to eliminate a parallel-execution file-overlap on `src/adapters/delivery/obsidian-fs/index.ts` with Plan 02-10 (which was bumped to wave 10 — see 02-10's wave-10 coordination note). Plan 02-10 wires `sentinel_check_failed` into `ObsidianFsDelivery.preflight()`; this plan modifies the same file's `write` / `update` / `delete` methods. Running sequentially in wave 11 ensures we apply our edits ON TOP of 02-10's preflight wiring without merge conflicts.

In scope:
1. Refuse `update()` calls without `opts.expectedHash` with `{ok:false, reason:"hash_mismatch", message: "update() requires opts.expectedHash for hashProtected='strong' adapters"}`. Remove the silent `effectiveExpectedHash = opts?.expectedHash ?? existingHash` fallback.
2. Change all three audit-flag derivations in `ObsidianFsDelivery.write/update/delete` from `opts?.sink !== undefined` to `this.memorySinkRegistry?.findSinkContaining(id) !== null` (with `?? false` for the no-registry case so Phase 1 fixture constructors that omit the registry still behave as before).
3. Author or extend `src/adapters/delivery/obsidian-fs/index.test.ts` (create if absent) with the 5 cases listed under must_haves truths.

Out of scope:
- Changes to `src/adapters/delivery/obsidian-fs/write.ts` — the inner `writeNote` already accepts `isMemorySinkWrite` as a passed-through field; the change is in how the FACADE derives the value before passing.
- Changes to `src/adapters/delivery/stub/delivery.ts` — Stub does not stamp audit rows; only obsidian-fs writes audit.
- Changes to the `delete()` path's existing `opts.expectedHash` check (already correct).
- Changes to the `preflight()` sentinel-check wiring (Plan 02-10 owns that — already landed in wave 10 by the time this plan runs).
- Migration of existing audit-row data — the change is forward-only; rows written under Phase 2 with the old `opts.sink`-based flag remain valid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-memory-namespace-provenance-contract/02-REVIEW.md
@docs/v2/adr/004-memory-sink-handles.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-05 — refuse update() without expectedHash</name>
  <files>src/adapters/delivery/obsidian-fs/index.ts, src/adapters/delivery/obsidian-fs/index.test.ts</files>
  <read_first>
    - src/adapters/delivery/obsidian-fs/index.ts (lines 248–316 `update()` body; lines 318–392 `delete()` body for the parallel pattern at line 344; line numbers may have shifted after Plan 02-10 landed its preflight-wiring changes in wave 10 — read the current file)
    - .planning/phases/02-memory-namespace-provenance-contract/02-REVIEW.md (WR-05 detail at lines 187–193)
    - src/adapters/delivery/types.ts (the `WriteOptions.expectedHash?` shape — unchanged here)
  </read_first>
  <behavior>
    - `await delivery.update(docId, {properties: {x: 1}})` (no opts at all) returns `{ok: false, reason: "hash_mismatch", message: /requires opts\\.expectedHash/}`.
    - `await delivery.update(docId, {properties: {x: 1}}, {})` (opts present but no expectedHash) returns the same refusal.
    - `await delivery.update(docId, {properties: {x: 1}}, {expectedHash: <current-hash>})` succeeds (current behavior preserved).
    - The refusal happens AFTER preflight Guards (so a sink-targeted update without expectedHash still gets the Guard A/B path first if applicable). Note: 02-10's `sentinel_check_failed` preflight wiring also runs before this refusal — verify the ordering survives this plan's edit.
    - The refusal happens BEFORE reading the file from disk — symmetric with the `delete()` shape at line 344.
  </behavior>
  <action>
    In `src/adapters/delivery/obsidian-fs/index.ts` `update()` method:

    1. Right after the preflight check (which now includes Plan 02-10's `sentinel_check_failed` branch — confirm the preflight still returns early on its existing failure paths before this new check), BEFORE `safeJoinInsideVault` and `fs.readFile`, add:

    ```ts
    if (opts?.expectedHash === undefined) {
      return {
        ok: false,
        reason: "hash_mismatch",
        message: `update() requires opts.expectedHash for hashProtected="strong" adapters`,
      };
    }
    ```

    2. Remove the `existingHash` computation block (currently lines 295–299 pre-02-10; line numbers may have shifted: `computeNoteHash`, `effectiveExpectedHash = opts?.expectedHash ?? existingHash`). Replace `effectiveExpectedHash` with `opts.expectedHash` directly at the writeNoteInternal call. (The `computeNoteHash` import may then become unused — remove if so.)

    3. Update the TSDoc comment on `update()` to document the new requirement explicitly.

    Add 3 tests in `src/adapters/delivery/obsidian-fs/index.test.ts` (create or extend):

    - "update without opts returns hash_mismatch": create a doc via `delivery.write` (capture its newHash), then call `delivery.update(id, {properties: {x: 1}})` — assert the refusal shape.
    - "update with opts but no expectedHash returns hash_mismatch": same setup, call `delivery.update(id, {properties: {x: 1}}, {})` — assert refusal.
    - "update with correct expectedHash succeeds": same setup, call `delivery.update(id, {properties: {x: 1}}, {expectedHash: newHash})` — assert `.ok === true`.

    Use a tmp vault with `write_enabled: true` and the standard Plan 02-04 test harness.
  </action>
  <verify>
    <automated>npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/index.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "update\\(\\) requires opts\\.expectedHash" src/adapters/delivery/obsidian-fs/index.ts` returns ≥ 1 match.
    - `grep -nE "computeNoteHash" src/adapters/delivery/obsidian-fs/index.ts` returns either zero matches OR only inside comments — the silent fallback computation is gone.
    - `npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/index.test.ts` reports the 3 WR-05 tests passing.
    - Plan 02-10's `sentinel_check_failed` preflight wiring still works after this plan's edits — verify by running 02-10's sentinel-related tests too: `npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/sentinel.test.ts`.
    - `npx tsc --noEmit` clean.
    - `npm test` reports 825+ passing; check that `update_frontmatter` MCP handler (separate path through `src/frontmatter/update.ts`) is unaffected.
  </acceptance_criteria>
  <done>`update()` honors hashProtected:"strong" — callers must read first; the OCC contract is no longer silently downgraded; 02-10's preflight wiring still functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-08 — derive isMemorySinkWrite from findSinkContaining(id) across write/update/delete</name>
  <files>src/adapters/delivery/obsidian-fs/index.ts, src/adapters/delivery/obsidian-fs/index.test.ts</files>
  <read_first>
    - src/adapters/delivery/obsidian-fs/index.ts (line 229 in `write()`; line 313 in `update()`; line 375 in `delete()` — line numbers may have shifted after Plans 02-10 and 02-14 Task 1; read the current file)
    - src/memory/registry.ts (the `findSinkContaining` method at lines 190–202)
    - src/audit/audit.ts and src/db/queries/audit.ts (the `is_memory_sink_write` column flow — for context)
    - .planning/phases/02-memory-namespace-provenance-contract/02-REVIEW.md (WR-08 detail at lines 234–238)
  </read_first>
  <behavior>
    - When `memorySinkRegistry` is provided AND `findSinkContaining(id)` returns a sink, the audit row's `isMemorySinkWrite` is `true` REGARDLESS of whether `opts.sink` was supplied.
    - When `memorySinkRegistry` is provided AND `findSinkContaining(id)` returns null, the audit row's `isMemorySinkWrite` is `false` REGARDLESS of `opts.sink`.
    - When `memorySinkRegistry` is undefined (Phase 1 fixture constructor), the audit row's `isMemorySinkWrite` falls back to `false` — preserves Phase 1 behavior.
    - The change applies symmetrically to `write()`, `update()`, and `delete()`.
    - Existing 02-04 tests for `record_observation` and `supersede` continue to pass.
  </behavior>
  <action>
    **Step 1 — extract a helper method** on the `ObsidianFsDelivery` class:

    ```ts
    /**
     * Derive the `is_memory_sink_write` flag for the audit row.
     *
     * WR-08: This MUST use the resolved truth (`findSinkContaining(id)`),
     * not the caller-intent signal (`opts.sink !== undefined`). The two
     * signals diverge when a write lands inside a sink WITHOUT the caller
     * having routed through the sink-aware path. The audit must reflect
     * what the disk says, not what the caller said.
     *
     * When no registry is configured (Phase 1 fixture constructors), the
     * flag falls back to `false` — preserves back-compat fixture tests.
     */
    private isMemorySinkWriteFor(id: DocId): boolean {
      const sink = this.memorySinkRegistry?.findSinkContaining(id);
      return sink !== null && sink !== undefined;
    }
    ```

    **Step 2 — replace the three call sites:**

    - In `write()`: `isMemorySinkWrite: opts?.sink !== undefined,` → `isMemorySinkWrite: this.isMemorySinkWriteFor(id),`
    - In `update()`: same replacement.
    - In `delete()`: same replacement.

    **Step 3 — update the inline comments** at each call site to reference Plan 02-14 (MEM-08 follow-up, WR-08): "the audit row's `is_memory_sink_write` flag is derived from `registry.findSinkContaining(id)` — the resolved-target truth, not caller intent."

    **Step 4 — extend `src/adapters/delivery/obsidian-fs/index.test.ts`** with:

    - "isMemorySinkWrite is true when DocId resolves into a registered sink, regardless of opts.sink": construct the delivery WITH a registry that has one sink registered. Call `delivery.write(sinkResolvedId, doc, {})` (no `opts.sink`). Assert the audit row's `is_memory_sink_write === true` (read via `vault.db.audit.listWrites()`).
    - "isMemorySinkWrite falls back to false when no registry is configured": construct delivery with `memorySinkRegistry = undefined`. Call `delivery.write(anyId, doc, {})`. Assert audit row's `is_memory_sink_write === false`.

    Capture audit rows via `vault.db.audit.listWrites({})`. Use a doc with `source: "user"` to bypass Guard B for these tests (we're testing audit stamping, not provenance).
  </action>
  <verify>
    <automated>npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/index.test.ts && npx vitest run --no-coverage src/memory/ src/adapters/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "isMemorySinkWriteFor" src/adapters/delivery/obsidian-fs/index.ts` returns ≥ 4 matches (helper decl + 3 call sites).
    - `grep -nE "isMemorySinkWrite: opts\\?\\.sink !== undefined" src/adapters/delivery/obsidian-fs/index.ts` returns **zero matches** (old derivation is gone from all three operations).
    - `npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/index.test.ts` reports ≥ 2 WR-08 tests passing.
    - The 02-04 record_observation + 02-06 audit-row tests still pass: `npx vitest run --no-coverage src/memory/tools/record-observation.test.ts src/memory/resources/`.
    - The 02-10 sentinel preflight wiring still passes: `npx vitest run --no-coverage src/adapters/delivery/obsidian-fs/sentinel.test.ts`.
    - `npx tsc --noEmit` clean.
    - `npm test` reports 825+ passing with zero regressions.
  </acceptance_criteria>
  <done>The audit `is_memory_sink_write` column reflects RESOLVED truth, not CALLER INTENT — any future bypass that lands inside a sink is correctly flagged.</done>
</task>

</tasks>

<verification>
- The `audit_log` MCP tool with `is_memory_sink_write: true` filter continues to return the same rows it did before — the change is invariant on the existing happy path.
- `bash scripts/lint-adapters.sh` reports zero new violations.
- `npm run eval:baseline` reports 30/30 with no regression.
- Plan 02-10's preflight wiring (sentinel_check_failed) is preserved across this plan's edits to the same file.
</verification>

<success_criteria>
- WR-05 closed: `update()` honors `hashProtected: "strong"`.
- WR-08 closed: audit trail reflects resolved target, not caller intent.
- Wave-10 file-overlap with 02-10 eliminated by moving this plan to wave 11 sequential after 02-10.
- MEM-08 invariant strengthened: the discriminator column cannot be silently misreported by a future bypass code path.
</success_criteria>

<output>
Create `.planning/phases/02-memory-namespace-provenance-contract/02-14-SUMMARY.md` when done.
</output>
</content>
</invoke>
