---
phase: 02-memory-namespace-provenance-contract
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/v2/adr/004-memory-sink-handles.md
  - docs/v2/MEMORY_CONTRACT.md
  - CHANGELOG.md
autonomous: true
requirements:
  - MEM-12
must_haves:
  truths:
    - "ADR-004 example YAML uses underscored PropertyBag keys (`observed_at`, `superseded_by`, `superseded_reason`)"
    - "ADR-004 + MEMORY_CONTRACT.md agree on confidence enum: `[direct, inferred, uncertain]`"
    - "`superseded_reason: string` is documented as an optional contract property with the cross-field rule: required (non-empty) when `status === \"superseded\"`"
    - "CHANGELOG `[Unreleased] → ### Documentation` notes the ADR-004 amendment"
  artifacts:
    - path: docs/v2/adr/004-memory-sink-handles.md
      provides: amended contract YAML example, underscored keys, fixed confidence enum, new superseded_reason rule
    - path: docs/v2/MEMORY_CONTRACT.md
      provides: aligned underscored-key spec, confidence enum aligned, superseded_reason cross-field rule
  key_links:
    - from: docs/v2/adr/004-memory-sink-handles.md
      to: docs/v2/MEMORY_CONTRACT.md
      via: cross-reference; identical canonical key names and confidence enum
      pattern: "observed_at|superseded_by|superseded_reason"
---

<objective>
Land the doc-only ADR-004 amendment that resolves the three contract discrepancies discovered in research before any Phase 2 code is written. This keeps the memory contract single-source-of-truth and prevents downstream code from forking on naming.

Three fixes:
1. Adopt underscored PropertyBag keys (`observed_at`, `superseded_by`) — matches MEMORY_CONTRACT.md normative text and the existing fixture. Strip the hyphenated examples in ADR-004 lines 130–148.
2. Adopt the confidence enum `[direct, inferred, uncertain]` from MEMORY_CONTRACT.md. Strip the older `[observed, inferred, user-confirmed]` example.
3. Add `superseded_reason: string` as an optional property with the cross-field rule: MUST be non-empty when `status === "superseded"`.

Purpose: pin the contract before any validator/tool/fixture code reads it.
Output: amended docs + CHANGELOG entry. No code touched.

Risk note: trivial doc edit; no test harness needed. Plan 02 onward depends on the amended spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-memory-namespace-provenance-contract/02-CONTEXT.md
@.planning/phases/02-memory-namespace-provenance-contract/02-RESEARCH.md
@docs/v2/adr/004-memory-sink-handles.md
@docs/v2/MEMORY_CONTRACT.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend ADR-004 §MemoryContract example and Provisioning rules</name>
  <files>docs/v2/adr/004-memory-sink-handles.md</files>
  <action>
    In `docs/v2/adr/004-memory-sink-handles.md` apply three targeted edits to the §MemoryContract section (the YAML example block around lines 130–148):

    1. Rewrite every hyphenated key in the example YAML to underscored form: `observed-at` → `observed_at`, `superseded-by` → `superseded_by`. Any prose that names these keys (lines 119–155 and the §Resolution section) MUST also use the underscored form. Adopting this matches MEMORY_CONTRACT.md normative text and the existing fixture.

    2. In the `confidence` rule, replace `allowed: [observed, inferred, user-confirmed]` with `allowed: [direct, inferred, uncertain]`. Update any prose referencing the old enum (search for `observed,` and `user-confirmed`).

    3. Append a new optional property to the `default-memory-v1` example YAML:
       - `superseded_reason: { type: string }`
       Add a one-paragraph normative rule under the example: "When `status` is set to `superseded`, `superseded_reason` MUST be present and non-empty. Guard A enforces this cross-field invariant under the `supersede_mismatch` reason code."

    Do not change the file's ADR ID, date, or status. Do not touch §Sentinel, §Resolution invariants, or any other section beyond what is required above. Per D-12 / MEM-12, this is a doc-only ratification — adoption is already accepted; this amendment fixes the example to match the operational spec.

    Verify the file ends with a clear "Amended: 2026-05-15 — Phase 2 plan 02-01 (underscored keys, confidence enum, superseded_reason)" footer line (add it under the existing date stamp if absent).
  </action>
  <verify>
    <automated>grep -E "observed-at|superseded-by|user-confirmed|observed,\s*inferred,\s*user-confirmed" docs/v2/adr/004-memory-sink-handles.md | grep -v '^#' | grep -v 'Amended' ; if [ $? -eq 0 ]; then echo "FAIL: stale keys/enum still present" && exit 1; fi; grep -q "superseded_reason" docs/v2/adr/004-memory-sink-handles.md && grep -q "direct, inferred, uncertain" docs/v2/adr/004-memory-sink-handles.md && grep -q "Amended:.*Phase 2" docs/v2/adr/004-memory-sink-handles.md</automated>
  </verify>
  <done>ADR-004 contains zero hyphenated PropertyBag keys (excluding markdown headings), confidence enum is `[direct, inferred, uncertain]`, `superseded_reason` documented as an optional property with the cross-field rule, footer notes the Phase 2 amendment.</done>
</task>

<task type="auto">
  <name>Task 2: Align MEMORY_CONTRACT.md and CHANGELOG with the amendment</name>
  <files>docs/v2/MEMORY_CONTRACT.md, CHANGELOG.md</files>
  <action>
    In `docs/v2/MEMORY_CONTRACT.md`:
    1. Confirm the underscored keys are already canonical (search for hyphenated variants and fix any stragglers).
    2. Confirm the confidence enum already shows `[direct, inferred, uncertain]` (it should — fix if not).
    3. Add a normative subsection under §Required properties OR §Cross-field invariants (whichever fits the doc structure) describing the `superseded_reason` field exactly as in Task 1: "Optional; required (non-empty) when `status === \"superseded\"`. Guard A enforces this under reason code `supersede_mismatch`."

    In `CHANGELOG.md` under `[Unreleased] → ### Documentation` (create the subsection if absent), add a single line:
    `- ADR-004 amended: underscored PropertyBag keys, confidence enum aligned to [direct, inferred, uncertain], superseded_reason field added (Phase 2 plan 02-01).`

    Do not edit any other CHANGELOG section. Do not touch sign-off or release tags.
  </action>
  <verify>
    <automated>grep -E "observed-at|superseded-by|user-confirmed" docs/v2/MEMORY_CONTRACT.md | grep -v '^#' ; if [ $? -eq 0 ]; then echo "FAIL: MEMORY_CONTRACT.md has stale keys/enum" && exit 1; fi; grep -q "superseded_reason" docs/v2/MEMORY_CONTRACT.md && grep -q "ADR-004 amended" CHANGELOG.md</automated>
  </verify>
  <done>MEMORY_CONTRACT.md is byte-aligned with the amended ADR-004 on key naming, confidence enum, and the superseded_reason rule. CHANGELOG carries a one-line Documentation entry.</done>
</task>

</tasks>

<verification>
- `grep -E "observed-at|superseded-by|user-confirmed" docs/v2/` returns zero matches outside markdown comment lines or quoted historical references.
- `grep -q "direct, inferred, uncertain" docs/v2/adr/004-memory-sink-handles.md` succeeds.
- `grep -q "superseded_reason" docs/v2/adr/004-memory-sink-handles.md docs/v2/MEMORY_CONTRACT.md` succeeds.
- `npm test` is unaffected (this plan does not touch test code).
- The existing v1-baseline eval suite passes unchanged (no code touched).
</verification>

<success_criteria>
- ADR-004 §MemoryContract YAML example uses only underscored keys.
- ADR-004 confidence enum reads `[direct, inferred, uncertain]`.
- ADR-004 documents `superseded_reason: string` as optional with the `status === "superseded"` cross-field rule.
- MEMORY_CONTRACT.md prose matches ADR-004 on all three points.
- CHANGELOG `[Unreleased] → ### Documentation` lists the amendment.
- Roadmap Phase 2 success criterion 5 ("ADR-004 amendment committed before implementation") is satisfied.
- MEM-12 ratification recorded.
</success_criteria>

<output>
Create `.planning/phases/02-memory-namespace-provenance-contract/02-01-SUMMARY.md` when done. Note: this plan is doc-only — no code, no tests.
</output>
