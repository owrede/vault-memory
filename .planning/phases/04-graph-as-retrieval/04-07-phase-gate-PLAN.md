---
phase: 04-graph-as-retrieval
plan: 07
type: execute
wave: 6
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
  - 04-06
files_modified:
  - evals/v1-baseline/tools-list.snapshot.json
  - CHANGELOG.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - docs/v2/PHASE-4-SIGN-OFF.md
autonomous: false
requirements:
  - GRA-01
  - GRA-02
  - GRA-03
  - GRA-04
  - GRA-05
user_setup: []

must_haves:
  truths:
    - "Tool-list snapshot is regenerated EXACTLY ONCE with the additive diff: 2 new tools (`expand`, `cluster`) + 1 additive nested param on `search_hybrid` (`expand?: {...}`) + 1 additive output field on `list_backlinks` and `list_forward_links` results (`type: EdgeType`)."
    - "Diff review confirms only additive changes — no v1 fields removed, no required-fields-becoming-optional regressions."
    - "Full test suite, lint, adapter-lint, fixture-privacy lint, telemetry-lint, and full eval suite (v1-baseline + expand.yaml + search-hybrid-with-expand.yaml + cluster.yaml + conformance) all green on a clean checkout."
    - "`CHANGELOG.md` updated under `[Unreleased]` with every user-visible Phase 4 change."
    - "`docs/v2/PHASE-4-SIGN-OFF.md` exists with GRA-01..GRA-05 checklist + resolving commit SHAs (or plan-id refs)."
    - "`.planning/STATE.md` and `.planning/ROADMAP.md` reflect Phase 4 = COMPLETE."
  artifacts:
    - path: "docs/v2/PHASE-4-SIGN-OFF.md"
      provides: "GRA-01..GRA-05 checklist + per-plan summary + resolving commits"
      contains: "GRA-01"
    - path: "evals/v1-baseline/tools-list.snapshot.json"
      provides: "Updated snapshot with 2 new tools + additive diffs; 23 v1 entries byte-identical"
      contains: "expand"
    - path: "CHANGELOG.md"
      provides: "[Unreleased] section enumerates Phase 4 deliverables"
      contains: "Phase 4"
  key_links:
    - from: "docs/v2/PHASE-4-SIGN-OFF.md"
      to: ".planning/REQUIREMENTS.md"
      via: "GRA-01..GRA-05 traceability"
      pattern: "GRA-0[12345]"
    - from: "evals/v1-baseline/tools-list.snapshot.json"
      to: "scripts/dump-tools.mjs (Phase 0 / FND-10)"
      via: "regen via `npm run eval:snapshot`"
      pattern: "expand"
---

<objective>
Wave 6 — Phase 4 sign-off. Regenerate the tool-list snapshot with the full additive diff, confirm zero breaking changes, run the full eval + conformance suite, update CHANGELOG + STATE + ROADMAP, and produce the phase sign-off document. This is the only plan in Phase 4 that touches the tool-list snapshot — concentrating all additive diff review in one PR per RESEARCH §"Strictly additive search_hybrid changes" + PATTERNS line 38.

Purpose: GRA-01..GRA-05 traceability + phase gate. Mirrors Phase 3 sign-off shape (`docs/v2/PHASE-3-SIGN-OFF.md`).

Output: Snapshot regen + sign-off doc + CHANGELOG/STATE/ROADMAP updates. Includes a checkpoint for maintainer review of the additive diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-graph-as-retrieval/04-CONTEXT.md
@.planning/phases/04-graph-as-retrieval/04-RESEARCH.md
@.planning/phases/04-graph-as-retrieval/04-01-edges-substrate-PLAN.md
@.planning/phases/04-graph-as-retrieval/04-02-edge-extractors-PLAN.md
@.planning/phases/04-graph-as-retrieval/04-03-expand-tool-PLAN.md
@.planning/phases/04-graph-as-retrieval/04-04-search-hybrid-expand-PLAN.md
@.planning/phases/04-graph-as-retrieval/04-05-cluster-tool-PLAN.md
@.planning/phases/04-graph-as-retrieval/04-06-evals-conformance-PLAN.md
@docs/v2/PHASE-3-SIGN-OFF.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@evals/v1-baseline/tools-list.snapshot.json
@CHANGELOG.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Regenerate tool-list snapshot with the full additive diff</name>
  <files>evals/v1-baseline/tools-list.snapshot.json</files>
  <behavior>
    - The regenerated snapshot contains:
      - All 30 Phase 3 tools BYTE-IDENTICAL (23 v1 + 7 Phase 3 entries — verify via per-entry diff that no Phase 3 entry changed).
      - 2 NEW entries: `expand`, `cluster` (with the JSON Schemas from Plans 04-03 / 04-05).
      - 1 nested property added to `search_hybrid.inputSchema.properties.expand` (Plan 04-04).
      - The tool-list output is generated by `scripts/dump-tools.mjs` (Phase 0 / FND-10); regen via `npm run eval:snapshot`.
    - No v1 tool entry has fields removed, defaults changed, or required-list shrunk.
    - The diff is reviewable in a single PR.
  </behavior>
  <action>
    1. Run `npm run eval:snapshot` to regenerate `evals/v1-baseline/tools-list.snapshot.json`. (Per Phase 0 / FND-10, this script invokes `scripts/dump-tools.mjs` which connects to the MCP server via stdio, calls `tools/list`, and writes JSON.)
    2. `git diff evals/v1-baseline/tools-list.snapshot.json` to review. Expected diff:
       - 2 new top-level entries: `{name: "expand", ...}` and `{name: "cluster", ...}`.
       - 1 nested addition under `search_hybrid.inputSchema.properties`: `expand: {type: "object", properties: {hops: ..., direction: ..., edge_types: ...}}`.
       - Zero diffs in the 23 v1 entries and the 7 Phase 3 entries.
    3. If `list_backlinks` / `list_forward_links` input schemas DO show diffs: this should NOT happen — Plan 04-01 only added the additive `type` field to RESULT rows (output), not to the input schema. The tool-list snapshot pins the INPUT schema only (per existing `scripts/dump-tools.mjs` behavior). If you see input-schema diffs on these tools, investigate before proceeding.
    4. Commit the snapshot diff under a clear commit subject.

    The maintainer reviews the diff in the checkpoint below (Task 2).
  </action>
  <verify>
    <automated>npm run eval:snapshot && git diff --exit-code evals/v1-baseline/tools-list.snapshot.json || echo "Diff present — proceed to checkpoint"</automated>
  </verify>
  <done>Snapshot regenerated; diff matches the additive-only expectation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tool-list snapshot regen (Task 1) — additive diff covering 2 new tools + 1 nested param on `search_hybrid`.</what-built>
  <how-to-verify>
    1. Run `git diff evals/v1-baseline/tools-list.snapshot.json` and confirm:
       - 2 new top-level array entries: `expand`, `cluster` with the locked schemas (hops literal union, edge_types enum, etc.).
       - 1 nested addition: `search_hybrid.inputSchema.properties.expand`.
       - ZERO diffs in the 23 v1 entries (these byte-identical hash was last regenerated for Phase 3; the v1 baseline is the floor).
       - ZERO diffs in the 7 Phase 3 entries (`get_outline`, `search_sections`, `get_document_bundle`, `assemble_dossier`, plus search_hybrid Phase 3 params, plus the 2 promoted MCP Resources if pinned).
    2. Run `npm run eval:baseline` — must pass.
    3. Run `npm test` — full suite green.
    4. Inspect the descriptions of `expand` and `cluster` (in the tool-list JSON): they should mention `_memory` opacity (expand), hop cap=2 (expand), Louvain (cluster), 5000-node hard cap (cluster), and the cluster_id rule (smallest member DocId).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to sign-off; "revise" + reason to halt.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full eval + conformance + lint phase gate</name>
  <files>(no source changes — pure verification)</files>
  <behavior>
    - Every gate command passes on a clean checkout.
  </behavior>
  <action>
    Run the full Phase 4 gate sequence and capture results for the sign-off doc (Task 4):
    ```bash
    npm run lint               # tsc --noEmit
    npm test                   # full vitest suite (1076+ Phase 3 tests + new Phase 4 tests)
    npm run eval:baseline      # evals/v1-baseline/baseline.test.ts
    npm run eval:snapshot      # already done in Task 1; re-run to confirm idempotency
    npx vitest run src/graph/expand.integration.test.ts
    npx vitest run src/graph/cluster.integration.test.ts
    npx vitest run src/search/hybrid-expand.integration.test.ts
    npx vitest run src/adapters/source/conformance.test.ts
    bash scripts/lint-adapters.sh        # ADP-12: zero hits outside adapters
    bash scripts/check-fixture-privacy.sh # FND-11
    bash scripts/lint-no-telemetry.sh    # FND-12
    npm run build && node -e "import('./dist/cli.js').then(() => console.log('ok'))"  # bundle smoke (graphology family)
    ```
    Record pass/fail per command + counts (e.g., "1076 → 1100+ tests pass; 23 v1 + 7 Phase 3 + 5 Phase 4 = 35 tool entries; 5 expand queries P/R all ≥ 0.8") for the sign-off doc.

    If any command fails, STOP — return to the relevant plan's revisions, do not advance to Task 4.
  </action>
  <verify>
    <automated>npm run lint && npm test && npm run eval:baseline && bash scripts/lint-adapters.sh && bash scripts/check-fixture-privacy.sh && bash scripts/lint-no-telemetry.sh</automated>
  </verify>
  <done>All gate commands exit 0; results captured for sign-off.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Author docs/v2/PHASE-4-SIGN-OFF.md + update CHANGELOG + STATE + ROADMAP</name>
  <files>docs/v2/PHASE-4-SIGN-OFF.md, CHANGELOG.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <behavior>
    - `docs/v2/PHASE-4-SIGN-OFF.md` exists, structured per `docs/v2/PHASE-3-SIGN-OFF.md`.
    - `CHANGELOG.md` `[Unreleased]` section has Phase 4 entries under appropriate subsections.
    - `.planning/STATE.md` has `status: phase_complete`, `stopped_at: Phase 4 complete`, and updated progress count.
    - `.planning/ROADMAP.md` Phase 4 entry is marked `[x]` complete; per-plan `[x]` checkboxes filled.
  </behavior>
  <action>
    1. **`docs/v2/PHASE-4-SIGN-OFF.md`** — mirror `docs/v2/PHASE-3-SIGN-OFF.md` structure:
       - Header: phase name, sign-off date, plan count, test count delta.
       - **GRA-01..GRA-05 traceability table**: for each requirement, list resolving plan IDs and key commit subjects.
       - **Per-plan summary**: brief recap of 04-01..04-06 outcomes.
       - **Test count delta**: before Phase 4 (1076 tests) → after Phase 4 (e.g., 1100+).
       - **Tool-list delta**: 30 → 32 (or whatever the final count is), all additive.
       - **Eval results**: ≥5 expand queries with P/R ≥ 0.8, ~3 search-hybrid-with-expand queries, cluster.yaml snapshot pinned.
       - **Conformance**: cross-adapter (obsidian-fs + stub) cases green for expand + cluster.
       - **Assumption outcomes**:
         - A1 (MIN_MENTION_LEN=4 FP rate): record the empirical result from Plan 04-06 Task 1.
         - A3 (cluster determinism across Node minors): record CI Node version + statement that cluster.yaml snapshot guards drift.
         - A4 (`randomWalk: true + rng` covers all Louvain entropy): record library-source read findings.
       - **Open questions resolved** + **deferred to v2.x**: copy from RESEARCH §Open Questions.
       - **Known limitations**: single-vault expand only; `embed` edge type still deferred; no MCP Resources promotion for expand/cluster.

    2. **`CHANGELOG.md`** — under `[Unreleased]` add:
       ```markdown
       ### Added
       - `expand` MCP tool — typed-edge BFS retrieval with citation packets + `via` provenance (hop, edge type, direction, seed). Hops 1–2. Memory namespace opaque per ADR-004.
       - `cluster` MCP tool — Louvain community detection over the typed-edge graph, deterministic across runs. 5000-node hard cap with `force:true` override.
       - `search_hybrid` accepts optional nested `expand?: {hops, direction?, edge_types?}` — auto-attaches per-hit expansions; original ranking preserved.
       - Typed-edge storage substrate: `edges` table with 4 edge types (`wikilink`, `mention`, `frontmatter-ref`, `hyperlink`). Indexer extracts all four types in a single per-note parse pass.
       - `list_backlinks` / `list_forward_links` / bundle/dossier linked entries gain additive `type: EdgeType` field on result rows.
       - 3 new eval YAMLs: `_queries/expand.yaml` (≥5 manual gold-set queries), `_queries/search-hybrid-with-expand.yaml`, `_queries/cluster.yaml` (D-12 determinism snapshot).

       ### Dependencies
       - Added: `graphology ^0.26.0`, `graphology-communities-louvain ^2.0.2`, `seedrandom ^3.0.5`, `@types/seedrandom` (dev). All MIT, all pure-JS ESM, all manually provenance-verified per RESEARCH §Package Legitimacy Audit.

       ### Internal
       - Migration 011: `edges` table + chunked backfill from `wikilinks` (10k-row chunks; idempotent INSERT OR IGNORE).
       - `wikilinks` table preserved for v1 invariance per D-01; v3 cleanup tracked.
       ```

    3. **`.planning/STATE.md`** — update front matter:
       ```yaml
       status: phase_complete
       stopped_at: Phase 4 complete
       last_updated: "<YYYY-MM-DD>T<HH:MM:SS>Z"
       last_activity: <YYYY-MM-DD>
       progress:
         total_phases: 10
         completed_phases: 5
         total_plans: 44
         completed_plans: 51   # 44 prior + 7 Phase 4
         percent: 50
       ```
       Append a Phase 4 entry under "Recent decisions affecting current work" / Accumulated Context.

    4. **`.planning/ROADMAP.md`** — mark Phase 4 complete:
       - Top-line: `- [x] **Phase 4: Graph-as-retrieval** - ...`
       - Section header: `### Phase 4: Graph-as-retrieval — COMPLETE (<YYYY-MM-DD>)`
       - Each plan `- [x] 04-01-edges-substrate-PLAN.md — ...` through `- [x] 04-07-phase-gate-PLAN.md — ...` with one-line recap each.
       - Progress table row for Phase 4: `7/7 | Complete | <YYYY-MM-DD>`.
       - Per-success-criterion lines marked MET with proof (e.g., "MET (`expand.yaml` ships 5+ queries, P/R ≥ 0.8 on all)").

    Commit messages: one atomic commit for the sign-off (`docs(04): Phase 4 sign-off + CHANGELOG + STATE + ROADMAP`) per Phase 3 precedent.
  </action>
  <verify>
    <automated>test -f docs/v2/PHASE-4-SIGN-OFF.md && grep -q "GRA-01" docs/v2/PHASE-4-SIGN-OFF.md && grep -q "Phase 4" CHANGELOG.md && grep -q "phase_complete" .planning/STATE.md</automated>
  </verify>
  <done>Sign-off doc + CHANGELOG + STATE + ROADMAP all reflect Phase 4 COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool-list snapshot regen | Snapshot is generated locally by `scripts/dump-tools.mjs`; reviewed by maintainer in the checkpoint. |
| Sign-off doc | Authored locally; no external inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Integrity | Accidental v1-entry change leaks into snapshot regen | mitigate | Maintainer-blocking checkpoint reviews the diff; per-entry byte-identity is verifiable via `jq` slicing. |
| T-04-07-02 | Integrity | CHANGELOG misses a user-visible change | mitigate | Checklist in sign-off doc cross-references per-plan SUMMARYs. |
| T-04-07-03 | Integrity | STATE/ROADMAP not reflecting completion | mitigate | Task 4 verify step greps for `phase_complete` and `Phase 4`. |
| T-04-07-SC | Tampering | npm install | N/A | No new deps. |
</threat_model>

<verification>
**Acceptance:**
- Task 1 snapshot regen passes the additive-only diff inspection.
- Task 2 checkpoint approved by maintainer.
- Task 3 full gate sequence exits 0 on every command.
- Task 4 produces sign-off doc + CHANGELOG/STATE/ROADMAP updates; the grep-based verify gate green.

**Eval queries:** all pinned in Plan 04-06; this plan only re-runs them as part of the gate.

**Snapshot checks:** the tool-list snapshot regen IS the snapshot check for this plan.
</verification>

<validation>
**Nyquist Dimension 8:**
- **Coverage map:**
  - GRA-01..GRA-05 traceability → `docs/v2/PHASE-4-SIGN-OFF.md` checklist with resolving plan IDs/commits.
  - Tool-list additive-only invariance → manual diff review at Task 2 checkpoint.
  - Full eval suite + conformance → Task 3 gate sequence.
- **Sampling:** phase gate is full-suite; no per-task narrow vitest needed beyond Task 3.
</validation>

<success_criteria>
1. Tool-list snapshot regenerated with the additive diff (2 new tools + `search_hybrid.expand`); 23 v1 + 7 Phase 3 entries byte-identical.
2. Maintainer-blocking checkpoint approved.
3. All gate commands pass: `lint`, `test`, `eval:baseline`, `eval:snapshot`, three integration tests, conformance test, `lint-adapters`, `check-fixture-privacy`, `lint-no-telemetry`, build smoke.
4. `docs/v2/PHASE-4-SIGN-OFF.md` exists with GRA-01..GRA-05 checklist + assumption outcomes (A1, A3, A4).
5. `CHANGELOG.md` `[Unreleased]` covers every user-visible Phase 4 change + the 3 new deps.
6. `.planning/STATE.md` and `.planning/ROADMAP.md` reflect Phase 4 COMPLETE.
</success_criteria>

<commit>
Atomic commit message (Tasks 1–4 produce one or two commits; the sign-off itself is one commit):

```
docs(04): Phase 4 sign-off — Graph-as-retrieval COMPLETE

- evals/v1-baseline/tools-list.snapshot.json: additive diff — 2 new tools
  (expand, cluster) + 1 nested search_hybrid.expand param. 23 v1 + 7
  Phase 3 entries byte-identical.
- docs/v2/PHASE-4-SIGN-OFF.md: GRA-01..GRA-05 traceability, per-plan
  recap, assumption outcomes (A1 MIN_MENTION_LEN result, A3 Node-version
  pin, A4 Louvain entropy survey), test count delta.
- CHANGELOG.md [Unreleased]: full Phase 4 deliverable list + 3 new deps.
- .planning/STATE.md + .planning/ROADMAP.md: Phase 4 marked complete.

Refs: GRA-01, GRA-02, GRA-03, GRA-04, GRA-05
```
</commit>

<output>
Create `.planning/phases/04-graph-as-retrieval/04-07-phase-gate-SUMMARY.md` when done, mirroring the Phase 3 sign-off SUMMARY pattern.
</output>
