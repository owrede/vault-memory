---
phase: 01
plan: 01
plan_id: 01-01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/adapters/source/types.ts
  - src/adapters/delivery/types.ts
  - src/adapters/change-feed/types.ts
  - src/adapters/capabilities.ts
  - src/adapters/registry.ts
  - tests/types/docid-brand.test-d.ts
  - src/adapters/source/index.ts
  - src/adapters/delivery/index.ts
  - src/adapters/change-feed/index.ts
  - src/adapters/registry.test.ts
autonomous: true
requirements: [ADP-04, ADP-05, ADP-06]
status: draft
tags: [adapters, types, branded-types, registry]
must_haves:
  truths:
    - "Branded DocId rejects raw string assignment at compile time"
    - "SourceConnector / DeliveryAdapter / ChangeFeed interface declarations exist and compile"
    - "parseDocId is the only exported DocId constructor; mintDocId is module-private"
    - "Canonical types (Document, BlockNode, Edge, ChangeEvent, SourceHandle, MemorySink) live in src/types.ts and tsc --noEmit passes"
  artifacts:
    - path: "src/types.ts"
      provides: "Document, BlockNode, Edge, ChangeEvent, SourceHandle, MemorySink, DocId branded type, WikilinkRef"
      contains: "type DocId = string & { readonly __brand:"
    - path: "src/adapters/source/types.ts"
      provides: "SourceConnector, SourceCapabilities, ListOptions, DocumentRef interfaces"
      exports: ["SourceConnector", "SourceCapabilities", "ListOptions", "DocumentRef"]
    - path: "src/adapters/delivery/types.ts"
      provides: "DeliveryAdapter, DeliveryCapabilities, WriteOptions, WriteResult discriminated union"
      exports: ["DeliveryAdapter", "DeliveryCapabilities", "WriteOptions", "WriteResult", "WriteSuccess", "WriteConflict"]
    - path: "src/adapters/change-feed/types.ts"
      provides: "ChangeFeed interface + ChangeEvent tagged union"
      exports: ["ChangeFeed", "ChangeEvent"]
    - path: "src/adapters/capabilities.ts"
      provides: "Shared capability sub-types (EdgeType, RefHashKind, WatchKind, BodyShape, PropertiesShape, HashProtected)"
    - path: "src/adapters/registry.ts"
      provides: "parseDocId(s) -> DocId, formatDocId(scheme, authority, resource) -> DocId, AdapterRegistry class with registerSource/resolveSource/registerDelivery/resolveDelivery/registerChangeFeed/resolveChangeFeed"
    - path: "tests/types/docid-brand.test-d.ts"
      provides: "Compile-time negative test asserting raw string is not assignable to DocId"
      contains: "@ts-expect-error"
  key_links:
    - from: "src/adapters/registry.ts"
      to: "src/types.ts (DocId)"
      via: "import type { DocId } from \"../types.js\""
      pattern: "import type \\{ .*DocId.* \\} from"
    - from: "src/adapters/source/types.ts"
      to: "src/types.ts (Document, DocId)"
      via: "import type ... from \"../../types.js\""
      pattern: "from \"\\.\\./\\.\\./types\\.js\""
    - from: "tests/types/docid-brand.test-d.ts"
      to: "src/adapters/registry.ts (parseDocId)"
      via: "import { parseDocId }"
      pattern: "parseDocId"
---

<objective>
Establish the canonical type surface and adapter directory bootstrap for Phase 1.
This plan is the foundation that ALL subsequent plans (01-02..06) import from.
Lands as the first plan of the phase, on its own wave (no dependencies).

Purpose:
- Add the v2 canonical types (Document, BlockNode, Edge, ChangeEvent, SourceHandle,
  MemorySink, branded DocId, WikilinkRef) to src/types.ts so downstream adapter code
  and the doc_uri migration (01-02) can reference them.
- Declare the three seam interfaces (SourceConnector, DeliveryAdapter, ChangeFeed)
  in their dedicated directories per ADR-002 §"Decision" §"Recommended Project Structure".
- Ship the adapter registry (`src/adapters/registry.ts`) as the SOLE minting point
  for branded DocIds, enforcing ADP-05's compile-time identity contract.
- Prove the branded-DocId guard at the TypeScript level via a negative test file
  consumed by `tsc --noEmit` (no new devDep — option A from 01-PATTERNS.md).

Output: 9 new files + 1 modified file. No runtime behavior change. `tsc --noEmit`
passes. All 324 v1 tests remain green (no production code touched yet).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-adapter-extraction-tech-debt-up/01-CONTEXT.md
@.planning/phases/01-adapter-extraction-tech-debt-up/01-RESEARCH.md
@.planning/phases/01-adapter-extraction-tech-debt-up/01-PATTERNS.md
@.planning/phases/01-adapter-extraction-tech-debt-up/01-VALIDATION.md

@docs/v2/adr/002-adapter-seams.md
@docs/v2/adr/001-document-identity.md
@docs/v2/adr/003-document-shape.md
@docs/v2/adr/004-memory-sink-handles.md

@src/types.ts
@src/rerank/reranker.ts
@src/vault/manager.ts
@src/write/write.ts

<interfaces>
<!-- Key existing patterns this plan extends. From 01-PATTERNS.md analog mapping. -->

From src/rerank/reranker.ts:39 — the canonical "interface + multiple impls" template:
```typescript
export interface Reranker {
  /** Score each chunk… Returns one score per chunk… Higher = more relevant. */
  score(query: string, chunks: readonly string[]): Promise<number[]>;
}
```
Copy this style for SourceConnector / DeliveryAdapter / ChangeFeed: brief, single-purpose
interface with TSDoc on each method, no class concerns. Header block 10-35 lines per S-1.

From src/write/write.ts:19-35 — the discriminated-union result pattern:
```typescript
export interface WriteSuccess { ok: true; newHash: string; noteId: number; created: boolean }
export interface WriteConflict { ok: false; reason: "hash_mismatch" | "permission_denied"; … }
export type WriteResult = WriteSuccess | WriteConflict
```
Re-use VERBATIM in src/adapters/delivery/types.ts so the obsidian-fs delivery impl
in plan 01-04 can return existing-style values unchanged.

From src/vault/manager.ts — the per-handle resolver pattern:
```typescript
class VaultManager {
  private vaults: Map<string, Vault> = new Map();
  register(name: string, vault: Vault): void { … }
  require(name: string): Vault { … }  // throws on miss
  list(): string[] { … }
}
```
Mirror this for AdapterRegistry (registerSource/resolveSource/list).

From RESEARCH §Pattern 2 (lines 336-352) — branded DocId IIFE template:
```typescript
const { mintDocId, parseDocId } = (() => {
  const mint = (s: string): DocId => s as DocId;
  const parse = (s: string): DocId => {
    if (!/^[a-z][a-z0-9-]*:\/\/[^/]+\/.+$/.test(s)) throw new Error(`Invalid DocId: ${s}`);
    return mint(s);
  };
  return { mintDocId: mint, parseDocId: parse };
})();
export { parseDocId };  // mintDocId stays module-private
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 01-01-01: Append canonical types + branded DocId to src/types.ts</name>
  <files>src/types.ts</files>
  <behavior>
    - Append a section-divider comment block ("// ──── v2 canonical types (Phase 1) ────") at the end of src/types.ts before any existing trailing exports.
    - Add `export type DocId = string & { readonly __brand: "DocId" };` per ADP-05 (RESEARCH §Pattern 2 lines 336-352; PATTERNS row "src/types.ts" line 78).
    - Add `export type SourceHandle = string & { readonly __brand: "SourceHandle" };` — same nominal-brand pattern as DocId. Used by Document.source and the adapter registry.
    - Add `export type MemorySinkHandle = string & { readonly __brand: "MemorySinkHandle" };` — Phase 2 will populate; declare the type now per ADR-004 so plan 01-04 delivery seam can reference it without circular-import. PATTERNS confirms src/types.ts is the canonical home.
    - Add `interface Document` per ADR-003 §"Decision": { id: DocId; source: SourceHandle; title: string; blocks: BlockNode[]; properties: Record<string, unknown>; links: Edge[]; mtime: number; hash: string; display_url?: string | null }. Use `interface` (matches ParsedNote at lines 47-84). TSDoc on every field per S-1.
    - Add `type BlockNode = { kind: "paragraph"; text: string } | { kind: "heading"; level: 1|2|3|4|5|6; text: string } | { kind: "code"; lang?: string; text: string } | { kind: "list"; ordered: boolean; items: string[] }` (Phase 1 minimal shape per ADR-003; richer block types are Phase 3 work).
    - Add `interface Edge { type: "wikilink" | "mention" | "frontmatter-ref" | "hyperlink"; target: DocId | string; rel?: string }` (typed shape per ADR-003; Phase 1 leaves this empty in practice — D-04 defers wikilinks-as-edges to Phase 4, but the TYPE must compile now).
    - Add `interface WikilinkRef { target: string; alias?: string; section?: string }` — the existing shape used by `src/reader/wikilinks.ts` (read that file first to confirm shape). Per D-05, the obsidian-fs source adapter will populate `Document.properties.wikilinks: WikilinkRef[]` (an `unknown`-typed PropertyBag entry — but the interface lives here for shared use).
    - Add `type ChangeEvent = { kind: "create"; id: DocId; at: number } | { kind: "update"; id: DocId; at: number } | { kind: "delete"; id: DocId; at: number } | { kind: "rename"; old_id: DocId; new_id: DocId; at: number };` per ADR-002 lines 171-176 VERBATIM. Plan 01-05's obsidian-fs change-feed will only emit create/update/delete (not rename) per RESEARCH A3 / Risk #3; the type permits rename for future adapters.
    - Add `interface MemorySink { handle: MemorySinkHandle; resolveTo: DocId }` — minimal Phase 1 shape; Phase 2 (MEM-01..12) will expand. Declared here so 01-04 delivery seam compiles against the shape.
    - TSDoc on every field/member (matches existing ParsedNote lines 51-71 style).
    - Test file: extend src/types.test.ts (create if absent) with a type-only structural assertion that Document.id is a DocId (no runtime test needed — covered by tests/types/docid-brand.test-d.ts in task 04).
    - Do NOT modify any existing types in src/types.ts (additions only).
  </behavior>
  <action>Append the v2 canonical types section to src/types.ts. Use `interface` for records, `type` only for aliases / unions, matching existing style (ParsedNote=interface at 47-84, WriteResult=type at write.ts:34). Place all new exports under a single divider comment "// ──── v2 canonical types (Phase 1, ADR-002/003/004) ────". Each field gets a TSDoc comment. Per S-2, use `.js` extensions on any relative imports added (none needed for this file). No production code consumes these types yet — purpose is to publish the type surface so 01-02..06 can import.</action>
  <verify>
    <automated>npm run lint && npm test -- --run src/types</automated>
  </verify>
  <done>
    - All new types compile under tsc --noEmit (npm run lint passes).
    - grep -E '^export (type|interface) (DocId|SourceHandle|MemorySinkHandle|Document|BlockNode|Edge|WikilinkRef|ChangeEvent|MemorySink)' src/types.ts returns 10 matches.
    - npm test passes (no behavior change; only type additions).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-02: Create src/adapters/capabilities.ts (shared capability sub-types)</name>
  <files>src/adapters/capabilities.ts</files>
  <behavior>
    - File MUST be < 60 lines per PATTERNS row "src/adapters/capabilities.ts" (small type-only barrel).
    - Export the shared sub-types referenced from BOTH source and delivery types so they don't have to be duplicated:
      - `type BodyShape = "flat-text" | "blocks" | "html-fragments"` (ADR-002 §SourceCapabilities)
      - `type PropertiesShape = "untyped" | "typed-schema-bound"` (ADR-002 §SourceCapabilities)
      - `type EdgeType = "wikilink" | "embed" | "mention" | "frontmatter-ref" | "hyperlink"` (ADR-003)
      - `type RefHashKind = "content" | "remote-token" | "none"` (ADR-002 §DocumentRef.hash + Adversarial Finding 7)
      - `type WatchKind = "push" | "poll" | "none"` (ADR-002 §SourceCapabilities)
      - `type HashProtected = "strong" | "best-effort" | "none"` (ADR-002 §DeliveryCapabilities + Adversarial Finding 10)
      - `type NamingMode = "caller-provided" | "adapter-derived" | "remote-assigned"` (ADR-002 §DeliveryCapabilities)
    - All exports are `type` aliases (no `interface` — these are union literals).
    - No runtime code; pure type module.
    - File header TSDoc block (10-15 lines) per S-1 explaining: "shared capability sub-types referenced by both src/adapters/source/types.ts and src/adapters/delivery/types.ts; consolidated here to avoid duplication. ADR-002 §Capability Descriptors is the source of truth."
  </behavior>
  <action>Create the file with the seven exported `type` aliases above and a header block. Verify line count stays under 60.</action>
  <verify>
    <automated>npm run lint && test "$(wc -l < src/adapters/capabilities.ts)" -lt 60</automated>
  </verify>
  <done>
    - File exists with the seven exports.
    - tsc --noEmit passes.
    - File < 60 lines.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-03: Create src/adapters/source/types.ts (SourceConnector interface)</name>
  <files>src/adapters/source/types.ts, src/adapters/source/index.ts</files>
  <behavior>
    Per ADR-002 §SourceConnector (lines 56-77) and PATTERNS row "src/adapters/source/types.ts":
    - Header block (10-35 lines per S-1) explaining the contract, including Invariant references (I-1..I-7).
    - `import type { DocId, Document, SourceHandle } from "../../types.js";` (with .js extension per S-2).
    - `import type { BodyShape, PropertiesShape, EdgeType, RefHashKind, WatchKind } from "../capabilities.js";`
    - Export `interface DocumentRef { id: DocId; mtime: number; hash: string }` — VERBATIM from ADR-002 line 67. The `hash` field semantics depend on `SourceCapabilities.refHashKind` (per Adversarial Finding 7).
    - Export `interface ListOptions { excludeGlobs?: string[]; since?: number; limit?: number }` — VERBATIM from ADR-002 §ListOptions.
    - Export `interface SourceCapabilities { bodyShape: BodyShape; properties: PropertiesShape; linkTypes: readonly EdgeType[]; identityStable: boolean; permissions: boolean; contentHashStable: boolean; refHashKind: RefHashKind; watch: WatchKind }` — VERBATIM from ADR-002 §SourceCapabilities + Adversarial Findings 7/10.
    - Export `interface SourceConnector` with methods (TSDoc each per ParsedNote style):
      - `readonly handle: SourceHandle`
      - `readonly capabilities: SourceCapabilities`
      - `listDocuments(opts?: ListOptions): AsyncIterable<DocumentRef>`
      - `readDocument(id: DocId): Promise<Document>`
      - `hash(id: DocId): Promise<string>` (semantics gated by capabilities.refHashKind)
      - `exists(id: DocId): Promise<boolean>` (MUST NOT throw on unknown id)
      - `formatDisplayUrl?(id: DocId): string | null` — optional method per D-01; obsidian-fs implements, stub returns null.
    - Also create src/adapters/source/index.ts as a barrel: `export type * from "./types.js";` (re-export for downstream importers).
    - No runtime code.
    - No `class` declarations (these are interface declarations only).
  </behavior>
  <action>Create src/adapters/source/types.ts with the SourceConnector + SourceCapabilities + ListOptions + DocumentRef interfaces and a header block per S-1. Use the Reranker (src/rerank/reranker.ts:39-48) template as the interface-decl style guide. Add the index.ts barrel. All imports use .js extension; type imports use `import type` (verbatimModuleSyntax: true).</action>
  <verify>
    <automated>npm run lint && grep -c "export interface SourceConnector" src/adapters/source/types.ts</automated>
  </verify>
  <done>
    - src/adapters/source/types.ts and src/adapters/source/index.ts exist.
    - `tsc --noEmit` passes.
    - `grep -c "export interface SourceConnector" src/adapters/source/types.ts` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-04: Create src/adapters/delivery/types.ts (DeliveryAdapter interface)</name>
  <files>src/adapters/delivery/types.ts, src/adapters/delivery/index.ts</files>
  <behavior>
    Per ADR-002 §DeliveryAdapter (lines 128-153), PATTERNS row "src/adapters/delivery/types.ts", and S-3 discriminated-union pattern:
    - Header block per S-1.
    - `import type { DocId, Document, SourceHandle } from "../../types.js";`
    - `import type { HashProtected, NamingMode } from "../capabilities.js";`
    - Re-use the EXACT WriteResult shape from src/write/write.ts:19-35 — re-declare here (do not import from src/write/, which is moving in plan 01-04):
      ```typescript
      export interface WriteSuccess { ok: true; newHash: string; doc_id: DocId; created: boolean }
      export interface WriteConflict { ok: false; reason: "hash_mismatch" | "permission_denied" | "not_found"; currentHash?: string; message?: string }
      export type WriteResult = WriteSuccess | WriteConflict
      ```
      NOTE: replace v1's `noteId: number` with `doc_id: DocId` per the new identity contract (ADR-001).
    - Export `interface UpdateResult` and `interface DeleteResult` mirroring the same Success | Conflict pattern (per S-3).
    - Export `interface WriteOptions { expectedHash?: string; clientId?: string; dryRun?: boolean }` (matches existing WriteNoteInput.expectedHash at write.ts:51).
    - Export `interface DeliveryCapabilities { atomic: boolean; hashProtected: HashProtected; enforcedSchema: boolean; naming: NamingMode }` — VERBATIM from ADR-002 lines 202-209 + Adversarial Finding 10.
    - Export `interface DeliveryAdapter` with:
      - `readonly handle: SourceHandle`
      - `readonly capabilities: DeliveryCapabilities`
      - `write(id: DocId, doc: Partial<Document>, opts?: WriteOptions): Promise<WriteResult>`
      - `update(id: DocId, patch: Partial<Document>, opts?: WriteOptions): Promise<UpdateResult>`
      - `delete(id: DocId, opts?: WriteOptions): Promise<DeleteResult>`
    - Phase-2 hook: leave the interface SHAPE such that Phase 2 (MEM-01..12) can inject the MemorySink guard at write() entry without changing the signature. Add a TSDoc note on `write()`: "Phase 2 adds MemorySink guard (provenance required; source:agent outside configured sink rejected); Phase 1 implementations have only the existing write_enabled flag + safeJoinInsideVault path safety." This is non-binding documentation but signals the seam contract to plan 01-04.
    - Also create src/adapters/delivery/index.ts barrel.
  </behavior>
  <action>Create src/adapters/delivery/types.ts with DeliveryAdapter + DeliveryCapabilities + WriteOptions + WriteResult + UpdateResult + DeleteResult per the spec above. Re-use the discriminated-union shape from src/write/write.ts:19-35 verbatim. Add the index.ts barrel. Add the Phase-2 hook TSDoc note on write().</action>
  <verify>
    <automated>npm run lint && grep -c "export interface DeliveryAdapter" src/adapters/delivery/types.ts</automated>
  </verify>
  <done>
    - src/adapters/delivery/types.ts and src/adapters/delivery/index.ts exist.
    - `tsc --noEmit` passes.
    - `grep -c "export interface DeliveryAdapter" src/adapters/delivery/types.ts` returns 1.
    - Phase-2 hook TSDoc note present on `write()` method.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-05: Create src/adapters/change-feed/types.ts (ChangeFeed interface)</name>
  <files>src/adapters/change-feed/types.ts, src/adapters/change-feed/index.ts</files>
  <behavior>
    Per ADR-002 §ChangeFeed (lines 164-188) and PATTERNS row "src/adapters/change-feed/types.ts":
    - Header block per S-1. Mention that Phase 1 obsidian-fs emits only create/update/delete (not rename) per RESEARCH A3; the type permits rename for future adapters.
    - `import type { DocId, ChangeEvent, SourceHandle } from "../../types.js";`
    - `import type { WatchKind } from "../capabilities.js";`
    - Re-export `ChangeEvent` from src/types.ts for adapter-facing convenience: `export type { ChangeEvent } from "../../types.js";`
    - Export `interface ChangeFeedCapabilities { watch: WatchKind; emitsRename: boolean; bufferSize?: number }` (the `emitsRename` flag is honesty per ADR-002 I-7; obsidian-fs sets it to `false` in plan 01-05).
    - Export `interface ChangeFeed` with:
      - `readonly handle: SourceHandle`
      - `readonly capabilities: ChangeFeedCapabilities`
      - `subscribe(handler: (e: ChangeEvent) => void | Promise<void>): Disposable` — returns a TS 5.2+ Disposable (`{ [Symbol.dispose]: () => void }`). The codebase targets ES2023 (tsconfig.json) so Symbol.dispose is natively available.
      - `close(): Promise<void>`
    - Optional: `drain?(): Promise<void>` — present in v1 VaultWatcher (watcher.ts:115); permit on the interface as optional so plan 01-05 can expose it without breaking the type contract.
    - Also create src/adapters/change-feed/index.ts barrel.
  </behavior>
  <action>Create src/adapters/change-feed/types.ts with ChangeFeed + ChangeFeedCapabilities per the spec. Re-export ChangeEvent from src/types.ts. Add the index.ts barrel.</action>
  <verify>
    <automated>npm run lint && grep -c "export interface ChangeFeed" src/adapters/change-feed/types.ts</automated>
  </verify>
  <done>
    - src/adapters/change-feed/types.ts and src/adapters/change-feed/index.ts exist.
    - `tsc --noEmit` passes.
    - `grep -c "export interface ChangeFeed " src/adapters/change-feed/types.ts` returns 1.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-06: Create src/adapters/registry.ts (parseDocId + AdapterRegistry)</name>
  <files>src/adapters/registry.ts, src/adapters/registry.test.ts</files>
  <behavior>
    Per ADR-002 §Registry (lines 256-267), RESEARCH §Pattern 2 (lines 336-352), and PATTERNS row "src/adapters/registry.ts":
    - Header block per S-1 (10-35 lines): the single minting point for branded DocIds; mirrors src/vault/manager.ts in shape.
    - `import type { DocId, SourceHandle } from "../types.js";`
    - `import type { SourceConnector } from "./source/types.js";`
    - `import type { DeliveryAdapter } from "./delivery/types.js";`
    - `import type { ChangeFeed } from "./change-feed/types.js";`
    - IIFE-closed `mintDocId` (module-private) + `parseDocId` (exported) per RESEARCH §Pattern 2 VERBATIM:
      ```typescript
      const { mintDocId, parseDocId } = (() => {
        const SCHEME_AUTHORITY_RESOURCE = /^[a-z][a-z0-9-]*:\/\/[^/]+\/.+$/;
        const mint = (s: string): DocId => s as DocId;
        const parse = (s: string): DocId => {
          if (!SCHEME_AUTHORITY_RESOURCE.test(s)) throw new Error(`Invalid DocId: ${s}`);
          return mint(s);
        };
        return { mintDocId: mint, parseDocId: parse };
      })();
      export { parseDocId };
      ```
    - Export a helper `formatDocId(scheme: string, authority: string, resource: string): DocId` that constructs and validates a DocId (use parseDocId internally; do NOT expose mintDocId).
    - Export `parseSourceHandle(s: string): SourceHandle` that validates `<scheme>://<authority>` (no resource path) and returns a branded SourceHandle.
    - Export class `AdapterRegistry` mirroring src/vault/manager.ts shape:
      - Three private Maps: `sources: Map<SourceHandle, SourceConnector>`, `deliveries: Map<SourceHandle, DeliveryAdapter>`, `changeFeeds: Map<SourceHandle, ChangeFeed>`.
      - Methods: `registerSource(handle, adapter)`, `resolveSource(handle): SourceConnector` (throws on miss with helpful message — mirror manager.require pattern), `listSources(): SourceHandle[]`. Same triad for delivery and change-feed.
    - Unit tests in src/adapters/registry.test.ts (co-located per S-4):
      - parseDocId accepts `obsidian-fs://my-vault/path/to/note.md` and returns a DocId.
      - parseDocId rejects `not-a-uri`, `obsidian-fs:/missing-slash`, `obsidian-fs://`, empty string, `OBSIDIAN-FS://X/y` (uppercase scheme), `123://x/y` (digit-leading scheme) — each throws.
      - AdapterRegistry.registerSource then resolveSource returns the same instance.
      - resolveSource(unregistered) throws with handle in message.
      - listSources returns all registered handles.
  </behavior>
  <action>Create src/adapters/registry.ts with the IIFE-closed mintDocId + exported parseDocId, formatDocId, parseSourceHandle helpers, and the AdapterRegistry class. Create src/adapters/registry.test.ts with the unit tests above. Mirror src/vault/manager.ts for the three Map-based handle resolvers. The regex `/^[a-z][a-z0-9-]*:\/\/[^/]+\/.+$/` is the canonical DocId validator per RESEARCH line 346.</action>
  <verify>
    <automated>npm test -- --run src/adapters/registry</automated>
  </verify>
  <done>
    - src/adapters/registry.ts exists with `export { parseDocId }`, `export function formatDocId`, `export function parseSourceHandle`, `export class AdapterRegistry`.
    - mintDocId is NOT exported (grep -c "export.*mintDocId" src/adapters/registry.ts returns 0).
    - All unit tests pass.
    - `grep -c "as DocId" src/adapters/registry.ts` returns exactly 1 (the single mint cast inside the IIFE).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 01-01-07: Create tests/types/docid-brand.test-d.ts (compile-time negative test)</name>
  <files>tests/types/docid-brand.test-d.ts</files>
  <behavior>
    Per ADP-05, RESEARCH §Branded DocId, PATTERNS row "tests/types/docid-brand.test-d.ts":
    - Use option (A) from PATTERNS: `@ts-expect-error` directive lines inside a plain `.test-d.ts` file. Relies on `tsc --noEmit` (already in `npm run lint:check`); NO new devDep.
    - File header TSDoc explaining: "This file is a compile-time negative test. It MUST contain `@ts-expect-error` directives that prove raw string is not assignable to DocId. If TypeScript stops emitting an error on these lines, the @ts-expect-error directive itself fails the compile — that's the assertion."
    - Imports:
      ```typescript
      import type { DocId } from "../../src/types.js";
      import { parseDocId } from "../../src/adapters/registry.js";
      ```
    - Negative cases (each with `// @ts-expect-error` on the line above):
      1. `// @ts-expect-error — raw string is not assignable to DocId\nconst x: DocId = "obsidian-fs://v/a.md";`
      2. `// @ts-expect-error — string variable is not assignable to DocId\nconst raw: string = "obsidian-fs://v/a.md"; const y: DocId = raw;`
      3. A function that takes a DocId should reject a raw string arg:
         ```typescript
         function takesDocId(_id: DocId): void {}
         // @ts-expect-error — raw string is not assignable to DocId
         takesDocId("obsidian-fs://v/a.md");
         ```
    - Positive case (no @ts-expect-error — MUST compile cleanly):
      `const valid: DocId = parseDocId("obsidian-fs://my-vault/note.md");`
      `takesDocId(valid);`
    - The file MUST be picked up by `tsc --noEmit`. Verify by adding `tests/**/*.test-d.ts` to tsconfig's `include` if it's not already covered (current tsconfig excludes only `**/*.test.ts` — `.test-d.ts` is a different glob and should already be included by the broad src+tests pattern; check and adjust if needed).
    - If tsconfig.json `include` doesn't already cover `tests/types/**`, modify tsconfig.json to add `tests/types/**/*.test-d.ts` to `include`. Be careful: existing `exclude: ["**/*.test.ts"]` does NOT exclude `.test-d.ts` files; verify with grep.
  </behavior>
  <action>Create tests/types/docid-brand.test-d.ts per the spec. Verify tsconfig.json picks it up; if not, extend `include` minimally. Run `npm run lint` (= `tsc --noEmit`) — it MUST pass. If any @ts-expect-error directive is on a line where tsc finds NO error, lint fails (which is the assertion we want).</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>
    - tests/types/docid-brand.test-d.ts exists with at least 3 @ts-expect-error directives + 1 positive case.
    - `npm run lint` (= `tsc --noEmit`) passes cleanly.
    - Test that DocId branding works: change one @ts-expect-error to `@ts-expect-error WRONG` (a non-real directive — actually just remove `@ts-expect-error` from one of the negative cases) — `npm run lint` MUST fail; revert. (This is a manual smoke check; not part of the automated verify.)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type-only file boundary | This plan adds type declarations and one IIFE-closed mint. No runtime data flows. No trust boundary crossed at runtime. |
| Compile-time identity contract | The branded DocId is the new trust boundary for downstream plans: anything that holds a `DocId` has been validated by `parseDocId` (or minted inside the registry's IIFE). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | parseDocId regex | mitigate | Regex `/^[a-z][a-z0-9-]*:\/\/[^/]+\/.+$/` rejects malformed handles; unit test in registry.test.ts asserts uppercase/digit-leading/missing-resource cases all throw. |
| T-01-01-02 | Spoofing | DocId minting outside the registry | mitigate | mintDocId is closed inside an IIFE; only parseDocId is exported. Compile-time DocId brand prevents raw-string callers from forging DocIds. Negative test in tests/types/docid-brand.test-d.ts asserts this. |
| T-01-01-03 | Information disclosure | New type files | accept | Type declarations contain no user data; no exfil surface. |

**Net security delta:** Neutral. This plan introduces no new runtime code paths; it only declares interfaces and one IIFE-closed mint. The existing security threats from src/write/write.ts (path traversal — Tampering) and chokidar's followSymlinks: false (symlink escape — Information disclosure) are NOT touched in this plan; they relocate intact in plans 01-04 and 01-05 respectively. Phase 2's MemorySink guard placement is signaled via a TSDoc note on `DeliveryAdapter.write()` (task 04) — Phase 1 does NOT implement it, but the seam shape permits the addition.
</threat_model>

<verification>
## Plan-Level Checks (run after all tasks complete)

```bash
# 1. Full lint + typecheck (covers tsc --noEmit including .test-d.ts files)
npm run lint:check

# 2. Full test suite (no regressions; 324 v1 tests still green)
npm test

# 3. Confirm new files are reachable from src/ tree
find src/adapters -type f -name '*.ts'

# 4. Confirm DocId brand is enforced (the negative test compiles)
npm run lint  # tsc --noEmit picks up tests/types/docid-brand.test-d.ts

# 5. PR-boundary command (per VALIDATION.md "After every plan wave"):
npm run lint:check && npm test && npm run eval:baseline
# Note: scripts/lint-adapters.sh does not exist yet (plan 01-06 creates it).
```
</verification>

<success_criteria>
- [ ] All 7 tasks complete with `<automated>` verifies green.
- [ ] `npm run lint:check` passes (tsc --noEmit + prettier + existing shell lints).
- [ ] `npm test` passes — all 324 v1 tests still green; no regressions.
- [ ] `npm run eval:baseline` passes — v1-baseline eval suite unchanged.
- [ ] Branded DocId compile-time guard verified by tests/types/docid-brand.test-d.ts under tsc --noEmit.
- [ ] mintDocId is module-private (`grep -c "export.*mintDocId" src/adapters/registry.ts` returns 0).
- [ ] Three seam-interface files exist and compile.
- [ ] AdapterRegistry has registerSource/resolveSource/listSources triad (plus delivery and change-feed equivalents).
</success_criteria>

<output>
After completion, create `.planning/phases/01-adapter-extraction-tech-debt-up/01-01-SUMMARY.md` capturing:
- Final file list created/modified.
- The exact regex used in parseDocId.
- Decision on tests/types/docid-brand.test-d.ts approach (option A — @ts-expect-error, no new devDep — confirmed).
- Whether tsconfig.json `include` needed extension.
- Note for plan 01-02..06: types are now available at the import paths listed in `<must_haves>.artifacts`.
</output>

## Dependencies / Unlocks

- **Depends on:** Nothing (Wave 1 — first plan of the phase).
- **Unlocks:** ALL subsequent plans (01-02 through 01-06). The branded DocId, three seam interfaces, and AdapterRegistry are the prerequisite type surface for every downstream task.

## Backwards-Compat Checklist

This plan touches no production code paths — no v1 tool surface change. All 23 v1 tools continue to work unchanged because their handlers, schemas, and snapshot are untouched. The only changes are additions to src/types.ts (no removals or modifications of existing exports), and new files under src/adapters/ and tests/types/. Snapshot test (`evals/v1-baseline/tools-list.snapshot.json`) is unaffected.

| v1 Tool | Snapshot Untouched | Handler Untouched |
|---------|--------------------|-------------------|
| All 23 (search, search_hybrid, search_text, read_note, write_note, update_frontmatter, query_frontmatter, suggest_frontmatter, delete_note, get_backlinks, get_forwardlinks, list_vaults, vault_status, get_audit_log, index_vault, fetch, etc.) | ✓ — no PR field changes inputSchema | ✓ — no handler routed through adapter yet |
