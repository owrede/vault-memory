---
phase: 07-visual-contract-editor-canvas
plan: 09
type: execute
wave: 3
depends_on: ["07-03", "07-04"]
files_modified:
  - plugin/src/chrome/reindex-panel.svelte
  - plugin/src/chrome/reindex-panel.test.ts
  - plugin/src/chrome/stats-panel.svelte
  - plugin/src/chrome/stats-panel.test.ts
  - plugin/src/chrome/chrome-view.ts
  - plugin/src/chrome/chrome-view.test.ts
  - plugin/main.ts
  - plugin/src/chrome/reindex-stats.css
autonomous: true
requirements: [PLG-03, PLG-04]
must_haves:
  truths:
    - "The plugin exposes a side-panel view that bundles Reindex + Stats panels under one workspace leaf"
    - "Clicking 'Reindex this vault' calls `trigger_reindex({scope: 'this'})` MCP tool; progress notifications render as a live progress bar"
    - "Clicking 'Reindex all vaults' calls `trigger_reindex({scope: 'all'})`"
    - "Stats panel shows notes, chunks, last-index timestamp, embedding model + dimensions, audit_log counts by kind, peer-MCP connection status, contract count; refreshes on demand"
    - "All reads + writes go through MCP tools — no direct DB access from the plugin"
  artifacts:
    - path: plugin/src/chrome/chrome-view.ts
      provides: "ItemView subclass that hosts the Reindex + Stats panels in a single workspace leaf; opened via plugin command"
      contains: "VIEW_TYPE_CHROME"
    - path: plugin/src/chrome/reindex-panel.svelte
      provides: "Reindex buttons + progress bar; subscribes to MCP progress notifications via progressToken"
    - path: plugin/src/chrome/stats-panel.svelte
      provides: "Read-only stats grid; refresh button; polls get_runtime_stats on demand"
  key_links:
    - from: plugin/src/chrome/reindex-panel.svelte
      to: src/plugin-tools/trigger-reindex.ts
      via: "plugin.mcpClient.callTool('trigger_reindex', {scope, progressToken}) + onProgress(token, handler)"
      pattern: "trigger_reindex"
    - from: plugin/src/chrome/stats-panel.svelte
      to: src/plugin-tools/get-runtime-stats.ts
      via: "plugin.mcpClient.callTool('get_runtime_stats', {vault})"
      pattern: "get_runtime_stats"
---

<objective>
Close PLG-03 (manual reindex with progress feedback) and PLG-04 (read-only stats snapshot) chrome panels. Both panels render in a single side-panel `ItemView` so they share a workspace leaf and are openable via a single plugin command. Wave-3-parallel with 07-08, 07-10.

Purpose: Make vault-memory operability (reindex + observability) discoverable from Obsidian.
Output: ItemView + two Svelte panels + ≥6 new tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md
@.planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md
@.planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md
@.planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md
@docs/v2/adr/007-contract-editor.md
@plugin/src/services/mcp-client.ts
@src/plugin-tools/trigger-reindex.ts
@src/plugin-tools/get-runtime-stats.ts
@plugin/main.ts

<interfaces>
From plugin/src/services/mcp-client.ts (07-03):
```typescript
class VaultMemoryMcpClient {
  callTool(name: string, args: object): Promise<unknown>
  onProgress(token: string, handler: (progress: number, total?: number) => void): () => void
}
```

From src/plugin-tools/trigger-reindex.ts (07-04):
- Input: `{vault?: string, scope: "this" | "all", progressToken?: string}`
- Output: `{ok: true, vaults: string[]}`
- Side effect: emits `notifications/progress` with the provided `progressToken`

From src/plugin-tools/get-runtime-stats.ts (07-04):
- Input: `{vault?: string}`
- Output: `{vault, notes, chunks, last_index_at, embedding_model, embedding_dim, audit_log_by_kind, peer_mcp_status, contract_count}`

Obsidian:
```typescript
class ItemView extends View {
  getViewType(): string
  getDisplayText(): string
  getIcon(): string
  onOpen(): Promise<void>
  onClose(): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Reindex panel</name>
  <files>plugin/src/chrome/reindex-panel.svelte, plugin/src/chrome/reindex-panel.test.ts</files>
  <read_first>
    - plugin/src/services/mcp-client.ts (onProgress signature)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-CHROME-REINDEX (PLG-03)"
    - .planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md "Reindex panel" copy + spacing
    - src/plugin-tools/trigger-reindex.ts (07-04)
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md §"Open Question on progress streaming" + MCP SDK 1.29 progress notifications
  </read_first>
  <behavior>
    - Component props: `{mcpClient: VaultMemoryMcpClient, activeVault: string | null}`.
    - Renders two buttons: "Reindex this vault" (disabled if `activeVault === null`) and "Reindex all vaults".
    - On click: generates a unique `progressToken` (`crypto.randomUUID()`), subscribes via `mcpClient.onProgress(token, handler)`, calls `mcpClient.callTool("trigger_reindex", {scope, progressToken: token, vault?})`.
    - Renders a progress bar bound to `progress / total`; when `total` is undefined renders an indeterminate spinner.
    - On completion (tool call resolves), unsubscribes; renders "Reindex complete — N vaults processed".
    - On error: renders the error message inline; unsubscribes; both buttons re-enable.
  </behavior>
  <action>
    Implement the Svelte component. Pattern F doc-block citing D-CHROME-REINDEX + PLG-03. Uses Obsidian's `Notice` for the "complete" toast in addition to the inline state per UI-SPEC §"Editor surface" copy table for the reindex toast accent rule. Inline progress bar uses `var(--interactive-accent)` for the fill per UI-SPEC §"Color" rule 6. Co-located test: (a) mock mcpClient; (b) click "this vault" button triggers a `callTool` call with the expected args; (c) `onProgress` handler invocations update a `data-testid="progress-fill"` element's width attribute; (d) the "all vaults" button works analogously; (e) error response surfaces inline.
  </action>
  <verify>
    <automated>cd plugin && npm test -- --run plugin/src/chrome/reindex-panel.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `reindex-panel.svelte` source contains the literals `"trigger_reindex"` and `"onProgress"` (or the destructured equivalent).
    - Test file contains ≥4 `it(...)` blocks; all pass.
    - Source uses `var(--interactive-accent)` for the progress fill.
    - `cd plugin && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>Reindex is reachable from the chrome surface; progress streams live via MCP notifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Stats panel</name>
  <files>plugin/src/chrome/stats-panel.svelte, plugin/src/chrome/stats-panel.test.ts</files>
  <read_first>
    - plugin/src/services/mcp-client.ts (callTool signature)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-CHROME-STATS (PLG-04)"
    - .planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md "Stats panel" layout
    - src/plugin-tools/get-runtime-stats.ts (07-04 — the output shape)
  </read_first>
  <behavior>
    - Component props: `{mcpClient: VaultMemoryMcpClient, activeVault: string | null}`.
    - On mount: calls `get_runtime_stats({vault: activeVault ?? undefined})`; renders result fields in a 2-column key/value grid.
    - Rendered fields (per CONTEXT D-CHROME-STATS): notes, chunks, last_index_at (ISO formatted), embedding_model + embedding_dim ("bge-m3 × 1024"), audit_log_by_kind (as a nested list of "kind: count"), peer_mcp_status (green dot for available, red for unavailable), contract_count.
    - "Refresh" button re-fetches; spinner shows during fetch.
    - Failure path renders the error inline ("Could not load stats: <reason>"); does NOT crash the panel.
    - All values mono-font for machine-readable strings (counts, timestamps, model names) per UI-SPEC Typography "Mono" row.
  </behavior>
  <action>
    Implement the Svelte panel. Pattern F doc-block citing D-CHROME-STATS + PLG-04. CSS grid for the 2-col key/value layout, using `var(--font-monospace)` for value cells. Peer-MCP status uses `var(--text-success)` / `var(--text-error)` dots (not `--interactive-accent` per UI-SPEC §"Color" anti-rules). Co-located test: (a) mock mcpClient returns a known payload; assert all rendered fields match; (b) refresh button re-calls `get_runtime_stats` once per click; (c) error path renders the inline error and does not throw; (d) peer-MCP available status renders green; unavailable renders red.
  </action>
  <verify>
    <automated>cd plugin && npm test -- --run plugin/src/chrome/stats-panel.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `stats-panel.svelte` source contains the literal `"get_runtime_stats"`.
    - Test file contains ≥4 `it(...)` blocks; all pass.
    - Source uses `var(--font-monospace)` for value cells.
    - `cd plugin && npx tsc --noEmit && npm run build` exits 0.
  </acceptance_criteria>
  <done>Stats panel is read-only and refreshable; all reads through MCP — no DB coupling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Compose the chrome ItemView + register plugin command</name>
  <files>plugin/src/chrome/chrome-view.ts, plugin/src/chrome/chrome-view.test.ts, plugin/main.ts, plugin/styles.css</files>
  <read_first>
    - plugin/src/chrome/reindex-panel.svelte (task 1)
    - plugin/src/chrome/stats-panel.svelte (task 2)
    - plugin/main.ts (07-03 — onload sequence)
    - Obsidian `ItemView` + `workspace.getRightLeaf` API reference
    - plugin/src/views/contract-editor/view.ts (analog ItemView wiring pattern)
    - .planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md "Side-panel layout" (if defined; otherwise default to two stacked sections with `var(--size-4-6)` separation)
  </read_first>
  <action>
    Create `chrome-view.ts` exporting `VIEW_TYPE_CHROME = "vault-memory-chrome"` and `ChromeView extends ItemView`. `getViewType` returns the constant; `getDisplayText` returns `"vault-memory"`; `getIcon` returns `"activity"` (Lucide). `onOpen()` mounts a Svelte wrapper containing reindex-panel above stats-panel, separated by a heading row "Operations" / "Stats". The two panels receive `{mcpClient: this.plugin.mcpClient, activeVault: this.plugin.settingsStore.get("defaultVault")}` props. `onClose()` disposes the Svelte tree. Update `plugin/main.ts` `onload()` to: (a) `this.registerView(VIEW_TYPE_CHROME, leaf => new ChromeView(leaf, this))`; (b) `this.addCommand({id: "open-chrome", name: "Open vault-memory panel", callback: () => this.activateChromeView()})`; (c) implement `activateChromeView()` that resolves a right-leaf workspace pane and reveals it. Tested via `chrome-view.test.ts` with mock obsidian: (a) `getViewType` returns the expected constant; (b) `onOpen` is callable without throwing; (c) `getDisplayText` returns `"vault-memory"`. Append CSS to `plugin/styles.css` for the chrome view section spacing.
  </action>
  <verify>
    <automated>cd plugin && npm test -- --run plugin/src/chrome/chrome-view.test.ts --reporter=basic && cd plugin && npm run typecheck && npm run build 2>&1 | tail -5 && grep -q "registerView(VIEW_TYPE_CHROME" plugin/main.ts && grep -q "open-chrome" plugin/main.ts</automated>
  </verify>
  <acceptance_criteria>
    - `chrome-view.ts` exports `VIEW_TYPE_CHROME` and `ChromeView` (extends `ItemView`).
    - `plugin/main.ts` registers the view and a command with id `"open-chrome"`.
    - Test file contains ≥3 `it(...)` blocks; all pass.
    - Stats + Reindex panels both render under the ChromeView per `onOpen` source inspection.
    - `cd plugin && npm run typecheck && npm run build` exits 0.
  </acceptance_criteria>
  <done>PLG-03 + PLG-04 close in a single discoverable side-panel; the user runs a plugin command "Open vault-memory panel" to reach both operations + observability surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `trigger_reindex` over MCP | I/O-heavy operation; misuse could DoS |
| `get_runtime_stats` over MCP | Read-only; surfaces vault contents counts but not content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Denial of Service | Reindex button spammed | accept | Button disables during in-flight call; user-visible state; v2.0.0 ships without a rate limiter |
| T-07-09-02 | Information Disclosure | Stats panel | accept | Counts + timestamps + model names — non-sensitive metadata; no content surfaced |
</threat_model>

<verification>
- `cd plugin && npm test -- --run --reporter=basic` exits 0.
- `cd plugin && npm run typecheck && npm run build` exits 0.
- `grep -n "trigger_reindex" plugin/src/chrome/reindex-panel.svelte` returns ≥1 hit.
- `grep -n "get_runtime_stats" plugin/src/chrome/stats-panel.svelte` returns ≥1 hit.
- Manual smoke: run "Open vault-memory panel" command in Obsidian; expected: side panel opens with both reindex + stats sections; clicking refresh on stats fetches via MCP; clicking reindex shows a live progress bar.
</verification>

<success_criteria>
- PLG-03 complete: one-click reindex with live progress feedback via MCP notifications.
- PLG-04 complete: read-only stats grid; refresh on demand; no direct DB access.
- Both panels share a single workspace leaf, reachable via a plugin command.
</success_criteria>

<output>
Create `.planning/phases/07-visual-contract-editor-canvas/07-09-SUMMARY.md` when done. Summary includes: chrome view type id; command id; progress feedback path (MCP progressToken); stats field count; explicit confirmation that no `this.app.metadataCache` / direct DB access from either panel.
</output>
