---
phase: 07-visual-contract-editor-canvas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v2/adr/007-contract-editor.md
  - docs/v2/adr/README.md
  - plugin/manifest.json
  - plugin/versions.json
  - plugin/package.json
  - plugin/tsconfig.json
  - plugin/esbuild.config.mjs
  - plugin/main.ts
  - plugin/src/views/contract-editor/view.ts
  - plugin/src/views/contract-editor/spike/canvas-pane.svelte
  - plugin/src/views/contract-editor/spike/StepNode.svelte
  - plugin/styles.css
  - plugin/tests/mocks/obsidian.ts
  - plugin/vitest.config.ts
  - plugin/tsconfig.json
  - package.json
  - examples/contracts/meeting-prep.contract
autonomous: false
requirements: [CAN-10]
must_haves:
  truths:
    - "ADR 007 exists, status Accepted, lists `.contract` JSON schema verbatim, jsoncanvas-fork rescope rationale, Variant C UI lock, plugin-tool gating, D-CANON rules"
    - "Working prototype Obsidian plugin opens `examples/contracts/meeting-prep.contract` in a registered view and renders the assembly DAG using Svelte Flow"
    - "Upstream renderer license is MIT-compatible (verified against `@xyflow/svelte` MIT)"
    - "Go/no-go gate has been resolved by a human checkpoint; if no-go, Phase 7 escalates to re-discuss BEFORE any subsequent plan executes"
  artifacts:
    - path: docs/v2/adr/007-contract-editor.md
      provides: "Phase 7 ADR — `.contract` JSON schema, jsoncanvas rescope, Variant C lock, CAN-07 canonicalization rules verbatim, plugin-tool gating decision"
      contains: "ADR-007"
    - path: plugin/manifest.json
      provides: "Obsidian plugin manifest; id=vault-memory, isDesktopOnly=true, minAppVersion=1.5.0"
      contains: '"id": "vault-memory"'
    - path: plugin/main.ts
      provides: "Plugin entry; registerView + registerExtensions(['contract']) + minimum onload/onunload"
    - path: plugin/src/views/contract-editor/view.ts
      provides: "ContractEditorView extends TextFileView; getViewData/setViewData stubs sufficient to round-trip JSON"
    - path: examples/contracts/meeting-prep.contract
      provides: "Spike fixture — meeting-prep contract translated to .contract JSON wrapper"
  key_links:
    - from: plugin/main.ts
      to: plugin/src/views/contract-editor/view.ts
      via: "registerView(VIEW_TYPE_CONTRACT, leaf => new ContractEditorView(leaf))"
      pattern: "registerView\\(VIEW_TYPE_CONTRACT"
    - from: plugin/main.ts
      to: "Obsidian Plugin API"
      via: "registerExtensions(['contract'], VIEW_TYPE_CONTRACT)"
      pattern: "registerExtensions\\(\\[\"contract\"\\]"
---

<objective>
Land ADR 007 + a working Obsidian plugin prototype that proves the Variant C editor surface is buildable on top of `@xyflow/svelte` 1.5.2 (rescoped from "fork jsoncanvas" per RESEARCH §3 Pitfall 4). The deliverable gates every other Phase 7 plan. If the prototype reveals the renderer choice is non-viable, Phase 7 escalates to re-discuss BEFORE any further plan runs.

Purpose: Resolve the single highest implementation risk in Phase 7 before bulk work begins.
Output: ADR 007 (Accepted), a `plugin/` package skeleton (`manifest.json`, `package.json`, `tsconfig.json`, `esbuild.config.mjs`, `main.ts`, `styles.css`, `views/contract-editor/view.ts`, `views/contract-editor/spike/{canvas-pane,StepNode}.svelte`, `tests/mocks/obsidian.ts`, `vitest.config.ts`), the workspace root wiring in the repo `package.json`, and `examples/contracts/meeting-prep.contract` as the prototype fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md
@.planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md
@.planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md
@.planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md
@docs/v2/adr/006-task-contract-dsl.md
@docs/v2/adr/README.md
@evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml
@src/contracts/schema.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author ADR 007 — Contract Editor</name>
  <files>docs/v2/adr/007-contract-editor.md, docs/v2/adr/README.md</files>
  <read_first>
    - docs/v2/adr/006-task-contract-dsl.md (structural template — top-of-file metadata, Decision-per-section format, Threat Model, Pitfalls)
    - docs/v2/adr/README.md (MADR-style table the new ADR row appends to)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md (sections "Decisions" — all D-* lock the ADR content)
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md §3 Pitfall 4 (jsoncanvas-fork rescope rationale)
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md §6 (suppression API extension rationale)
    - .planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md (Variant C three-pane layout reference)
  </read_first>
  <action>
    Create `docs/v2/adr/007-contract-editor.md` with status `Accepted`, phase `7`, related `ADR-001, ADR-002, ADR-003, ADR-006`. Mirror the section structure of ADR-006: `## Context`, then one `## Decision: <name>` section per CONTEXT.md locked decision (`D-UI`, `D-SURFACE`, `D-FORMAT`, `D-FORMAT-SCHEMA`, `D-FORMAT2`, `D-AUTH`, `D-CANON`, `D-CANON-TEST`, `D-PALETTE`, `D-WATCH-PLUGIN-OUT`, `D-WATCH-SERVER-NOTIFY`, `D-WATCH-NO-PLUGIN-WATCH`, `D-MCP-SURFACE`, `D-CHROME-PHILOSOPHY`, `D-DIST-PRIMARY`, `D-SKILL-NAMING`, `D-VERSION`), then `## Threat Model` (per-device safeStorage posture; `[plugin] enabled` default-OFF gate; v1 tools-list snapshot invariant; GitHub Releases tarball MITM mitigation), then `## Pitfalls` (jsoncanvas-fork rescope per RESEARCH §3 Pitfall 4; suppression API extension per RESEARCH §6 Pitfall 1; plugin↔server stdio collision per RESEARCH §6 Pitfall 6 with strategy (a) for v2.0.0). The ADR MUST carry verbatim the canonicalization rules from CONTEXT D-CANON ("Always canonical" + "Always preserved" + "Editor state survives") and the `.contract` JSON shape sketch from D-FORMAT-SCHEMA. Append a new row to `docs/v2/adr/README.md` MADR table referencing 007 with status Accepted, date 2026-05-19, decision summary "Variant C palette+canvas+inspector Obsidian plugin authoring `.contract` JSON emitted as canonical Phase 6 YAML; renderer Svelte Flow rather than jsoncanvas fork".
  </action>
  <verify>
    <automated>test -f docs/v2/adr/007-contract-editor.md && grep -q "ADR-007" docs/v2/adr/007-contract-editor.md && grep -q "Accepted" docs/v2/adr/007-contract-editor.md && grep -q "D-UI" docs/v2/adr/007-contract-editor.md && grep -q "D-CANON" docs/v2/adr/007-contract-editor.md && grep -q "D-MCP-SURFACE" docs/v2/adr/007-contract-editor.md && grep -q "Svelte Flow" docs/v2/adr/007-contract-editor.md && grep -q "007-contract-editor" docs/v2/adr/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/v2/adr/007-contract-editor.md` exists with `**Status:** Accepted` on a header line within the first 10 lines.
    - File contains a `## Decision:` heading for each of D-UI, D-SURFACE, D-FORMAT, D-FORMAT-SCHEMA, D-FORMAT2, D-AUTH, D-CANON, D-WATCH-PLUGIN-OUT, D-WATCH-SERVER-NOTIFY, D-MCP-SURFACE.
    - File contains a `## Threat Model` section listing at minimum: per-device safeStorage posture; `[plugin] enabled` default-OFF gate; v1 snapshot invariant; GitHub Releases tarball MITM.
    - File contains a `## Pitfalls` section explicitly mentioning the jsoncanvas-fork rescope.
    - `docs/v2/adr/README.md` contains a table row referencing `007-contract-editor.md` with status `Accepted`.
  </acceptance_criteria>
  <done>ADR 007 lands as the source of truth for every subsequent Phase 7 plan; the rescoping of CAN-10 from "fork jsoncanvas" to "wire Svelte Flow" is documented; the canonicalization rules are pinned verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bootstrap plugin/ package and prototype editor view</name>
  <files>plugin/manifest.json, plugin/versions.json, plugin/package.json, plugin/tsconfig.json, plugin/esbuild.config.mjs, plugin/main.ts, plugin/styles.css, plugin/src/views/contract-editor/view.ts, plugin/src/views/contract-editor/spike/canvas-pane.svelte, plugin/src/views/contract-editor/spike/StepNode.svelte, plugin/tests/mocks/obsidian.ts, plugin/vitest.config.ts, package.json, examples/contracts/meeting-prep.contract</files>
  <read_first>
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md §"Code Examples" Ex. 1 (manifest fields), Ex. 2 (esbuild config), §"Architecture Patterns" Pattern 1+2 (registerView, TextFileView lifecycle), §"Recommended Project Structure" (`plugin/` tree)
    - .planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md "Plugin tree (Svelte/canvas/inspector components) — GREENFIELD" + "`plugin/main.ts` — Obsidian `Plugin` entry point" pattern
    - package.json (root — confirm Node engines, current deps, workspaces field absence; this task either adds `"workspaces": ["plugin"]` or documents a non-workspace install)
    - tsconfig.json (root — `plugin/tsconfig.json` MUST `"extends": "../tsconfig.json"` and override `module`/`moduleResolution`/`outDir` only)
    - evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml (source content the .contract fixture wraps verbatim)
    - src/contracts/schema.ts (the ContractFileSchema Zod the .contract wrapper carries verbatim)
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md "Pitfall 5" (`vitest` `obsidian` alias to a stub module)
    - .planning/phases/07-visual-contract-editor-canvas/07-UI-SPEC.md (theme inheritance — `var(--background-primary)` etc. for `plugin/styles.css`)
  </read_first>
  <action>
    Scaffold the `plugin/` package tree as a sibling to `src/`. Add `"workspaces": ["plugin"]` to the root `package.json` so `npm install` from the repo root resolves the plugin's deps; if workspaces are already declared, append the entry. Create `plugin/package.json` declaring devDependencies on `svelte` `^5.55`, `@xyflow/svelte` `^1.5`, `esbuild` `^0.28`, `esbuild-svelte`, `svelte-preprocess`, `builtin-modules` `^5`, `tslib` `^2.6`, `vitest` matching the root version, `typescript` matching the root; peer-dep on `obsidian` `^1.5`. Plugin scripts: `build` (esbuild production), `dev` (esbuild watch), `test` (vitest run), `typecheck` (`tsc --noEmit`). Create `plugin/manifest.json` per RESEARCH §"Code Examples" Ex. 1 (id `vault-memory`, version `2.0.0`, minAppVersion `1.5.0`, isDesktopOnly `true`, description "Local-first agentic knowledge layer for Obsidian — semantic memory, briefs, and visual task contract editor."). Create `plugin/versions.json` mapping `"2.0.0": "1.5.0"`. Create `plugin/tsconfig.json` extending root tsconfig with module `ESNext`, moduleResolution `Bundler`, jsx omitted, `compilerOptions.types` including `node` and `svelte`. Create `plugin/esbuild.config.mjs` per RESEARCH §"Code Examples" Ex. 2 — entryPoint `main.ts`, format `cjs`, target `es2022`, outfile `main.js`, externalize `obsidian`, `electron`, all `builtins`, plus `esbuild-svelte` plugin via `sveltePreprocess()`. Create `plugin/main.ts` as a `Plugin` subclass with `onload()` that calls `this.registerView(VIEW_TYPE_CONTRACT, leaf => new ContractEditorView(leaf, this))` and `this.registerExtensions(["contract"], VIEW_TYPE_CONTRACT)`; `onunload()` empty (Obsidian auto-unregisters views). Create `plugin/src/views/contract-editor/view.ts` exporting `VIEW_TYPE_CONTRACT = "vault-memory-contract-editor"` and `ContractEditorView extends TextFileView` with `getViewType` returning the constant, `getDisplayText` returning `this.file?.basename ?? "Contract"`, `getIcon` returning `"git-branch"`, `setViewData(data, clear)` `JSON.parse`ing into a private `currentJson` and mounting the spike Svelte component, `getViewData()` returning `JSON.stringify(currentJson, null, 2)`, `clear()` nulling state. Mount `plugin/src/views/contract-editor/spike/canvas-pane.svelte` wrapping `@xyflow/svelte`'s `<SvelteFlow>` with `snapGrid={[20, 20]}`, custom `nodeTypes` mapping `step` to `StepNode.svelte`, and a deterministic LTR topological layout (220×120 node footprint per UI-SPEC) computed from `currentJson.contract.assembly`. `StepNode.svelte` renders the step's `as` alias + verb name only — full inspector forms come in a later plan. Create `plugin/styles.css` with the three-pane CSS grid (palette/canvas/inspector columns), using Obsidian CSS variables only (`var(--background-primary)`, `var(--background-secondary)`, `var(--text-normal)`, `var(--interactive-accent)`) per UI-SPEC §"Design System". Create `plugin/tests/mocks/obsidian.ts` exporting stub classes for `Plugin`, `TextFileView`, `WorkspaceLeaf`, `Vault`, `App`, `PluginSettingTab`, `Setting`, `Modal`, `Notice` — minimum surface the codec + view code imports per RESEARCH Pitfall 5. Create `plugin/vitest.config.ts` defining `resolve.alias.obsidian` → `./tests/mocks/obsidian.ts`. Create `examples/contracts/meeting-prep.contract` by reading the YAML fixture at `evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml`, converting to JSON (via `yaml.parseDocument(...).toJS()`), wrapping under `{ "$schema": "https://vault-memory.dev/schemas/contract-v1.json", "vmFormatVersion": 1, "contract": <converted>, "editor": { "nodes": [], "selection": null, "viewport": {"x":0,"y":0,"zoom":1.0}, "yamlComments": {} } }` with `"editor.nodes"` populated by the default LTR topological layout, ID format `step:<step.as>` and one entry per assembly step.
  </action>
  <verify>
    <automated>test -f plugin/manifest.json && test -f plugin/main.ts && test -f plugin/esbuild.config.mjs && test -f plugin/src/views/contract-editor/view.ts && test -f examples/contracts/meeting-prep.contract && grep -q '"isDesktopOnly": true' plugin/manifest.json && grep -q "registerExtensions" plugin/main.ts && grep -q "TextFileView" plugin/src/views/contract-editor/view.ts && node -e "const j=require('./examples/contracts/meeting-prep.contract'); if(j.vmFormatVersion!==1) process.exit(1); if(!j.contract||!j.contract.assembly) process.exit(2);" && cd plugin && npm run typecheck && npm test -- --run --reporter=basic && cd ..</automated>
  </verify>
  <acceptance_criteria>
    - `plugin/manifest.json` parses as JSON with `id == "vault-memory"`, `version == "2.0.0"`, `minAppVersion == "1.5.0"`, `isDesktopOnly == true`.
    - `plugin/main.ts` source contains the literal substrings `registerView(VIEW_TYPE_CONTRACT` and `registerExtensions(["contract"]` (or single-quoted equivalent).
    - `plugin/src/views/contract-editor/view.ts` exports `VIEW_TYPE_CONTRACT` whose value matches `"vault-memory-contract-editor"`, and a class extending `TextFileView` with `getViewType`, `getDisplayText`, `setViewData`, `getViewData`, `clear` methods.
    - `examples/contracts/meeting-prep.contract` parses as JSON; `vmFormatVersion === 1`; `contract.name === "meeting-prep"`; `editor.nodes.length === contract.assembly.length`.
    - `cd plugin && npm run typecheck` exits 0.
    - `cd plugin && npm test -- --run` exits 0 (no plugin tests yet — empty pass is acceptable).
    - Root `package.json` includes `"plugin"` in its `workspaces` array OR documents a non-workspace install in `plugin/README.md`.
  </acceptance_criteria>
  <done>Plugin builds; opening a `.contract` file in Obsidian launches `ContractEditorView`; the prototype renders the meeting-prep assembly as a left-to-right DAG using Svelte Flow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Go/no-go gate — spike outcome</name>
  <what-built>
    - ADR 007 at `docs/v2/adr/007-contract-editor.md` (Accepted)
    - Working plugin skeleton at `plugin/` rendering `examples/contracts/meeting-prep.contract` in a `registerView` view via `@xyflow/svelte`
    - Verified MIT license on `@xyflow/svelte` (npm registry metadata)
  </what-built>
  <how-to-verify>
    1. Read `docs/v2/adr/007-contract-editor.md` and confirm every locked D-* decision in `07-CONTEXT.md` "Decisions" has a corresponding `## Decision:` section.
    2. From the repo root run `cd plugin && npm install && npm run build` and verify `plugin/main.js` is produced without errors.
    3. Symlink or copy `plugin/` into an Obsidian vault's `.obsidian/plugins/vault-memory/` directory (with `manifest.json`, `main.js`, `styles.css`). Open `examples/contracts/meeting-prep.contract` in that vault. Expected: view opens automatically; canvas renders one node per assembly step laid out left to right.
    4. Confirm `npm view @xyflow/svelte license` returns `MIT`.
    5. Decide go/no-go. If go, type "approved" and proceed. If no-go, document the reason in the resume message and escalate Phase 7 to re-discuss (variants A/B/D in `design-variants/` are the fallback).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to Wave 2 plans, or describe the spike outcome and escalation reason if no-go.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `npm install @xyflow/svelte` install path | npm package install introduces JS that runs in Obsidian's renderer process |
| `plugin/` directory in user vault | Plugin code runs with Electron renderer privileges; reads/writes `.contract` files |
| User's Obsidian instance loading the plugin | Plugin manifest enables JS execution inside Electron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | `npm install @xyflow/svelte`, `svelte`, `esbuild`, `esbuild-svelte`, `svelte-preprocess`, `builtin-modules`, `tslib` | mitigate | Use Package Legitimacy Audit results in 07-RESEARCH.md §"Package Legitimacy Audit"; all 7 packages are [VERIFIED] (npm-registry-confirmed, source-repo-confirmed); blocking-human spike checkpoint (Task 3) verifies MIT license on `@xyflow/svelte` via `npm view @xyflow/svelte license` before any plan that depends on Wave 1 proceeds |
| T-07-01-PROTO | Tampering | Spike prototype loaded in a real Obsidian vault | accept | Prototype is loaded only into the maintainer's test vault; not distributed; spike checkpoint surfaces any unexpected behavior before Wave 2 begins |
| T-07-01-ADR | Information Disclosure | ADR 007 published in public repo | accept | ADR contains design rationale only; no secrets; matches ADR-001..006 publishing posture |
</threat_model>

<verification>
- `docs/v2/adr/007-contract-editor.md` exists with Accepted status and covers every locked D-* decision from CONTEXT.md.
- `plugin/` directory contains `manifest.json`, `main.ts`, `esbuild.config.mjs`, `package.json`, `tsconfig.json`, `vitest.config.ts`, `styles.css`, `src/views/contract-editor/view.ts`, `src/views/contract-editor/spike/canvas-pane.svelte`, `src/views/contract-editor/spike/StepNode.svelte`, `tests/mocks/obsidian.ts`.
- `cd plugin && npm install && npm run typecheck && npm run build && npm test -- --run` exits 0 at every step.
- `examples/contracts/meeting-prep.contract` parses as the documented `.contract` JSON shape (`$schema`, `vmFormatVersion: 1`, `contract`, `editor`).
- Root `package.json` lists `"plugin"` under `workspaces` so `npm install` from the root resolves plugin deps.
- The human checkpoint resolved approved.
</verification>

<success_criteria>
- ADR 007 documents the rescoping decision verbatim; future maintainers reading the ADR can reconstruct why jsoncanvas-fork was abandoned for Svelte Flow.
- The plugin prototype renders the `meeting-prep` reference contract end-to-end inside Obsidian.
- The spike checkpoint resolves "approved" — go/no-go is logged.
- Every Wave 2+ plan can begin with confidence that the renderer choice + plugin shape are buildable.
</success_criteria>

<output>
Create `.planning/phases/07-visual-contract-editor-canvas/07-01-SUMMARY.md` when done. Summary includes: ADR 007 commit SHA, plugin/manifest.json version, `@xyflow/svelte` resolved version + license, spike checkpoint outcome (go/no-go + rationale), enumerated list of Phase 7 plans now unblocked.
</output>
