---
phase: 07-visual-contract-editor-canvas
plan: 06
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - examples/contracts/meeting-prep.contract
  - examples/contracts/project-status.contract
  - examples/contracts/code-review-brief.contract
  - examples/contracts/round-trip.test.ts
autonomous: true
requirements: [CAN-06, CAN-07]
must_haves:
  truths:
    - "Three reference `.contract` files exist in `examples/contracts/` matching the three Phase 6 reference contracts byte-for-byte after canonicalization round-trip"
    - "For each reference contract: `import yaml → emit .contract → emit yaml` produces YAML whose parsed JS value deepEqual's the original parse"
    - "Editor-state base64 comment block survives across two round-trip cycles"
    - "`smoketest-trivial.yaml` also round-trips cleanly through the codec"
  artifacts:
    - path: examples/contracts/meeting-prep.contract
      provides: "Reference contract 1 — meeting-prep contract in .contract JSON wrapper"
      contains: '"name": "meeting-prep"'
    - path: examples/contracts/project-status.contract
      provides: "Reference contract 2 — project-status contract in .contract JSON wrapper"
      contains: '"name": "project-status"'
    - path: examples/contracts/code-review-brief.contract
      provides: "Reference contract 3 — code-review-brief contract in .contract JSON wrapper"
      contains: '"name": "code-review-brief"'
    - path: examples/contracts/round-trip.test.ts
      provides: "CAN-07 acceptance test — three reference contracts + smoketest-trivial round-trip cleanly"
  key_links:
    - from: examples/contracts/round-trip.test.ts
      to: plugin/src/codec/contract-codec.ts
      via: "imports parseYaml + emitYaml; tests run via vitest from repo root"
      pattern: "from \"../../plugin/src/codec/contract-codec"
    - from: examples/contracts/meeting-prep.contract
      to: evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml
      via: "byte-comparable after canonical emit per CAN-06"
      pattern: "name.*meeting-prep"
---

<objective>
Land the three reference `.contract` files and the CAN-07 acceptance test that asserts byte-comparable round-trip with the Phase 6 reference YAMLs. Wave-3-parallel with 07-05 (editor view) and the chrome plans — depends only on the codec (07-02).

Purpose: Lock the round-trip contract; CAN-06 + CAN-07 close in the same test file.
Output: Three `.contract` JSON files + one acceptance test that runs in vitest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md
@.planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md
@.planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md
@docs/v2/adr/007-contract-editor.md
@plugin/src/codec/contract-codec.ts
@plugin/src/codec/canonicalize.ts
@plugin/src/codec/editor-state-comment.ts
@evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml
@evals/fixtures/v2-test-vault/_contracts/project-status.yaml
@evals/fixtures/v2-test-vault/_contracts/code-review-brief.yaml
@evals/fixtures/v2-test-vault/_contracts/smoketest-trivial.yaml
@src/contracts/reference-contracts.test.ts

<interfaces>
From plugin/src/codec/contract-codec.ts (07-02):
```typescript
export function emitYaml(file: ContractDocumentShape): string
export function parseYaml(text: string): ContractDocumentShape
```

CAN-07 contract from CONTEXT.md D-CANON-TEST:
> For each of the three reference contracts (meeting-prep, project-status, code-review-brief):
> `import yaml → emit .contract → emit yaml` produces YAML that parses to a JS value
> `deepEqual` to the original AND the editor-state comment block survives a second cycle.
> Test lives in `examples/contracts/round-trip.test.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Generate the three reference `.contract` files from Phase 6 YAML fixtures</name>
  <files>examples/contracts/meeting-prep.contract, examples/contracts/project-status.contract, examples/contracts/code-review-brief.contract</files>
  <read_first>
    - evals/fixtures/v2-test-vault/_contracts/meeting-prep.yaml (source content)
    - evals/fixtures/v2-test-vault/_contracts/project-status.yaml (source content)
    - evals/fixtures/v2-test-vault/_contracts/code-review-brief.yaml (source content)
    - plugin/src/codec/contract-codec.ts (parseYaml derives the .contract shape)
    - plugin/src/codec/canonicalize.ts (canonical key order)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-REFS" (re-author the three Phase 6 reference contracts as .contract files)
    - .planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md "examples/contracts/{meeting-prep,project-status,code-review-brief}.contract"
    - examples/contracts/meeting-prep.contract (the 07-01 spike fixture — meeting-prep already exists; only project-status + code-review-brief are NEW; meeting-prep may need regeneration to match the same shape)
  </read_first>
  <action>
    Author a short Node script `examples/contracts/_generate.mjs` (or run the conversion inline via a one-shot `node -e`) that for each of the three YAML fixtures: (1) reads the YAML, (2) calls the bundled codec's `parseYaml` — but since the codec lives under `plugin/src/codec/` and is TypeScript, use the codec's source via `tsx` OR re-implement the equivalent transformation in plain JS for this generation step (recommended: write a small `.mjs` that does `import('yaml').then(...)` and emits the `.contract` JSON shape per D-FORMAT-SCHEMA). For each file, the resulting `.contract` JSON shape is `{ "$schema": "https://vault-memory.dev/schemas/contract-v1.json", "vmFormatVersion": 1, "contract": <parsed yaml as JS value>, "editor": { "nodes": <default LTR layout>, "selection": null, "viewport": {"x":0,"y":0,"zoom":1.0}, "yamlComments": {} } }` where `editor.nodes` uses the layout helper rules (one node per assembly step, id `step:<as>`, x = column * 260, y = row * 160). Pretty-print with 2-space indent. Confirm the 07-01-generated `meeting-prep.contract` matches the new convention; regenerate if not. Delete the generator script after use (it is one-shot, not committed) OR keep it as `examples/contracts/_generate.mjs` clearly marked one-shot regeneration tool — pick the lower-friction path; the test in Task 2 is the canonical regression check.
  </action>
  <verify>
    <automated>test -f examples/contracts/meeting-prep.contract && test -f examples/contracts/project-status.contract && test -f examples/contracts/code-review-brief.contract && node -e "for (const n of ['meeting-prep','project-status','code-review-brief']) { const j=require('./examples/contracts/'+n+'.contract'); if(j.vmFormatVersion!==1) throw new Error(n+' vmFormatVersion mismatch'); if(j.contract.name!==n) throw new Error(n+' contract.name mismatch'); if(j.editor.nodes.length!==j.contract.assembly.length) throw new Error(n+' editor.nodes count mismatch'); }"</automated>
  </verify>
  <acceptance_criteria>
    - `examples/contracts/meeting-prep.contract` exists, parses as JSON, `vmFormatVersion === 1`, `contract.name === "meeting-prep"`, `editor.nodes.length === contract.assembly.length`.
    - Same for `project-status.contract` and `code-review-brief.contract` (substituting the respective `name`).
    - Each file is pretty-printed (2-space indent — verifiable by `wc -l` returning >20).
    - `node examples/contracts/round-trip.test.ts` (if vitest entry) OR `npx vitest run examples/contracts/round-trip.test.ts` (after task 2) eventually passes — for this task, only the JSON shape is asserted.
  </acceptance_criteria>
  <done>Three reference `.contract` files exist with the standard wrapper; the round-trip test in task 2 will pin their content.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author CAN-07 round-trip acceptance test</name>
  <files>examples/contracts/round-trip.test.ts</files>
  <read_first>
    - src/contracts/reference-contracts.test.ts (analog test layout — same fixture iteration pattern)
    - plugin/src/codec/contract-codec.ts (parseYaml + emitYaml — under test)
    - plugin/src/codec/canonicalize.ts (the deterministic-emit guarantees the test relies on)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-CANON-TEST" (the exact test contract)
    - .planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md §"Code Examples" Ex. 4 (test scaffold)
    - vitest.config.ts (root — confirm `examples/` is included in the test glob; if not, this task updates the glob OR adds an explicit entry in `vitest.workspace.ts`)
    - evals/fixtures/v2-test-vault/_contracts/smoketest-trivial.yaml (additional minimal round-trip fixture)
  </read_first>
  <behavior>
    - For each of `meeting-prep.yaml`, `project-status.yaml`, `code-review-brief.yaml`, `smoketest-trivial.yaml`:
      - `parseYaml(originalYaml) → emitYaml → parseYaml → emitYaml` is a fixed-point: the third and fourth emissions are byte-identical.
      - `parseDocument(round1Yaml).toJS()` `deepEqual` `parseDocument(round2Yaml).toJS()`.
      - The editor-state comment block (`# vm-editor-state: <base64>`) is present at the head of every emitted YAML.
      - After two cycles, the base64-decoded editor state from the second cycle is structurally equal to the editor state from the first cycle.
    - For the three reference contracts specifically (CAN-06): the corresponding `.contract` file at `examples/contracts/<name>.contract` parses (via `ContractDocumentSchema.parse`) and its `contract` block deepEqual's the YAML's parsed JS value.
  </behavior>
  <action>
    Create `examples/contracts/round-trip.test.ts` following the analog at `src/contracts/reference-contracts.test.ts`. Use `node:fs/promises` for fixture reads (lint carve-out documented in the analog file). Iterate the four fixtures via a single `describe`/`for (const name of FIXTURES) it(...)` shape. Imports: `parseYaml`, `emitYaml` from `../../plugin/src/codec/contract-codec.js`; `parseDocument` from `yaml`; `ContractDocumentSchema` from `../../src/contracts/contract-file-schema.js`. Confirm the root `vitest.config.ts` (or `vitest.workspace.ts`) glob picks up `examples/**/*.test.ts`; if not, extend the include glob in a minimal additive change. The CAN-06 assertions iterate over the three reference contracts and confirm each `.contract` file's `contract` block matches the corresponding YAML.
  </action>
  <verify>
    <automated>npx vitest run examples/contracts/round-trip.test.ts --reporter=basic</automated>
  </verify>
  <acceptance_criteria>
    - `examples/contracts/round-trip.test.ts` exists.
    - The test file contains a `FIXTURES` constant listing the four fixture paths (3 reference + 1 smoketest-trivial).
    - The test asserts byte-identity between the 3rd and 4th `emitYaml` outputs (fixed-point).
    - The test asserts `parseDocument(round1).toJS()` `deepEqual` `parseDocument(round2).toJS()` for each fixture.
    - The test asserts every emitted YAML starts with the literal `"# vm-editor-state: "`.
    - The test contains at minimum 4 separate test cases (one per fixture) plus the three CAN-06 per-`.contract`-file assertions.
    - `npx vitest run examples/contracts/round-trip.test.ts` exits 0.
    - The root vitest config glob includes `examples/**/*.test.ts` — `npm test` from the repo root runs this file as part of the full suite.
  </acceptance_criteria>
  <done>CAN-07 acceptance is closed; the round-trip is a regression-protected invariant; CAN-06's three reference files are pinned by deepEqual against the Phase 6 YAML fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.contract` fixtures live in the public repo | Reference content visible to all users; must not contain secrets |
| Round-trip test reads from `evals/fixtures/v2-test-vault/` | Existing public fixture data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Information Disclosure | `.contract` fixtures | accept | Fixtures replicate the public Phase 6 reference contracts; no new sensitive data introduced |
| T-07-06-02 | Tampering | Generator script (if kept) | accept | One-shot regeneration; canonical regression check is the test itself, not the generator |
</threat_model>

<verification>
- `npx vitest run examples/contracts/round-trip.test.ts --reporter=basic` exits 0.
- `npm test --silent 2>&1 | grep -E "(round-trip|reference contracts)" | head -5` shows the round-trip test ran in the full suite.
- Three `.contract` files exist; each pretty-printed JSON; each has `vmFormatVersion: 1` and a populated `editor.nodes` array.
</verification>

<success_criteria>
- CAN-06: Three reference `.contract` files ship; each matches its Phase 6 YAML twin after canonicalization.
- CAN-07: Round-trip is a regression-pinned fixed-point; YAML comments preserved; editor-state survives two cycles.
- The acceptance test is included in the root `npm test` run.
</success_criteria>

<output>
Create `.planning/phases/07-visual-contract-editor-canvas/07-06-SUMMARY.md` when done. Summary includes: fixture list (4); deepEqual + fixed-point + comment-preservation assertions count; vitest glob update note if any; explicit confirmation that meeting-prep was regenerated to match the new convention if it had drifted from the 07-01 spike fixture.
</output>
