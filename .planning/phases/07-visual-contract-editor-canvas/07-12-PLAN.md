---
phase: 07-visual-contract-editor-canvas
plan: 12
type: execute
wave: 5
depends_on: ["07-05", "07-08", "07-09", "07-10"]
files_modified:
  - docs/v2/plugin/INSTALL.md
  - docs/v2/plugin/SETTINGS.md
  - docs/v2/plugin/SECRETS.md
  - docs/v2/plugin/CONTRACT-EDITOR.md
  - docs/v2/plugin/CONNECTORS.md
  - docs/v2/plugin/README.md
  - README.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CAN-09]
must_haves:
  truths:
    - "Five plugin documentation files exist under `docs/v2/plugin/` matching CONTEXT D-DOCS-SET (INSTALL, SETTINGS, SECRETS, CONTRACT-EDITOR, CONNECTORS)"
    - "Each doc covers one user task end-to-end; tone matches `docs/v2/AGENT_AGNOSTIC.md` (terse, technical, second person)"
    - "README.md mentions the plugin install path via `vm-install` skill and links to `docs/v2/plugin/INSTALL.md`"
    - "Screencast `.mp4` ≤8 minutes covers install → first contract authored → first `instantiate_contract` call; published as a GitHub Release asset; linked from README and INSTALL.md — OR explicitly deferred to Phase 8 with the deferral recorded in STATE.md + ROADMAP.md"
  artifacts:
    - path: docs/v2/plugin/INSTALL.md
      provides: "vm-install skill instructions + manual sideload fallback (`.obsidian/plugins/vault-memory/` layout)"
    - path: docs/v2/plugin/SETTINGS.md
      provides: "Every settings knob documented with restart-required vs hot-swappable flagged; hot-swappable settings explicitly noted as in-memory-only across server restarts (config-file is authoritative)"
    - path: docs/v2/plugin/SECRETS.md
      provides: "Schlüsselbund integration; ${secret:name} reference syntax; per-device ciphertext implications; basic_text Linux fallback"
    - path: docs/v2/plugin/CONTRACT-EDITOR.md
      provides: "Walkthrough — open a .contract, use palette, edit inspector forms, save → see .yaml emitted"
    - path: docs/v2/plugin/CONNECTORS.md
      provides: "Adding peer-MCP servers, testing connections, referencing secrets"
  key_links:
    - from: README.md
      to: docs/v2/plugin/INSTALL.md
      via: "README section 'Obsidian plugin' links to install docs"
      pattern: "docs/v2/plugin/INSTALL.md"
    - from: docs/v2/plugin/INSTALL.md
      to: skills/vm-install/SKILL.md
      via: "INSTALL.md references the vm-install skill command verbatim"
      pattern: "vm-install"
---

<objective>
Close the CAN-09 documentation half: ship the five plugin docs under `docs/v2/plugin/` and produce the ≤8-minute screencast hosted on GitHub Releases. README updates to surface the new install path. Wave 4 — depends on every feature plan being complete so docs describe the shipped state.

Purpose: A user landing on README can install + use + configure the plugin without reading any other file.
Output: 5 markdown docs (D-DOCS-SET), 1 README amendment, 1 screencast (production tracked via the human checkpoint; deferral path explicitly bookkept in STATE/ROADMAP if recording cannot happen in this session).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md
@.planning/phases/07-visual-contract-editor-canvas/07-RESEARCH.md
@.planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md
@docs/v2/adr/007-contract-editor.md
@docs/v2/AGENT_AGNOSTIC.md
@docs/v2/ARCHITECTURE.md
@README.md
@plugin/manifest.json
@skills/vm-install/SKILL.md
@skills/vm-update/SKILL.md

<interfaces>
Documentation source-of-truth pointers:
- INSTALL.md: skills/vm-install/SKILL.md + setup.sh shape (07-11)
- SETTINGS.md: plugin/src/services/settings-store.ts DEFAULT_SETTINGS + restart-required mapping (07-03/07-08); hot-swappable in-memory-only note (07-04 task 2)
- SECRETS.md: plugin/src/services/safe-storage.ts + secrets-store.ts (07-08)
- CONTRACT-EDITOR.md: plugin/src/views/contract-editor/* (07-05) + examples/contracts/*.contract (07-06)
- CONNECTORS.md: plugin/src/chrome/connectors-panel.svelte (07-10) + src/plugin-tools/set-mcp-client.ts (07-04)

Doc tone reference: docs/v2/AGENT_AGNOSTIC.md (terse, technical, second-person, no marketing language)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the five plugin docs + README.md</name>
  <files>docs/v2/plugin/INSTALL.md, docs/v2/plugin/SETTINGS.md, docs/v2/plugin/SECRETS.md, docs/v2/plugin/CONTRACT-EDITOR.md, docs/v2/plugin/CONNECTORS.md, docs/v2/plugin/README.md</files>
  <read_first>
    - docs/v2/AGENT_AGNOSTIC.md (tone reference)
    - docs/v2/ARCHITECTURE.md (layer model — plugin = L5)
    - skills/vm-install/SKILL.md + setup.sh (07-11)
    - skills/vm-update/SKILL.md + update.sh (07-11)
    - plugin/src/services/settings-store.ts DEFAULT_SETTINGS (07-03)
    - plugin/src/services/safe-storage.ts (07-08 — basic_text caveat)
    - plugin/src/views/contract-editor/* (07-05 — palette + canvas + inspector behaviors)
    - examples/contracts/meeting-prep.contract (the reference fixture the editor walkthrough uses)
    - plugin/src/chrome/connectors-panel.svelte (07-10)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-DOCS-SET"
    - .planning/phases/07-visual-contract-editor-canvas/07-PATTERNS.md "docs/v2/plugin/{INSTALL,SETTINGS,SECRETS,CONTRACT-EDITOR,CONNECTORS}.md"
  </read_first>
  <action>
    Write each doc as a one-paragraph What/Why opener, a feature-by-feature table or list of behaviors, then a "Known limitations" / "Gotchas" section. Match the AGENT_AGNOSTIC tone (no "easily", "simply", "just"; no marketing language).

    `INSTALL.md`: Sections — "Overview" (plugin is L5 user surface for vault-memory); "Install via vm-install skill" (primary path; commands; what the skill does in 7 steps verbatim from SKILL.md); "Manual sideload" (download tarball from GitHub Releases; extract to `<vault>/.obsidian/plugins/vault-memory/`; enable in Settings → Community Plugins; set `[plugin] enabled = true` in `~/.vault-memory/config.toml`); "Update via vm-update skill"; "Uninstall" (delete the plugin directory; flip the config flag); "Compatibility" (Obsidian ≥ 1.5.0; desktop only — `isDesktopOnly: true`; macOS / Windows / Linux); "Sync substrate caveats" (Syncthing / iCloud / git-sync sync the plugin's data.json including ciphertext but not the keyring; per-device re-entry of secrets is required per RESEARCH §Pitfall 3 + §Runtime State Inventory).

    `SETTINGS.md`: Table of every settings key (one row per `VaultMemorySettings` field) with columns `Key`, `Default`, `Description`, `Restart-required`. Sections — "Hot-swappable settings" (reranker on/off, default vault, indexer batch size — describe what changes immediately); MUST explicitly call out that hot-swappable settings are in-memory-only across server restarts; the config file at `~/.vault-memory/config.toml` is the authoritative source of record; restarting the server reverts hot-swap mutations to the file's values. "Restart-required settings" (Ollama URL, embedding model, FTS tokenizer, server command — explain why these need a restart). "Advanced section" callout.

    `SECRETS.md`: Sections — "Overview" (safeStorage / Schlüsselbund); "Adding a secret" (UI walkthrough with `data-testid` references); "Referencing secrets" (`${secret:name}` syntax with regex); "Per-device ciphertext" (sync caveat verbatim from RESEARCH); "Linux backend caveats" (basic_text fallback; recommendation to install gnome-libsecret/kwallet); "Server-side resolution" (the `resolve_secret` MCP tool path; safeStorage decryption performed in the plugin process; plaintext passed to the server over local stdio for substitution; on `safe_storage_unavailable` user is prompted to re-enter); "Uninstall leaves keyring entries" (RESEARCH §Runtime State Inventory note).

    `CONTRACT-EDITOR.md`: Sections — "Overview" (Variant C three-pane editor; `.contract` JSON vs `.yaml` build artifact); "Opening a contract" (open `.contract` file → editor launches automatically via `registerExtensions`); "Palette" (5 sections: type catalog, read verbs, assembly verbs, escape (`literal`), peer-MCP dynamic); "Canvas" (drag from palette → adds a step; node positions persist in `editor.nodes`); "Inspector" (Zod-schema-derived form; alias-ref autocomplete); "Save" (writes both `.contract` and `_contracts/<name>.yaml`; SuppressionSet ensures no echo loop); "External edits" (`vault-memory://contracts/reloaded` notification surfaces a Modal prompt); end with a step-by-step walkthrough using `examples/contracts/meeting-prep.contract`.

    `CONNECTORS.md`: Sections — "Overview" (peer-MCP clients declared under `[contracts.mcp_clients]`; plugin reads/writes via `set_mcp_client` MCP tool — including the `{list: true}` inventory-read variant; server is the config authority); "Adding a connector" (form fields); "Referencing secrets" (cross-link to SECRETS.md `${secret:name}` syntax); "Test connection" (server attempts Client.connect()); "Removing a connector" (confirms then mutates); "Cloud-source connectors" (deferred to Phase 10/v3 — placeholder note).

    `docs/v2/plugin/README.md`: Brief index pointing at the five docs.

    Each doc opens with a YAML-style header block: `> Phase 7 / v2.0.0 — vault-memory Obsidian plugin / Last verified: <date>`.

    Total doc count: 5 + 1 index = 6 files under `docs/v2/plugin/`.
  </action>
  <verify>
    <automated>test -f docs/v2/plugin/INSTALL.md && test -f docs/v2/plugin/SETTINGS.md && test -f docs/v2/plugin/SECRETS.md && test -f docs/v2/plugin/CONTRACT-EDITOR.md && test -f docs/v2/plugin/CONNECTORS.md && test -f docs/v2/plugin/README.md && grep -q "vm-install" docs/v2/plugin/INSTALL.md && grep -q "Restart-required" docs/v2/plugin/SETTINGS.md && grep -q "in-memory" docs/v2/plugin/SETTINGS.md && grep -q "safeStorage" docs/v2/plugin/SECRETS.md && grep -q "Palette" docs/v2/plugin/CONTRACT-EDITOR.md && grep -q "set_mcp_client" docs/v2/plugin/CONNECTORS.md</automated>
  </verify>
  <acceptance_criteria>
    - All five required docs (INSTALL, SETTINGS, SECRETS, CONTRACT-EDITOR, CONNECTORS) plus the index README exist.
    - INSTALL.md references `vm-install` and `vm-update` skills.
    - SETTINGS.md contains a table with at minimum one row per `VaultMemorySettings` key.
    - SETTINGS.md explicitly states that hot-swappable settings (reranker on/off, default vault, indexer batch size) are in-memory-only across server restarts; the config file is authoritative.
    - SECRETS.md references `safeStorage`, `${secret:name}`, and the per-device ciphertext caveat.
    - CONTRACT-EDITOR.md references the five palette sections and the SuppressionSet.
    - CONNECTORS.md references `set_mcp_client` and cross-links to SECRETS.md.
    - No doc uses the words "easily", "simply", "just", "blazingly", or "magnificent" (project tone constraint).
  </acceptance_criteria>
  <done>The doc set is complete; a new user can read INSTALL.md → install via vm-install → open `examples/contracts/meeting-prep.contract` and follow CONTRACT-EDITOR.md to author their first contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update README.md with plugin section</name>
  <files>README.md</files>
  <read_first>
    - README.md (current content — Phase 1 already rewrote it to "any MCP-aware agent" framing)
    - docs/v2/plugin/INSTALL.md (task 1 — link target)
    - .planning/phases/07-visual-contract-editor-canvas/07-CONTEXT.md "D-SCREENCAST" (link to GitHub Release-hosted .mp4)
  </read_first>
  <action>
    Add a new section to README.md titled `## Obsidian plugin (Phase 7 / v2.0.0)`. Content: one-paragraph What/Why; install path "Run `/vm-install` in Claude Code (or any MCP-skill-aware client) — see [docs/v2/plugin/INSTALL.md](docs/v2/plugin/INSTALL.md) for full installation + manual sideload steps."; capability summary (visual contract editor, settings, secrets, reindex, stats, connectors); screencast link placeholder (`[Walkthrough screencast](https://github.com/owrede/vault-memory/releases/download/v2.0.0/vault-memory-plugin-walkthrough.mp4)` — task 3 confirms or replaces the exact URL, or marks the link as "pending Phase 8" if the screencast is deferred). Insert this section AFTER the existing "What it does" and "Quickstart" sections, BEFORE the "Architecture / docs" links section. Do not modify existing README content beyond inserting this section.
  </action>
  <verify>
    <automated>grep -q "## Obsidian plugin" README.md && grep -q "vm-install" README.md && grep -q "docs/v2/plugin/INSTALL.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a new H2 section starting with `## Obsidian plugin`.
    - README.md references `vm-install` (the install skill name).
    - README.md links to `docs/v2/plugin/INSTALL.md`.
    - README.md contains a `## Obsidian plugin` placeholder link to the screencast asset (which task 3 confirms exists or marks as pending).
  </acceptance_criteria>
  <done>Discoverability — a reader landing on the repo README sees the plugin path within the first scroll.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Record + publish the ≤8-minute screencast (or defer to Phase 8)</name>
  <what-built>
    - Five plugin docs at `docs/v2/plugin/`
    - README.md updated with plugin section linking to INSTALL.md and the screencast
    - Plugin v2.0.0 GitHub Release tag (pending Phase 8) will host the `.mp4`
  </what-built>
  <how-to-verify>
    Manual-only action — Claude cannot record a screencast:
    1. Set up a clean Obsidian vault on the recording machine.
    2. Record screen (Loom, OBS, or QuickTime) following this storyboard (target ≤8 minutes):
       - 0:00–1:00 — Install via `/vm-install` skill (show Claude Code invocation + the 7-checkpoint output)
       - 1:00–2:00 — Open Obsidian → enable plugin in Settings → Community Plugins → plugin loads
       - 2:00–4:00 — Open `examples/contracts/meeting-prep.contract` in Obsidian; show the three-pane editor; drag a verb from palette; edit a field in the inspector; save; show the emitted `_contracts/meeting-prep.yaml`
       - 4:00–5:30 — Settings tab tour; secrets panel (add a fake secret); reindex (show progress bar); stats panel (refresh)
       - 5:30–7:00 — Add a peer-MCP connector; test connection
       - 7:00–8:00 — Run `instantiate_contract` (from Claude or MCP Inspector) against the authored contract; show the result
    3. Trim + compress to ≤8 minutes.
    4. When v2.0.0 is tagged (Phase 8), upload as a release asset named `vault-memory-plugin-walkthrough.mp4`.
    5. Confirm the URL embedded in README.md resolves (post-tag); update if the asset name differs.
    6. Type "approved" to mark CAN-09 docs half complete.

    **Deferral path:** If the screencast cannot be recorded in this session, type "deferred to phase 8" — the executor MUST then perform two bookkeeping edits before exiting:
    1. Add a line to the Phase 7 section of `.planning/STATE.md`: `- CAN-09 docs complete; screencast deferred to Phase 8 (REL-XX bookkeeping)`.
    2. Add a backlog line to the Phase 8 section of `.planning/ROADMAP.md` (or, if `.planning/BACKLOG.md` exists, to that file instead): `- [ ] Phase 8 backlog: ≤8min screencast covering install → first contract → first instantiate_contract (CAN-09 carryover from Phase 7)`.
    These edits are required for the deferred path; without them the deferral is incomplete and CAN-09 is not closeable.
  </how-to-verify>
  <resume-signal>Type "approved" with the GitHub Release asset URL when the screencast is published, OR type "deferred to phase 8" to mark the recording as a Phase 8 task and proceed (executor will apply the STATE.md + ROADMAP.md bookkeeping edits before resuming).</resume-signal>
  <acceptance_criteria>
    - Recorded path: GitHub Release asset URL provided; README.md screencast link resolves to a real `.mp4`.
    - Deferred path: `.planning/STATE.md` Phase 7 section contains the line `CAN-09 docs complete; screencast deferred to Phase 8`; `.planning/ROADMAP.md` Phase 8 section (or `.planning/BACKLOG.md`) contains the backlog line for the ≤8min screencast; both edits committed.
    - Either path: CAN-09 docs half is closed (the five docs + README section are merged regardless of screencast status).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public-facing documentation | Visible to all users; must not leak fixture vault PII or maintainer credentials |
| Screencast `.mp4` | If recorded against the maintainer's vault, may surface vault content on screen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Information Disclosure | Documentation examples | accept | Examples use the public `evals/fixtures/v2-test-vault/` content; no real vault data referenced |
| T-07-12-02 | Information Disclosure | Screencast recording | mitigate | Storyboard requires recording against a clean vault, not the maintainer's daily vault |
</threat_model>

<verification>
- All 5 docs + 1 index README exist under `docs/v2/plugin/`.
- README.md contains the `## Obsidian plugin` section.
- Manual: screencast exists OR is explicitly deferred to Phase 8 with STATE.md + ROADMAP.md bookkeeping edits applied.
- Tone check: no marketing-language words present in any of the new docs.
</verification>

<success_criteria>
- CAN-09 documentation half closes: 5 docs + screencast (or deferred-to-Phase-8 explicit note in STATE.md + ROADMAP.md).
- A reader landing on the repo can install + use + configure the plugin from the docs alone.
- The doc set is internally cross-linked; SECRETS ↔ CONNECTORS ↔ CONTRACT-EDITOR are linked where they reference each other's concepts.
</success_criteria>

<output>
Create `.planning/phases/07-visual-contract-editor-canvas/07-12-SUMMARY.md` when done. Summary includes: doc file list (6 files); README section title + link target; screencast status (recorded with URL / deferred to Phase 8 with STATE.md + ROADMAP.md bookkeeping); list of any cross-references between docs.
</output>
