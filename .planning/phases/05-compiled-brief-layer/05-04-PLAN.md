---
phase: 05-compiled-brief-layer
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - src/brief/resources.ts
  - src/brief/resources.test.ts
  - src/memory/resources/index.ts
  - src/brief/index.ts
  - src/server.ts
  - src/adapters/source/conformance.test.ts
  - evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml
  - evals/v1-baseline/tools-list.snapshot.json
  - docs/v2/PHASE-5-SIGN-OFF.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - BRF-09
  - BRF-11
user_setup: []

must_haves:
  truths:
    - "`src/brief/resources.ts` implements `readListBriefs(deps, opts?)` as a pure function over `MemorySinkRegistry` + `VaultManager` + `SourceConnector`; enumerates briefs via `SourceConnector.listDocuments({pathPrefix: '_memory/_briefs/'})` (no raw fs); filters by `properties.type === 'brief'`; projects to `ListBriefEntry`."
    - "Resource registered in `src/server.ts` via `server.registerResource('briefs', RESOURCE_URI_LIST_BRIEFS, {...}, handler)` mirroring `src/memory/resources/list-sinks.ts` registration at `src/server.ts:1022-1041`."
    - "Resource URI shape: `vault-memory://briefs` (per slice 1 constant); optional `?target=<pattern>` query parameter filters by target slug substring."
    - "Resource response shape: `{total: number, briefs: ListBriefEntry[]}` where each entry is `{doc_id, target, purpose, compiled_at, status, source_count, age_days, vault}`."
    - "`src/adapters/source/conformance.test.ts` extended with brief + staleness assertions, parametric over `obsidian-fs` AND `StubSource` (BRF-11 cross-adapter source-neutrality proof). Same daemon handler processes events from both."
    - "`briefs-staleness-stub.yaml` populated with the staleness scenario replayed against the stub `ChangeFeed`."
    - "`evals/v1-baseline/tools-list.snapshot.json` regenerated additively: +2 tool entries (`compile_brief`, `get_brief`). `list_briefs` is a Resource and stays OUT of the tools snapshot (per VALIDATION.md §Manual-Only Verifications)."
    - "`docs/v2/PHASE-5-SIGN-OFF.md` documents: all 11 BRF requirements green; tool count 32 → 34 (+1 Resource); Phase 8 retirement plan for REL-08 (≤32 tools) — promote 2 v1 tools to Resources."
    - "`.planning/ROADMAP.md` `Phase 5: Compiled brief layer` checkbox marked complete with a sign-off summary line."
    - "All 1211+ tests stay green; new tests for resources + conformance extension green."
  artifacts:
    - path: "src/brief/resources.ts"
      provides: "readListBriefs(deps, opts?) → ListBriefsResource (pure read-only)"
      contains: "readListBriefs"
    - path: "src/adapters/source/conformance.test.ts"
      provides: "Brief + staleness conformance suite parametric over obsidian-fs + StubSource (BRF-11)"
      contains: "compile_brief + staleness"
    - path: "evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml"
      provides: "BRF-11 cross-adapter eval — same staleness scenario as briefs-curated, replayed against stub ChangeFeed"
      contains: "adapters:"
    - path: "evals/v1-baseline/tools-list.snapshot.json"
      provides: "Tool surface snapshot regenerated (additive: +compile_brief, +get_brief; list_briefs Resource not in tools snapshot)"
      contains: "compile_brief"
    - path: "docs/v2/PHASE-5-SIGN-OFF.md"
      provides: "Phase 5 sign-off — all 11 BRF green; tool count 32 → 34; Phase 8 REL-08 retirement plan"
      contains: "Phase 5 Sign-Off"
  key_links:
    - from: "src/server.ts"
      to: "src/brief/resources.ts"
      via: "server.registerResource('briefs', RESOURCE_URI_LIST_BRIEFS, ..., handler)"
      pattern: "registerResource"
    - from: "src/adapters/source/conformance.test.ts"
      to: "src/brief/daemon.ts"
      via: "shared daemon handler processes events from both obsidian-fs + stub ChangeFeed"
      pattern: "BriefStalenessDaemon"
    - from: "docs/v2/PHASE-5-SIGN-OFF.md"
      to: ".planning/ROADMAP.md"
      via: "ROADMAP checkbox + summary line update on sign-off"
      pattern: "Phase 5"
---

<objective>
Phase gate slice. Ship the last remaining BRF surface (`list_briefs` MCP Resource), prove source-neutrality via cross-adapter conformance (BRF-11), regenerate the tool-list snapshot additively, and produce the Phase 5 sign-off document. After this slice, all 11 BRF requirements are green, the tool surface lifts 32 → 34 with the Phase 8 retirement plan documented, and ROADMAP Phase 5 is checked off.

Purpose: BRF-09 (list_briefs as MCP Resource not Tool) + BRF-11 (cross-adapter source-neutrality — same staleness scenario passes against stub ChangeFeed). Plus the phase gate: snapshot regen, sign-off doc, ROADMAP update. Marked `autonomous: false` because the snapshot regen requires a human-reviewed diff per VALIDATION.md §Manual-Only Verifications.

Output: `list_briefs` Resource registered; conformance suite extended with brief + staleness assertions over obsidian-fs + stub; `briefs-staleness-stub.yaml` populated; tools-list snapshot regenerated; PHASE-5-SIGN-OFF.md authored; ROADMAP checkbox flipped. All 11 BRF requirements green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-compiled-brief-layer/05-CONTEXT.md
@.planning/phases/05-compiled-brief-layer/05-RESEARCH.md
@.planning/phases/05-compiled-brief-layer/05-PATTERNS.md
@.planning/phases/05-compiled-brief-layer/05-VALIDATION.md
@.planning/phases/05-compiled-brief-layer/05-01-PLAN.md
@.planning/phases/05-compiled-brief-layer/05-02-PLAN.md
@.planning/phases/05-compiled-brief-layer/05-03-PLAN.md
@docs/v2/adr/005-brief-compile-strategy.md
@docs/v2/PHASE-4-SIGN-OFF.md
@docs/v2/MEMORY_CONTRACT.md
@src/types.ts
@src/memory/resources/list-sinks.ts
@src/memory/resources/index.ts
@src/server.ts
@src/adapters/source/conformance.test.ts
@src/adapters/source/types.ts
@src/brief/index.ts
@src/brief/daemon.ts
@evals/v1-baseline/tools-list.snapshot.json

<interfaces>
<!-- Canonical contracts. -->

From src/memory/resources/list-sinks.ts:1-55 — direct analog. readListSinks(deps) → ListSinksResource. Pure function over registry + manager. NO raw fs, NO gray-matter — read-only enumeration through adapters.

From src/server.ts:1022-1041 — Resource registration pattern (mirror verbatim for list_briefs).

From src/memory/resources/index.ts:21 (slice 1 added) — RESOURCE_URI_LIST_BRIEFS constant.

From src/adapters/source/types.ts:130 — SourceConnector.listDocuments(opts) for enumerating _memory/_briefs/*.md. No raw fs in src/brief/resources.ts.

From src/adapters/source/conformance.test.ts (lines 14-58) — describe.each parametric setup over multiple adapters. New brief + staleness suite plugs into the same describe.each so both adapters run.

From src/adapters/stub/assembly-fixture.js — stub fixture with ATLAS_0_DOC_ID, MEMORY_INNER_DOC_ID, etc.

From evals/v1-baseline/tools-list.snapshot.json — current 32-entry snapshot. Phase 5 diff: additive +2 (compile_brief, get_brief). list_briefs is a Resource — not in this snapshot.

From .planning/ROADMAP.md — Phase 5 section with success criteria. On sign-off, the checkbox flips and a one-line summary lands.

From docs/v2/PHASE-4-SIGN-OFF.md — structural analog for PHASE-5-SIGN-OFF.md: BRF-by-BRF status table, tool-count delta, Phase 8 hand-off note.

REL-08 (REQUIREMENTS.md): tool count ≤ 32 — Phase 8 promotes 2 v1 tools to Resources to compensate Phase 5's lift to 34. Documented in sign-off.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 5-04-01: list_briefs MCP Resource (BRF-09)</name>
  <files>src/brief/resources.ts, src/brief/resources.test.ts, src/brief/index.ts, src/server.ts</files>
  <behavior>
    - Test 1: readListBriefs against a vault with zero briefs returns `{total: 0, briefs: []}`.
    - Test 2: After compiling 3 briefs with distinct targets, readListBriefs returns `{total: 3, briefs: [...]}` with one entry per brief; entry fields are `{doc_id, target, purpose, compiled_at, status, source_count, age_days, vault}`.
    - Test 3: Entries with `properties.status === "superseded"` ARE included by default (resource is a discovery surface; callers filter client-side). The `status` field is exposed so callers can build their own filter; this matches the "let agents see the chain" stance documented in CONTEXT discretion.
    - Test 4: readListBriefs with `{target: "atlas"}` filters to briefs whose target slug substring-matches "atlas".
    - Test 5: `source_count` is computed from `vault.db.briefSources.sourcesForBrief(briefId).length` (consistent with the reverse-index of record).
    - Test 6: `age_days` is `floor((Date.now() - Date.parse(compiled_at)) / 86400000)`.
    - Test 7: Resource registered in src/server.ts via `server.registerResource("briefs", RESOURCE_URI_LIST_BRIEFS, ..., handler)` mirroring src/memory/resources/list-sinks.ts pattern.
    - Test 8: URI `vault-memory://briefs?target=atlas` is parsed correctly inside the handler (handler receives the URL object; extract `target` from `uri.searchParams`).
    - Test 9: Resource response payload is `{contents: [{uri, mimeType: "application/json", text: JSON.stringify({total, briefs}, null, 2)}]}` matching list-sinks shape.
    - Test 10: No `fs`, `gray-matter`, `path.join`, or `chokidar` imports in src/brief/resources.ts (adapter-seam discipline).
  </behavior>
  <action>
    Create src/brief/resources.ts per 05-PATTERNS.md analog `src/memory/resources/list-sinks.ts` (entire file, ~55 lines). RESEARCH §"Pattern: Resource Registration" lines 715-753 has the sketch. Implementation:

    1. Imports: type-only from `../memory/registry.js`, `../vault/index.js`, `../adapters/source/types.js`. NO fs/path/gray-matter/chokidar.
    2. Define `ListBriefsResource`, `ListBriefEntry`, `ListBriefsOpts`, `ListBriefsDeps` interfaces per PATTERNS lines 393-407.
    3. `readListBriefs(deps, opts = {})` async function:
       - Resolve vault list: if `opts.vault` set, single-vault via manager.require; else manager.vaults().
       - For each vault: `const connector = deps.sourceConnectorFor(vault.config.name); const docs = await connector.listDocuments({pathPrefix: "_memory/_briefs/"})`.
       - For each doc: filter by `doc.properties.type === "brief"`; if `opts.target` set, filter by substring match on `doc.properties.target`.
       - Build entry: doc_id, target, purpose, compiled_at, status, vault from doc.properties; source_count from `vault.db.briefSources.sourcesForBrief(doc.doc_id).length`; age_days computed from compiled_at.
       - Return `{total: out.length, briefs: out}`.

    Register in src/server.ts (mirror existing list-sinks registration at :1022-1041). Add after existing resource registrations (around :1061):
    - Import `RESOURCE_URI_LIST_BRIEFS` from `./memory/resources/index.js` (slice 1 constant).
    - Import `readListBriefs` from `./brief/resources.js`.
    - Call `server.registerResource("briefs", RESOURCE_URI_LIST_BRIEFS, {title: "Compiled briefs", description: "Discovery of briefs by target. BRF-09.", mimeType: "application/json"}, async (uri) => { const target = uri.searchParams.get("target") ?? undefined; const payload = await readListBriefs({registry: memorySinkRegistry, manager, sourceConnectorFor: (n) => sourceConnectors.get(n)!}, {target}); return {contents: [{uri: uri.href, mimeType: "application/json", text: JSON.stringify(payload, null, 2)}]}; })`.

    Update src/brief/index.ts barrel to re-export `readListBriefs`, `ListBriefsResource`, `ListBriefEntry`.

    Co-locate src/brief/resources.test.ts with Behavior tests 1-10. Use stub MemorySinkRegistry + stub SourceConnector + in-memory DB pattern from Phase 2 test utilities. Adapter-seam discipline: zero `fs`/`path.join`/`gray-matter`/`chokidar` imports in resources.ts.
  </action>
  <verify>
    <automated>npx vitest run src/brief/resources.test.ts &amp;&amp; bash scripts/lint-adapters.sh &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <done>readListBriefs green; list_briefs Resource registered at vault-memory://briefs with optional ?target=&lt;pattern&gt;; full suite (npm test) stays green; lint-adapters zero hits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5-04-02: Cross-adapter conformance + briefs-staleness-stub.yaml (BRF-11 source-neutrality)</name>
  <files>src/adapters/source/conformance.test.ts, evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml</files>
  <behavior>
    - Test 1 (conformance — obsidian-fs): Inside the existing describe.each block in conformance.test.ts, a new `describe("compile_brief + staleness", ...)` suite: write a brief via delivery.write, modify a source doc, replay the ChangeEvent through the daemon handler, assert brief.status flipped to "stale" and changed_sources contains modified DocId.
    - Test 2 (conformance — stub): Same suite runs against StubSource (BRF-11 source-neutrality proof). Same assertions pass.
    - Test 3 (conformance — rename via stub): StubSource emits rename-equivalent events; daemon handler updates brief_sources.chunk_doc_id in place; brief stays `status: "active"` (BRF-08 over stub).
    - Test 4 (conformance — delete): StubSource emits a `{kind: "delete"}` for a source doc with no matching create within 5s; daemon marks affected briefs stale with `changed_sources: [<deleted_id>]`.
    - Test 5 (conformance — startup full scan): missed events while daemon was down are recovered by startup scan; brief flips stale on `daemon.start()` (BRF-07 over stub).
    - Test 6 (eval YAML): `briefs-staleness-stub.yaml` exists with events array (compile / modify_source / expect_stale entries) and `adapters: [obsidian-fs, stub]` parametric declaration.
  </behavior>
  <action>
    Extend src/adapters/source/conformance.test.ts per 05-PATTERNS.md analog (lines 718-728). Add a new `describe("compile_brief + staleness (Phase 5 / BRF-11)", () => { ... })` block INSIDE the existing `describe.each([...])` wrapper at the top of the file so both adapters run.

    Pattern (build on the existing test layout — verify exact existing shape during impl):
    - `beforeEach` builds an isolated vault via the parametric `buildVault` factory + a fresh feed via `buildChangeFeed(vault)`.
    - Construct delivery adapter + source connector for the test vault.
    - Instantiate `new BriefStalenessDaemon()` and `await daemon.start(vault, feed, deps)`.
    - `afterEach` calls `await daemon.shutdown()`.

    Test cases (Behavior tests 1-5 above):
    1. **flips brief to stale when source is modified**: Seed source docs (ATLAS_0_DOC_ID etc. from `src/adapters/stub/assembly-fixture.js`). Call `handleCompileBrief({target: "test", source_doc_ids: [ATLAS_0_DOC_ID], purpose: "...", prepared_text: "Test brief about [[Atlas 0]]"})` — use tier-3 prepared_text to avoid Sampling/Ollama dependencies in conformance tests. Modify ATLAS_0_DOC_ID via delivery.update (real obsidian-fs) or stub mutation. For the obsidian-fs adapter, wait for chokidar fan-out (use a small polling loop on `daemon_state.getCursor`). For stub, manually feed.emit the ChangeEvent. Assert `get_brief({target: "test"})` returns `{brief: null, stale: true, changed_sources: [ATLAS_0_DOC_ID], reason: "stale_blocked"}`.
    2. **preserves brief→source link on rename**: Compile brief over ATLAS_0_DOC_ID. Rename ATLAS_0_DOC_ID → ATLAS_0_RENAMED_DOC_ID (for stub: emit delete+create with matching chunk hashes within grace-window; for obsidian-fs: actual file rename). Wait for daemon's grace-window correlation. Assert `vault.db.briefSources.sourcesForBrief(briefId)` rows now reference ATLAS_0_RENAMED_DOC_ID and brief stays `status: "active"`.
    3. **marks brief stale on source delete**: Compile brief over ATLAS_0_DOC_ID. Delete ATLAS_0_DOC_ID with no matching create. Wait 6s (past 5s grace-window). Assert brief flips stale with `changed_sources: [ATLAS_0_DOC_ID]`.
    4. **startup full scan recovers missed events**: Compile brief. Shut down daemon. Modify source while daemon down (direct DB mutation simulating an external edit). Restart daemon. Assert startup full scan flips brief stale (BRF-07).

    Reuse existing fixture imports per `src/adapters/stub/assembly-fixture.js`.

    Populate `evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml` per 05-PATTERNS.md lines 626-647. The eval YAML is documentation + a regression hook; the actual cross-adapter enforcement lives in the conformance.test.ts extension above. Shape:

    File contents:
    ```yaml
    # BRF-11 cross-adapter source-neutrality. Same staleness scenario replayed
    # against both obsidian-fs ChangeFeed AND stub ChangeFeed; identical outcomes.

    queries:
      - id: atlas-staleness-stub-conformance
        adapters: [obsidian-fs, stub]
        events:
          - type: compile
            target: atlas-stub-test
            source_doc_ids:
              - obsidian-fs://atlas/projects/Atlas-1.md
              - obsidian-fs://atlas/projects/Atlas-2.md
            purpose: BRF-11 source-neutrality test
            prepared_text: |
              # Brief: Atlas Stub Test
              ## Synthesis
              This brief cites [[Atlas-1]] and [[Atlas-2]].
          - type: modify_source
            doc_id: obsidian-fs://atlas/projects/Atlas-1.md
            new_content_suffix: \n\n## Update from BRF-11 test\nModified content.
          - type: expect_stale
            target: atlas-stub-test
            changed_sources_contains:
              - obsidian-fs://atlas/projects/Atlas-1.md
        rationale: >
          BRF-11 guarantee — same staleness scenario passes when the stub
          connector is used as the change-feed source. Proves the daemon and
          brief-layer code do not assume obsidian-fs-specific behavior;
          future Notion adapter (Phase 10) gets BRF-11 coverage for free.
    ```

    Adapter-seam discipline: conformance.test.ts is inside src/adapters/source/ so it MAY import the adapters directly per Phase 1 ADP. The new test cases import BriefStalenessDaemon + handleCompileBrief + handleGetBrief from src/brief/ — those are domain modules and may be imported from adapter test files (test code, not production adapter code).
  </action>
  <verify>
    <automated>npx vitest run src/adapters/source/conformance.test.ts &amp;&amp; test -s evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml &amp;&amp; grep -q "obsidian-fs" evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml &amp;&amp; grep -q "stub" evals/fixtures/v2-test-vault/_queries/briefs-staleness-stub.yaml &amp;&amp; bash scripts/lint-adapters.sh</automated>
  </verify>
  <done>Conformance suite extended with brief + staleness assertions parametric over obsidian-fs + StubSource; briefs-staleness-stub.yaml populated; BRF-11 source-neutrality proven; full suite (npm test) stays green; lint-adapters zero hits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5-04-03: Tools-list snapshot regen (human-reviewed diff per VALIDATION.md)</name>
  <files>evals/v1-baseline/tools-list.snapshot.json</files>
  <what-built>
    Slices 1-3 + Task 5-04-01 added two new MCP tools (`compile_brief`, `get_brief`) and one new MCP Resource (`list_briefs`). `evals/v1-baseline/tools-list.snapshot.json` (the human-reviewed tool-surface snapshot) is currently outdated by 2 entries.
  </what-built>
  <how-to-verify>
    1. Run `npm test -- tools-list.snapshot 2>&amp;1 | head -50` — this should FAIL with a snapshot mismatch listing exactly two added entries.
    2. Confirm the diff is EXACTLY:
       - Added: `compile_brief` (with the inputSchema documented in 05-02-PLAN.md Task 5-02-02).
       - Added: `get_brief` (with the inputSchema documented in 05-02-PLAN.md Task 5-02-03).
       - NO removed entries.
       - NO modified entries (no v1 tool changed shape).
       - `list_briefs` Resource is NOT in this snapshot (Resources have their own discovery surface per MCP SDK 1.29; not in tools-list).
    3. If the diff matches, run `npx vitest run -u evals/v1-baseline/baseline.test.ts` (or whichever update flag the existing test harness uses) to regenerate the snapshot.
    4. Confirm the regenerated file has 34 tool entries (was 32 before Phase 5).
    5. Commit the regenerated snapshot in the same PR.
    6. Type `approved` if the diff is correct, or describe any unexpected additions/modifications.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues with the snapshot diff</resume-signal>
</task>

<task type="auto">
  <name>Task 5-04-04: PHASE-5-SIGN-OFF.md + ROADMAP update (phase gate)</name>
  <files>docs/v2/PHASE-5-SIGN-OFF.md, .planning/ROADMAP.md</files>
  <action>
    Create `docs/v2/PHASE-5-SIGN-OFF.md` mirroring the structural analog `docs/v2/PHASE-4-SIGN-OFF.md`. Required sections:

    1. **Header**: phase number, sign-off date, branch (`phase-5-compiled-brief-layer`), final tool count (34), commit range (rev-parse of branch start to HEAD).

    2. **Per-requirement status table**: one row per BRF-01..BRF-11. Each row: requirement ID, description (one line), shipping artifact (file paths), test coverage (vitest scope or eval YAML id), STATUS = ✅ green.
       ```
       | ID | Description | Artifact | Test | Status |
       |----|-------------|----------|------|--------|
       | BRF-01 | Document in _memory/_briefs/ with provenance properties | default-brief-v1 contract + src/brief/compile.ts | src/brief/compile.test.ts + default-brief-v1.test.ts | ✅ |
       | BRF-02 | Phase 5 ADR resolves LLM strategy | docs/v2/adr/005-brief-compile-strategy.md | doc review | ✅ |
       | BRF-03 | compile_brief MCP tool | src/brief/compile.ts | src/brief/compile.test.ts | ✅ |
       | BRF-04 | get_brief MCP tool with D-13 decision tree | src/brief/get.ts | src/brief/get.test.ts | ✅ |
       | BRF-05 | Staleness daemon subscribes to ChangeFeed | src/brief/daemon.ts | src/brief/daemon.test.ts + conformance.test.ts | ✅ |
       | BRF-06 | Single-owner via ~/.vault-memory/locks/<vault>.lock | src/brief/lock.ts | src/brief/lock.test.ts | ✅ |
       | BRF-07 | Startup replay handles missed events | src/brief/daemon.ts runStartupScan | src/brief/daemon.test.ts startup scenarios + conformance | ✅ |
       | BRF-08 | Rename-event preserves brief→source links | src/brief/daemon.ts handleCreate/handleDelete grace-window | conformance.test.ts rename test | ✅ |
       | BRF-09 | list_briefs as MCP Resource (not Tool) | src/brief/resources.ts | src/brief/resources.test.ts | ✅ |
       | BRF-10 | 20-doc eval — modify source, brief flips stale within one cycle | briefs-curated.yaml | eval runner | ✅ |
       | BRF-11 | Same scenario passes against stub ChangeFeed | briefs-staleness-stub.yaml + conformance.test.ts | eval + conformance | ✅ |
       ```

    3. **Tool count delta**: was 32 (post-Phase-4); now 34 (+compile_brief, +get_brief). Phase 5 Resources: +list_briefs (alongside existing list_sinks, memory_stats).

    4. **REL-08 hand-off to Phase 8**: REL-08 requires tool count ≤ 32. Phase 5 lifted to 34 to ship the brief-layer signature differentiator without compromising the API. Phase 8 retirement plan: promote 2 v1 tools to MCP Resources to reach 32 again. Recommended candidates (planner picks; for documentation only — Phase 8 makes the final call):
       - `list_backlinks` (`vault-memory://backlinks/{docId}` Resource)
       - `list_forward_links` (`vault-memory://forward-links/{docId}` Resource)
       These are good Resource candidates because they are pure-read discovery surfaces with no side effects, and Phase 4 made them fan out over `edges` (typed) — clients that need to filter by `type` can do so client-side. Phase 8 ADR will revisit.

    5. **Migration delta**: migration 013 shipped (chunks.chunk_id_fragment + brief_sources + daemon_state).

    6. **Cross-phase hand-offs**:
       - **Phase 6 (contracts DSL)**: contracts may reference `ChunkId` for chunk-level citations; `compile_brief` is the Phase 6 reference compiler binding. `prepared_text` tier (D-10 tier 3) is the on-ramp for Phase 6 contracts that don't need an LLM (deterministic synthesis).
       - **Phase 10 (Notion connector)**: BRF-11 cross-adapter conformance proves brief-layer source-neutrality. A future Notion ChangeFeed slots into `feed.subscribe()` with no brief-layer changes. The `ChangeFeed.since: cursor` hook (deferred) becomes relevant when Notion's polling cursor lands.

    7. **Known follow-ups** (deferred per CONTEXT.md `<deferred>` block — copy verbatim or summarize):
       - Block-level staleness (v3 with Notion).
       - Auto-recompile in get_brief (v2.x config flag if friction is real).
       - Cross-vault briefs (v3 / Phase 10).
       - brief_diff tool (caller can compute client-side).
       - LLM-generated summaries in list_briefs.

    8. **Test count delta**: before Phase 5 — 1211 tests; after Phase 5 — record actual final count (likely ~1240+). All green.

    9. **Sign-off commit**: by Oliver Wrede, date 2026-MM-DD, with the final SHA.

    Update `.planning/ROADMAP.md`:
    - Find the `### Phase 5: Compiled brief layer` section.
    - Flip the phase checkbox to `- [x]` (or whatever the existing ROADMAP convention uses).
    - Add a sign-off summary line under the phase header: `**Status:** ✅ shipped 2026-MM-DD — 34 tools + 3 Resources; all 11 BRF requirements green; signature differentiator operational. See docs/v2/PHASE-5-SIGN-OFF.md.`
    - Do NOT modify any other phase's content.

    Adapter-seam discipline: this task is pure documentation; no code changes.
  </action>
  <verify>
    <automated>test -f docs/v2/PHASE-5-SIGN-OFF.md &amp;&amp; grep -q "BRF-01" docs/v2/PHASE-5-SIGN-OFF.md &amp;&amp; grep -q "BRF-11" docs/v2/PHASE-5-SIGN-OFF.md &amp;&amp; grep -q "REL-08" docs/v2/PHASE-5-SIGN-OFF.md &amp;&amp; grep -q "Phase 5" .planning/ROADMAP.md &amp;&amp; npm test</automated>
  </verify>
  <done>PHASE-5-SIGN-OFF.md exists with all 11 BRF rows green; ROADMAP Phase 5 checkbox flipped + sign-off summary; full test suite green; Phase 5 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP client → list_briefs Resource | Untrusted `target` query parameter; substring match is non-injective so no SQL/path injection risk. SourceConnector.listDocuments is the read seam. |
| Conformance suite → daemon | Daemon code reused verbatim across obsidian-fs + stub; no adapter-specific branches in daemon.ts. |
| Snapshot regen → human review | The +2-tool diff MUST be reviewed by a human (VALIDATION.md §Manual-Only Verifications). Auto-update would let stealth additions slip through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information Disclosure | list_briefs Resource exposes private brief metadata via the `vault-memory://briefs` URI | mitigate | MCP Resource access is gated by the client's transport (stdio, local-only). No remote exposure. Same surface as existing list_sinks Resource. Audit log records resource reads if needed. |
| T-05-04-02 | Tampering | Snapshot regen lets two new tools enter the v1-baseline without human review | mitigate | `autonomous: false` on this plan; Task 5-04-03 is `checkpoint:human-verify` with `gate: blocking`. Human reviews diff before snapshot update commits. |
| T-05-04-03 | Integrity | BRF-11 stub conformance passes locally but fails on real Notion adapter (Phase 10) | accept | Stub is a TEST DOUBLE, not a production substitute. Phase 10 will add its own conformance assertions when the Notion connector lands. BRF-11 proves the brief-layer has no obsidian-fs-specific branches; it does not certify any specific future adapter. |
| T-05-04-04 | Repudiation | PHASE-5-SIGN-OFF.md claims requirements green without test evidence | mitigate | Sign-off table requires a Test column with vitest scope or eval YAML id per row. CI must be green at sign-off commit time. |
| T-05-04-SC | Tampering | No npm installs in slice 4 | N/A | All deps pre-installed. No supply-chain checkpoint needed. |
</threat_model>

<verification>
**Acceptance:**
- `npm test` — 1211-test floor holds + new tests for list_briefs resource + conformance extension (4 new tests × 2 adapters = 8 new test runs).
- `npx tsc --noEmit` — clean.
- `bash scripts/lint-adapters.sh` — zero hits outside adapter dirs.
- `npm run eval:baseline` — green; tool-list snapshot regenerated additively to 34 entries.
- Manual: PHASE-5-SIGN-OFF.md read for completeness; ROADMAP checkbox flipped; sign-off commit on `phase-5-compiled-brief-layer` branch ready for merge to `main`.

**Eval queries:** All three Phase 5 YAMLs populated:
- briefs-curated.yaml (slice 2 + slice 3 staleness scenario).
- briefs-from-cluster.yaml (slice 3).
- briefs-staleness-stub.yaml (slice 4, BRF-11).

**Snapshot checks:** Tool count lifts 32 → 34 (additive: compile_brief + get_brief). list_briefs is a Resource (separate MCP discovery surface). Snapshot regen is human-reviewed per Task 5-04-03 checkpoint.

**Wave 0 stub references:** This slice fills the previously-empty `src/brief/resources.test.ts` from slice 1 and exercises the conformance.test.ts extension end-to-end.
</verification>

<success_criteria>
1. `src/brief/resources.ts` implements `readListBriefs` as a pure function over MemorySinkRegistry + VaultManager + SourceConnector; enumerates via `SourceConnector.listDocuments` (no raw fs); filters by `properties.type === "brief"`.
2. `list_briefs` MCP Resource registered at `vault-memory://briefs` with optional `?target=<pattern>` query param; response shape `{total, briefs: ListBriefEntry[]}`.
3. `src/adapters/source/conformance.test.ts` extended with brief + staleness assertions parametric over obsidian-fs + StubSource; all 4 conformance test cases green for both adapters (8 test runs).
4. `briefs-staleness-stub.yaml` populated with the BRF-11 cross-adapter scenario.
5. `evals/v1-baseline/tools-list.snapshot.json` regenerated additively to 34 entries; human-reviewed diff is exactly +compile_brief + +get_brief (Task 5-04-03 checkpoint).
6. `docs/v2/PHASE-5-SIGN-OFF.md` exists with: BRF-by-BRF status table (all 11 green), tool count delta (32 → 34), Phase 8 REL-08 retirement plan, cross-phase hand-offs (Phase 6, Phase 10).
7. `.planning/ROADMAP.md` Phase 5 checkbox flipped + sign-off summary line added.
8. `npm test` + `npx tsc --noEmit` + `bash scripts/lint-adapters.sh` + `npm run eval:baseline` all green.

**After this slice, agents can:** discover briefs by target slug via the `vault-memory://briefs` Resource. The brief-layer is proven source-neutral via cross-adapter conformance (BRF-11) so Phase 10 Notion connector inherits the staleness guarantee for free. Phase 5 is signed off; the signature differentiator of vault-memory v2 is shipped.
</success_criteria>

<commit>
Atomic commit messages (one per task):

```
feat(05-04): list_briefs MCP Resource (BRF-09)

- src/brief/resources.ts — readListBriefs(deps, opts?) pure function
  over MemorySinkRegistry + VaultManager + SourceConnector. No raw fs.
  Mirrors src/memory/resources/list-sinks.ts shape.
- src/server.ts — register at vault-memory://briefs with optional
  ?target=<pattern> substring filter. Response: {total, briefs:[...]}.

Refs: BRF-09
```

```
test(05-04): cross-adapter conformance + briefs-staleness-stub.yaml (BRF-11)

- src/adapters/source/conformance.test.ts — extended with brief +
  staleness suite inside the existing describe.each parametric block.
  4 test cases × 2 adapters (obsidian-fs + StubSource) = 8 test runs.
  Proves brief-layer code has no obsidian-fs-specific branches.
- briefs-staleness-stub.yaml populated with BRF-11 cross-adapter
  scenario (events: compile → modify_source → expect_stale).

Refs: BRF-11
```

```
chore(05-04): tools-list snapshot regen (additive +2 tools)

Human-reviewed diff per VALIDATION.md §Manual-Only Verifications:
- Added: compile_brief, get_brief.
- list_briefs Resource not in tools snapshot (separate MCP discovery).
- No removed or modified v1 entries.
- Tool count: 32 → 34. Phase 8 retirement plan in PHASE-5-SIGN-OFF.md.

Refs: BRF-03, BRF-04, BRF-09, REL-08 (Phase 8 hand-off)
```

```
docs(05-04): Phase 5 sign-off (all 11 BRF green; signature differentiator shipped)

- docs/v2/PHASE-5-SIGN-OFF.md — per-requirement status table (BRF-01..
  BRF-11 all green), tool count delta (32 → 34), REL-08 hand-off to
  Phase 8, cross-phase hand-offs (Phase 6 contracts DSL, Phase 10
  Notion connector), known follow-ups copied from CONTEXT deferred.
- .planning/ROADMAP.md — Phase 5 checkbox flipped + sign-off summary.

Refs: BRF-01..BRF-11
```
</commit>

<output>
Create `.planning/phases/05-compiled-brief-layer/05-04-SUMMARY.md` when done.
</output>
