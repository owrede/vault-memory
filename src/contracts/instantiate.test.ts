import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-03 (instantiate_contract
// orchestrator per CON-06 / ADR-006 §Decision 7).
describe("instantiate_contract (CON-06)", () => {
  it.todo("filled in by the slice referenced above");
});
