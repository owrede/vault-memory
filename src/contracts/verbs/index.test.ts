import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-03 (closed assembly
// verb dispatcher per D-A2a / ADR-006 §Decision 2 / Invariant C-1).
describe("verbs dispatcher (D-A2a baseline)", () => {
  it.todo("filled in by the slice referenced above");
});
