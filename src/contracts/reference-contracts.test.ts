import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-04 (eval scenarios for
// the three reference contracts per CON-07 / CON-08 / Q-CI-LLM /
// ADR-006 §Decision 13).
describe("reference contracts eval (CON-07/08)", () => {
  it.todo("filled in by the slice referenced above");
});
