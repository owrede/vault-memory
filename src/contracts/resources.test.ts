import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-04
// (vault-memory://contract-verbs/{vault} resource per CON-04 / D-A2b /
// ADR-006 §Decision 4).
describe("contract-verbs resource (CON-04, D-A2b)", () => {
  it.todo("filled in by the slice referenced above");
});
