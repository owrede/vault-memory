import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-03 (mustache template
// resolver per D-A2c / ADR-006 §Decision 4 / Invariant C-7).
describe("resolveTemplate (D-A2c, C-7)", () => {
  it.todo("filled in by the slice referenced above");
});
