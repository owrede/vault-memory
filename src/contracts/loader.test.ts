import { describe, it } from "vitest";
// Wave-0 stub — implementation lands in Plan 06-02 (loader + ChangeFeed
// hot reload per D-LOAD / ADR-006 §Decision 7).
describe("startContractRegistry (D-LOAD, CON-01 round-trip)", () => {
  it.todo("filled in by the slice referenced above");
});
