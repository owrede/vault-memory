import { describe, it } from "vitest";

/**
 * Phase 5 / BRF-01 / BRF-03 — `handleCompileBrief` test scaffolding.
 *
 * Wave 0 stub. Behavior tests land in Plan 05-02 alongside the
 * `src/brief/compile.ts` implementation (capability-first LLM ladder,
 * source-doc resolution, BriefBodyValidator, recompile auto-supersede).
 */
describe("handleCompileBrief (BRF-01, BRF-03)", () => {
  it.skip("[scaffold] tests land in slice 2 (Plan 05-02)", () => {});
});
