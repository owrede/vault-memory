import { describe, it } from "vitest";

/**
 * Phase 5 / BRF-09 — `list_briefs` MCP Resource scaffolding.
 *
 * Wave 0 stub. Behavior tests for resource registration, target-pattern
 * filtering, and shape (briefs[].{doc_id, target, purpose,
 * compiled_at, status, source_count, age_days}) land in Plan 05-04.
 */
describe("list_briefs MCP Resource (BRF-09)", () => {
  it.skip("[scaffold] tests land in slice 4 (Plan 05-04)", () => {});
});
